# 2026-08-12 开发日志

## 项目全面审查

- 代理：Codex 首席架构审查。
- 范围：当前 `main` / `origin/main` 工作区、冻结文档约束、任务表、迁移、后端测试、前端验证环境、部署静态检查、关键导出/导入/自动编排实现。
- 产出：新增 `work_sprint/项目审查-2026-08-12.md`。

### 验证结果

- `cargo fmt --all -- --check`：通过。
- `cargo test -p rustfs-transfer-common`：通过。
- `cargo test -p rustfs-transfer-center`：通过。
- `cargo test -p rustfs-transfer-edge`：通过。
- `powershell -ExecutionPolicy Bypass -File scripts/check-deploy.ps1`：通过。
- `npm run typecheck` / `npm run build`（双端前端）：未通过，原因是当前环境缺少 `vue-tsc` 可执行依赖，需先安装前端依赖后复测。

### 主要发现

- 当前工作区干净，`main` 与 `origin/main` 对齐；没有残留本地开发分支。
- Git 命令有用户级 ignore 权限警告，会污染脚本输出。
- `TASK-S1-WEB-EDGE-PROD-001` 顶部总览和任务卡片状态不一致。
- `TASK-S1-EDGE-REALTIME-QA-001` 未开始，真实 Edge 实时可视化端到端验收仍是最大缺口。
- `TASK-S1-INTEGRATION-001` 仍未完成，空库迁移和主闭环联调记录缺失。
- Edge 封盘后删除 `disk_runtime` 的策略需要确认是否影响刷新后“已封盘可拔盘”展示。
- 跨盘分块能力仍需真实端到端验收或明确登记为 v1.0 风险边界。

### 待跟进

- 安装前端依赖并复跑双端 typecheck/build。
- 修正 `work_sprint/TASKS.md` 状态不一致。
- 完成 Edge 真实前后端实时可视化 QA。
- 在空 PostgreSQL 执行双端迁移并补主闭环验收记录。

## Center 移除 center_config 表

- 代理：Codex 后端开发程序员。
- 范围：用户已明确授权修改冻结文档；仅移除 `center_config` 表设计和 Center 后端依赖该表的读取逻辑，不做部署、不碰真实磁盘、不做盘操作。
- 完成内容：
  - 从冻结数据库设计、架构表清单和协议字段映射中剔除 `center_config` 表，`disk_info.json.center.center_id` 改为来自中控配置文件 / 部署配置。
  - 从 `sql/center/0001_init_center.sql` 删除 `center_config` 表、注释和唯一索引；`signature_key.owner_code` 注释改为对应部署配置中的中控系统 ID。
  - Center 配置新增 `[center] center_id / center_name / protocol_version`，支持环境变量覆盖。
  - Center 初始化盘、重新初始化盘、Dashboard summary 和 readiness 不再读取 DB 单行配置；初始化写入的 `disk_info.json.center.center_id` 来自运行配置。
  - 调整测试，覆盖没有 DB 身份配置行时服务仍可 ready，并能按配置中心 ID 写入 `disk_info.json`。
- 验证方式：
  - `cargo fmt --all -- --check`：通过。
  - `cargo test -p rustfs-transfer-center`：通过，73 个 lib/main 测试、14 个 edge_auth 测试、11 个 import_worker 测试全部通过；仅有 Windows linker stdout 警告。
  - `rg -n "center_config|CenterConfigRecord" docs/v1.0冻结 sql/center crates/center-backend deploy/config work_sprint/TASKS.md`：无结果。
- 待跟进：
  - 尚未连接空 PostgreSQL 执行迁移验收。
  - 生产部署前必须为每个中控实例配置稳定且不重复的 `[center].center_id`，不得使用示例 UUID。

## Edge 后端 center_config 影响与封盘刷新缺口复核

- 代理：Codex Edge 边缘端后端开发工程师。
- 范围：复核 Edge 后端是否依赖 Center `center_config` 表，检查自动导出、WebSocket 事件、浏览器只读 summary 与封盘后可拔盘展示。
- 完成内容：
  - 确认 Edge 后端没有 `center_config` 表或 `CenterConfigRecord` 依赖；`crates/edge-backend/src/config.rs` 中的 `CenterConfig` 仅是边缘端本地中控 HTTP 连接配置。
  - 复核 Edge 自动导出仍通过 `summary -> scan -> create_export_job -> start_export_job` 复用受控能力，不绕过 Center verify/export-key。
  - 复核 WebSocket 事件覆盖 `DISK_*`、`SCAN_*`、`COPY_*`、`SEAL_DONE`，payload 使用 `disk_status_code`、`runtime_status`、`export_job_status`、`object_status` 等语义字段。
  - 修复封盘成功后删除 `disk_runtime` 的缺口：改为保留记录并更新 `runtime_status = DONE`，清空盘级错误，避免页面刷新后 HTTP summary 丢失已封盘可拔盘展示来源。
- 修改文件：
  - `crates/edge-backend/src/disk_worker.rs`
  - `crates/edge-backend/src/export_runtime.rs`
  - `crates/edge-backend/tests/disk_worker_tests.rs`
- 验证方式：
  - `rg -n "center_config|CenterConfigRecord" crates/edge-backend sql/edge deploy/config/edge.example.toml -S`：无结果。
  - `rg -n "clear_disk_runtime|CLEAR_DISK_RUNTIME_AFTER_SEAL_SQL|DELETE FROM disk_runtime WHERE disk_id = \$1|cleared edge disk runtime|cleared_runtime" crates/edge-backend -S`：无结果。
  - `cargo fmt --all -- --check`：通过。
  - `cargo test -p rustfs-transfer-edge`：通过；65 个单元测试、1 个 `disk_worker_tests`、2 个 `scanner` 测试全部通过；仅有 Windows linker stdout 警告。
- 残留问题：
  - 尚未在 VM 中跑真实插盘自动导出与浏览器端到端可视化验收。
  - `TASK-S1-EDGE-REALTIME-QA-001` 仍需主会话协调真实环境、前端页面和验收记录。

## TASK-S1-WEB-EDGE-PROD-001 Edge 前端生产态验收补齐

- 代理：Codex Edge 前端开发/审查。
- 范围：`web/edge-web` Dashboard、同步记录、HTTP summary / export records / WebSocket 前端契约；不做部署、不碰真实磁盘、不调用受控写接口。
- 完成内容：
  - 移除 Dashboard 中“执行恢复检查”生产不允许入口；异常盘只展示后端错误，不提供浏览器写操作按钮。
  - HTTP summary 失败时展示独立错误态，并隐藏扫描/导出零值指标面板，避免把空 summary 渲染成假进度、假盘位或假对象。
  - WebSocket 事件白名单补齐 `SCAN_STARTED`、`SCAN_PROGRESS`、`SCAN_DONE`、`COPY_STARTED`，避免丢弃后端真实阶段事件。
  - 当封盘后 `disk_runtime` 被清理时，仅在后端 `export_job_status = SEALED` 的前提下展示“已封盘，可拔盘”兜底；同步记录详情中缺少 runtime 的历史封盘盘位归一化为 `disk_status_code = SEALED`、`runtime_status = DONE`。
  - 复查前端路径仅使用 `/api/edge/dashboard/summary`、`/api/edge/dashboard/export-jobs`、详情子路径和 `/ws/edge/progress`；未发现 `disk_data_key`、`data_key_id`、nonce、tag、控制 token 暴露或展示。
- 验证方式：
  - `npm ci`：通过，按 `package-lock.json` 恢复依赖；npm 提示 esbuild/vue-demi postinstall 尚未审批，未放宽脚本权限。
  - `npm run test:unit`（edge-web）：通过，13 个测试全部通过。
  - `npm run typecheck`（edge-web）：通过。
  - `npm run build`（edge-web）：通过。
- 残留问题：
  - 未连接真实 Edge/VM 浏览器执行端到端插盘截图或录屏验收；`TASK-S1-EDGE-REALTIME-QA-001` 仍需主会话协调。
  - `TASK-S1-WEB-EDGE-PROD-001` 已同步为 `[x]`，但真实插盘/拔盘浏览器截图仍归属端到端 QA，不在本次安全只读前端收口范围内。

## TASK-S1-EDGE-REALTIME-QA-001 Edge 实时可视化 QA 准备

- 代理：Codex Edge 边缘端测试工程师。
- 范围：只读制定 Edge 真实实时可视化链路验收计划，并执行非破坏性本地和环境前置探测；未插盘、未导出、未初始化、未清盘、未手工修改数据库。
- 完成内容：
  - 阅读任务卡和冻结协议相关约束，确认验收路径为 `DISK_DETECTED -> DISK_CHECKING -> DISK_READY -> SCAN_* -> COPY_* -> SEAL_DONE`。
  - 新增 `work_sprint/edge_realtime_acceptance.md`，记录前置条件、服务检查、测试文件、WS 事件序列、HTTP summary 检查和截图/录像记录点。
  - 确认当前 worktree 根目录没有 `.local.env`；原始工作目录存在 `.local.env`，仅做变量名级别确认，未记录敏感值。
  - 本地 Downloads 可见近期设计和视频素材；未发现明确命名的 Edge 导出测试对象，需要正式执行前指定或准备测试 bucket 对象。
  - SSH config 中未配置 `fustfs_vm_codex`；使用 `.local.env` 中 Edge 连接信息尝试只读 SSH，认证失败，未进入 VM。
- 验证方式：
  - `cargo test -p rustfs-transfer-edge`：通过，65 个 lib/main 测试、1 个 disk worker 集成测试、2 个 scanner 集成测试和 doc test 全部通过；仅有 Windows linker stdout 警告。
  - `npm run test:unit`（`web/edge-web`）：通过，11 个前端契约测试全部通过。
  - `npm run typecheck`（`web/edge-web`）：未通过，当前环境缺少 `vue-tsc` 可执行依赖。
  - `npm run build`（`web/edge-web`）：未通过，同样缺少 `vue-tsc` 可执行依赖。
- 待跟进：
  - 主会话或用户需修复/提供 Edge VM SSH 免密访问，确认部署版本、服务名、Dashboard 访问地址和录屏方式。
  - 正式端到端前需安装项目本地前端依赖并复跑 Edge typecheck/build。
  - 正式执行阶段需用户明确授权真实插盘和自动导出，并由主会话协调 Center 初始化运输盘。

## 集成窗口合并记录

- 代理：Codex 集成工程师。
- 范围：合并 6 个 detached worktree 的必要改动到当前主线工作区；不部署、不碰真实磁盘、不执行真实导出/导入/初始化/重初始化。
- 合并内容：
  - Center 后端：补齐 `/api/disk/verify` 使用运行配置 `protocol_version`，并扩展配置协议版本测试。
  - Center 前端与 Center 测试窗口：未发现需要合入的代码产物；`node_modules` 等依赖产物不合并。
  - Edge 后端：封盘成功后 `disk_runtime` 改为保留并标记 `DONE`。
  - Edge 前端：合入 HTTP 错误态、移除恢复检查入口、封盘历史兜底、WS 事件白名单和前端契约测试。
  - Edge 测试：合入 `work_sprint/edge_realtime_acceptance.md`，`TASK-S1-EDGE-REALTIME-QA-001` 保持未开始。
  - `work_sprint/TASKS.md` 同步 `TASK-S1-WEB-EDGE-PROD-001` 总览和任务卡片为 `[x]`；未把真实 QA 标成完成。
- 验证方式：
  - `cargo fmt --all -- --check`：通过。
  - `cargo test -p rustfs-transfer-center`：通过，73 个 lib/main 测试、14 个 edge_auth 测试、11 个 import_worker 测试全部通过；仅有 Windows linker stdout 警告。
  - `cargo test -p rustfs-transfer-edge`：通过，65 个 lib/main 测试、1 个 disk worker 集成测试、2 个 scanner 集成测试和 doc test 全部通过；仅有 Windows linker stdout 警告。首次与 center 测试并行时因 build directory file lock 超时，单独重跑通过。
  - `npm ci`（`web/center-web`、`web/edge-web`）：通过；未提交 `node_modules`，npm 提示 esbuild/vue-demi postinstall 尚未审批，未放宽脚本权限。
  - `npm run typecheck`（双端前端）：通过。
  - `npm run build`（双端前端）：通过。
  - `rg -n "center_config|CenterConfigRecord" docs/v1.0冻结 sql/center crates/center-backend deploy/config work_sprint/TASKS.md`：无结果。
  - `rg -n "clear_disk_runtime|CLEAR_DISK_RUNTIME_AFTER_SEAL_SQL|DELETE FROM disk_runtime WHERE disk_id = \$1|cleared edge disk runtime|cleared_runtime" crates/edge-backend`：无结果。
- 待跟进：
  - `TASK-S1-EDGE-REALTIME-QA-001` 仍需真实环境、插盘窗口和用户明确授权。

## Edge 离线打包需求调整方案文档

- 代理：Codex 需求方案整理。
- 范围：按用户新约束整理 Edge 不联网后的可实施方案；只新增 `docs/` 下补充文档，不修改 `docs/v1.0冻结/`。
- 完成内容：
  - 确认项目实际 Edge 配置字段为 `center.base_url`、`center.edge_code`、`center.auth_key_id`、`center.edge_auth_secret`、`center.edge_auth_secret_env`。
  - 新增 `docs/Edge离线打包需求调整方案.md`，明确不新增数据库字段、不新增盘内字段，复用 `center.edge_auth_secret` 作为部署阶段固定授权 key。
  - 明确 Edge 删除运行期 Center `/api/edge/auth`、`/api/disk/verify`、`/api/disk/export-key` 和对象账本联网查询主流程。
  - 明确 Edge 与 Center 使用同一 HMAC-SHA256 派生规范从现有字段派生本次封盘 `disk_data_key`。
- 验证方式：
  - 文档级变更，未运行代码测试。
- 待跟进：
  - 后续实现需同步更新 `work_sprint/TASKS.md` 新增 P0 任务，并删除 Edge 导出路径中的 Center 联网依赖。

## TASK-S1-P0-EDGE-OFFLINE-PACK-001 架构协调领取

- 代理：Codex 首席系统架构师主会话。
- 范围：只做架构协调、任务拆分、任务表/日志同步和后续窗口监控；不直接编码、不部署、不碰真实磁盘、不执行真实初始化、导出、导入、清理或重新初始化。
- 现场保护：
  - 当前工作区已有未提交改动，包含上一轮 `center_config` 移除、Edge 封盘运行态保留、Edge Web 生产态收口、验收计划、冻结文档授权修改和若干 `.codex-vmware-*` 临时文件。
  - 这些改动视为既有工作，不得覆盖、回退或纳入无关任务。
- 完成内容：
  - 阅读 `docs/Edge离线打包需求调整方案.md`，确认本次是冻结文档补充方案，不修改 `docs/v1.0冻结/`。
  - 在 `work_sprint/TASKS.md` 新增并领取 P0 任务 `TASK-S1-P0-EDGE-OFFLINE-PACK-001`，顶部总览和任务卡片均同步为 `[~]`。
  - 将 `TASK-S1-EDGE-REALTIME-QA-001` 的依赖前置到离线打包任务之后，避免继续按旧的 Center verify/export-key 联网主流程验收。
  - 明确 Edge 后端、Center 后端、部署配置、测试验证责任边界和验收矩阵。
- 当前风险：
  - 现有 `TASK-S1-EDGE-AUTO-001` 验收条目仍写有 Center verify/export-key 失败时不得导出的旧约束，后续实现窗口需同步修订任务卡或补充说明。
  - AGENTS.md 安全约束仍写明 `edge_auth_secret` 只用于 HTTP API 鉴权、不用于文件加密；本次用户补充方案已明确授权其作为派生输入，后续实现必须只作为 HMAC 派生 key，不得直接作为 AES 密钥。
  - Center 端现有 `/api/edge/auth`、`/api/disk/verify`、`/api/disk/export-key` 测试可能仍代表旧联网主流程，需要分清删除主流程、保留非主流程测试或改写测试的边界。
- 计划验证矩阵：
  - `cargo fmt --all -- --check`
  - `cargo test -p rustfs-transfer-edge`
  - `cargo test -p rustfs-transfer-center`
  - 如修改 Edge/Center 前端展示或配置文档，再运行对应 `npm run typecheck` 和 `npm run build`，并做部署示例静态检查。
- 待跟进：
  - 创建并指派 Edge 后端、Center 后端、部署配置/安全审计、测试验证窗口推进实现。
  - 实现完成后创建集成窗口合并多窗口改动，避免 detached worktree 后续冲突。

## TASK-S1-P0-EDGE-OFFLINE-PACK-001 多窗口集成完成

- 代理：Codex 集成工程师。
- 范围：将 Edge 后端、Center 后端、部署安全审计、测试验证四个 detached worktree 的必要改动合入当前主线工作区；不部署、不碰真实磁盘、不执行真实插盘、初始化、导出、导入、清盘或重初始化。
- 来源盘点：
  - `C:\Users\Beipy\.codex\worktrees\9441\RustFSDisk`：Edge 离线打包后端改动；排除 `.codex-vmware-*` 临时截图/状态文件。
  - `C:\Users\Beipy\.codex\worktrees\d22c\RustFSDisk`：Center 离线派生导入改动；排除无关临时文件。
  - `C:\Users\Beipy\.codex\worktrees\09c5\RustFSDisk`：部署配置、安全审计、脚本和上线说明改动；排除临时产物和依赖产物。
  - `C:\Users\Beipy\.codex\worktrees\fc30\RustFSDisk`：非破坏性验证记录；合入 `work_sprint/edge_offline_pack_validation.md` 并追加集成后复测结论。
- 合并内容：
  - Edge 后端：移除旧 `center_client.rs` 与 crate 导出；生产路径不再构造 `CenterHmacClient`；插盘准入改为本地离线判断；导出 Worker 用 HMAC-SHA256 派生本次封盘 `disk_data_key`。
  - Center 后端：新增同规范离线派生函数；导入 Worker 使用 ACTIVE Edge secret 派生解密 key；导入阶段补齐 Edge ACTIVE、运输盘启用、data_key 绑定、错误 key、重复导入等权威判断。
  - 部署与文档：更新 `deploy/config/edge.example.toml`、`edge.env.example`、`scripts/check-deploy.ps1`、本地脚本说明和 `docs/上线要求.md` 的离线导出口径。
  - 任务与验证：`TASK-S1-P0-EDGE-OFFLINE-PACK-001` 顶部总览和任务卡片均同步为 `[x]`；验收清单按复测结果勾选。
- 验证方式：
  - `cargo fmt --all -- --check`：通过。
  - `cargo test -p rustfs-transfer-edge`：通过，63 个库/单元测试、1 个 disk worker 集成测试、2 个 scanner 集成测试和 doc tests 通过；仅 Windows linker stdout warning。
  - `cargo test -p rustfs-transfer-center`：通过，73 个库/单元测试、14 个 edge_auth 测试、15 个 import_worker 测试和 doc tests 通过；仅 Windows linker stdout warning。
  - `powershell -ExecutionPolicy Bypass -File scripts/check-deploy.ps1`：通过。
  - `rg -n "CenterHmacClient|export_key\(|/api/edge/auth|/api/disk/verify|/api/disk/export-key" crates/edge-backend/src crates/edge-backend/tests -S`：无结果。
  - 敏感字段静态审计：`edge_auth_secret` / `disk_data_key` 命中仅限配置读取、内存派生/加密、测试断言和文档说明；WS/HTTP summary 路径为“不返回”测试断言，未发现实际输出。
- 取舍与残留：
  - Center 端旧 HMAC API 测试仍保留，作为 Center 兼容接口覆盖；Edge 主流程已不引用。
  - 未清理 `.codex-vmware-*` 临时文件，避免删除未识别现场产物；最终提交前应由主会话确认是否忽略或清理。
  - 未做真实部署、真实插盘或端到端浏览器录屏验收；这些仍归属 `TASK-S1-EDGE-REALTIME-QA-001` 和主会话协调。

## TASK-S1-EDGE-REALTIME-QA-001 两台 VM 从零主流程验收领取

- 代理：Codex 首席系统架构师主会话协调；专门 VM 验收执行窗口负责实施。
- 用户授权：用户明确要求“现在去两台 linux 虚拟机上做主流程从零测试”。
- 范围：
  - 在两台 Linux VM 上执行从零主流程验收：服务配置检查、数据库/RustFS 连通、Center/Edge 服务状态、初始化盘、Edge 离线准入、扫描、导出、封盘、Center 导入、去重/状态记录和必要 Dashboard/WS 观察。
  - 主会话不直接部署、不直接执行盘操作；由专门执行窗口按边界实施并实时汇报。
  - 不泄露 `.local.env`、SSH、数据库、RustFS 密钥、token、密码、私钥或完整连接串。
- 任务状态：
  - `TASK-S1-EDGE-REALTIME-QA-001` 顶部总览和任务卡片已同步为 `[~]`。
- 前置确认：
  - `TASK-S1-P0-EDGE-OFFLINE-PACK-001` 已合入主工作区并通过 `cargo fmt`、Edge/Center 测试、部署静态检查。
  - Edge 运行期联网 verify/export-key 主流程已移除，当前验收应按离线打包方案执行。
- 待跟进：
  - 创建 VM 主流程验收执行窗口。
  - 验收完成后回写截图/日志摘要、命令结果、失败点和是否可将任务标为 `[x]`。

## TASK-S1-EDGE-REALTIME-QA-001 VM 从零主流程验收完成与热修集成

- 代理：Codex 多窗口集成工程师；来源 VM 验收窗口 `019ff3bc-1ee5-7a51-9698-61066ca179f4`。
- 范围：只同步验收结论、任务状态和 Edge 后端最小 bugfix；主工作区未执行部署、插盘、初始化、导出、导入、清盘或重初始化。
- 合入修复：`crates/edge-backend/src/control.rs` 中导出任务磁盘汇总 SQL 的 `SUM(...)` 显式 cast 为 `::BIGINT`，避免 PostgreSQL 返回 NUMERIC 后由 Rust 按 `i64` 解码 panic。
- VM 验收摘要：
  - Edge 侧从 `INITIALIZED` 运输盘进入 `READY`，扫描形成 59 个稳定对象，导出完成并封盘为 `SEALED`，`.partial=0`，Edge 对象状态无 `IMPORTED`。
  - Center 侧导入完成，导入结果为 `IMPORTED`，对象账本与归档对象数量/字节数一致；重复导入返回已完成跳过语义，计数保持不变。
  - HTTP summary、journald、只读数据库、只读 S3 和只读盘内文件证据均未记录密钥、token、密码、私钥或完整连接串。
  - 本轮未采集浏览器截图，以 Edge HTTP summary、服务日志和只读账本证据覆盖实时状态恢复与事件序列。
- 验证方式：VM 窗口已通过 `cargo fmt --all -- --check`、`cargo test -p rustfs-transfer-edge`、`cargo test -p rustfs-transfer-center` 和真实主流程验收；本集成窗口复跑 `cargo fmt --all -- --check`、`cargo test -p rustfs-transfer-edge`、`cargo test -p rustfs-transfer-center` 均通过。
- 任务状态：`TASK-S1-EDGE-REALTIME-QA-001` 顶部总览和任务卡片同步为 `[x]`。
## 主工作区测试通过代码集成收口

- 代理：Codex 多窗口集成工程师。
- 范围：将已测试通过的 VM 验收热修、Edge 后端拔盘语义修复和 Edge 前端联调修正合入 `F:\work\RustFSDisk` 主工作区；不提交、不 push、不部署、不碰真实磁盘。
- 合入内容：
  - VM 主流程验收窗口：确认 `crates/edge-backend/src/control.rs` 的导出任务磁盘汇总 `SUM(...)::BIGINT` 热修已在主工作区，`TASK-S1-EDGE-REALTIME-QA-001` 保持 `[x]`。
  - Edge 后端拔盘语义：移植缺席盘 rescan 后结算 `REMOVED`、dashboard summary 只读最新非 `REMOVED` 快照、`DISK_REMOVED` WebSocket 事件剔除已拔盘旧快照，并补单测；该代码已按用户要求先合入，仍待部署到 Edge VM 后做真实拔插复测。
  - Edge 前端联调修正：盘位卡片 SN 固定左下角、拒绝/错误卡片不再显示“悬浮查看原因”正文、Dashboard 每 3 秒重拉 HTTP summary、`DISK_*` 事件使用事件返回的 `disks[]` 替换当前盘列表。
- 排除内容：未合入 `.codex-*`、`.codex-vmware-*`、截图/缓存、`target`、`node_modules`、`.env.local`、密钥、token、连接串、SSH 配置、VM 临时脚本或真实部署产物。
- 验证方式：
  - `cargo fmt --all -- --check`：通过。
  - `cargo test -p rustfs-transfer-edge`：通过，包含新增拔盘 REMOVED 结算和 WS 剩余盘快照测试；仅 Windows linker stdout warning。
  - `cargo test -p rustfs-transfer-center`：通过；仅 Windows linker stdout warning。
  - `npm ci`（`web/edge-web`）：通过；未审批 esbuild/vue-demi postinstall 脚本。
  - `npm run typecheck`（`web/edge-web`）：通过。
  - `npm run build`（`web/edge-web`）：通过。
- 待跟进：
  - Edge 后端拔盘语义需要部署到 Edge VM 后做非破坏性真实插入/拔出复测，确认 udev/rescan 调用链可触发缺席盘结算。
  - Dashboard summary 聚合语义仍需在真实环境观察：终态 summary、WS 终态事件和导出记录三者是否完全一致。
## Edge 当前盘事实源与离线准入修复主工作区合入

- 代理：Codex 多窗口集成工程师。
- 来源窗口：`019ff1db-e185-7982-9602-8ee9372f143e`，来源 worktree `C:\Users\Beipy\.codex\worktrees\609c\RustFSDisk`。
- 范围：只合入测试/VM 验证通过的 Edge 后端、rescan/udev 和脱敏记录；不提交、不 push、不部署、不碰真实磁盘。
- 合入内容：
  - Edge 检测准入以 OS 可见协议盘为当前盘事实源，先发布/落库 `DETECTED`、`CHECKING`，再按本地 ext4、`disk_info.json`、protocol、`center_signature`、硬件 SN、partial 和 `disk_status_code` 判定 `READY` 或 `REJECTED`。
  - 移除插盘准入路径中的 Center verify 类型绑定；Center DNS 不通不再导致当前盘列表为空。
  - `/api/edge/dashboard/summary` 返回前执行 `control_refresh` 只读 rescan；该触发源不启动自动导出。
  - 保留并整合 `REMOVED/DISK_REMOVED` 语义：缺席盘结算为 `REMOVED`，summary 过滤，WS 当前盘快照剔除旧 `mount_path`。
  - summary 文件系统元数据只取命令输出第一条非空行，避免 `filesystem_type` 出现重复换行。
  - udev 规则补充分区 add/remove/change 事件，只通知 rescan systemd template，不直接执行业务操作。
- 排除内容：未合入 `.local.env`、`.env.local`、`.codex-*`、`.codex-vmware-*`、VM 源码包、`target`、`node_modules`、SSH/密钥/连接串或部署产物二进制；未合入来源 worktree 中无关 Center/冻结文档旧改动。
- 验证方式：
  - `cargo fmt --all -- --check`：通过。
  - `cargo test -p rustfs-transfer-edge`：通过，65 个 lib/main/bin 单元测试、1 个 disk_worker 集成测试、2 个 scanner 集成测试和 doc tests 全部通过；仅有 Windows linker stdout warning。
  - `rg -n "OfflineDiskVerifier|CenterDiskVerifier|verify_disk\(|/api/disk/verify|CenterHmacClient|export_key\(" crates/edge-backend/src crates/edge-backend/tests -S`：无结果。
- 待跟进：
  - 用户称插了两块盘，但 VM OS 只读检查只看到 1 个协议盘分区；需要主会话协调确认第二块盘是否真正接入 VM、挂载并包含 `rustfs-transfer/disk_info.json`。
  - 真实物理拔盘 WS 验证仍需用户配合拔盘；本地单测覆盖 `DISK_REMOVED` 事件不含旧 `mount_path`。

## Edge 未初始化 ext4 候选盘 summary 修复主工作区对账合入

- 代理：Codex 多窗口集成工程师。
- 来源窗口：`019ff1db-e185-7982-9602-8ee9372f143e`，来源 worktree `C:\Users\Beipy\.codex\worktrees\609c\RustFSDisk`。
- 范围：只合入 Edge 后端未初始化候选盘识别、summary fallback 和回归测试；不提交、不 push、不部署、不碰真实磁盘。
- 合入内容：
  - Edge 扫描保留 ext4 挂载候选盘，即使缺少 `rustfs-transfer/disk_info.json` 也进入运行账本。
  - 缺少 `disk_info.json` 的候选盘落为 `runtime_status=REJECTED`、`disk_status_code=UNREGISTERED`、`last_error_code=MANIFEST_INVALID`，并明确提示需要 Center 初始化。
  - summary fallback 将无盘内生命周期的 `REJECTED` 运行态显示为 `UNREGISTERED`，协议盘仍优先使用盘内 `disk_info.json.status.code`。
  - 空 rescan 对无 `disk_id` 的未初始化候选盘按 `device_path + mount_path` 结算 `REMOVED`。
  - 新增协议盘与未初始化 ext4 候选盘并存时字段不串用的回归测试。
- 排除内容：未合入来源 worktree 中旧 `center_client.rs`、旧 Center verify/export-key 形态、无关 Center/冻结文档/SQL 改动、`.codex-*`、`.codex-vmware-*`、`.local.env`、`.env.local`、VM 临时文件、`target`、`node_modules`、SSH/密钥/token/连接串或部署产物。
- 验证方式：
  - `cargo fmt --all -- --check`：通过。
  - `cargo test -p rustfs-transfer-edge`：通过，70 个 lib/main/bin 单元测试、1 个 disk_worker 集成测试、2 个 scanner 集成测试和 doc tests 全部通过；仅有 Windows linker stdout warning。
  - `git diff --check`：通过；仅有既有 CRLF 提示。
- 待跟进：
  - 真实页面是否显示未初始化 ext4 候选盘仍需部署到 Edge VM 后由主会话协调非破坏性复测。
  - 真实物理拔盘 WS 验证仍需用户配合拔盘。

## Edge 前端联调修改主工作区合入

- 代理：Codex 多窗口集成工程师。
- 来源窗口：`019ff3bf-75d2-72b0-b85c-a74735697708`，来源 worktree `C:\Users\Beipy\.codex\worktrees\b5af\RustFSDisk`。
- 范围：只合入 `web/edge-web/` 已完成并验证过的前端源码改动和脱敏记录；不合入后端、不部署、不碰真实磁盘、不修改冻结文档。
- 合入内容：
  - 合入 Dashboard 页面布局、盘位展示、全局导出进度、异常模块删改、源服务器/运输盘位 header 删除等用户确认的页面调整。
  - 合入 Edge 前端 API / WebSocket summary 处理、`DISK_*` 事件盘列表替换、周期性 summary 刷新和相关契约测试。
  - 合入 `EdgeTelemetry`、`ProductHeader`、`ParticleAetherField` 组件拆分，以及同步记录页隐藏顶部状态胶囊的最新完成态。
- 排除内容：未合入 `.env.local`、`.local.env`、`node_modules`、`dist`、临时截图/缓存、`.codex-*`、`.codex-vmware-*`、密钥、token、连接串、SSH 配置、VM 临时文件或任何后端未完成修复。
- 验证方式：
  - `npm run typecheck`（`web/edge-web/`）：通过。
  - `npm run build`（`web/edge-web/`）：通过，Vite 构建完成；`dist/` 未进入 git 状态。
  - `git diff --check -- web/edge-web work_sprint/dev_log/2026-08-12.md`：通过；仅有既有 CRLF 提示。
- 待跟进：
  - 本次仅合入前端源码，不做真实 Edge VM 页面复测；如需浏览器验收，需要主会话后续协调运行环境。
## Edge Dashboard JSONC 字段契约补充

- 记录事项：新增 `docs/Edge Dashboard实时快照JSONC字段契约.md`。
- 背景：用户要求用 JSON 注释方式明确 `/api/edge/dashboard/summary` 与 `/ws/edge/progress` 的统一快照字段含义。
- 内容：以 `jsonc` 形式定义 Edge Dashboard 快照结构，保留真实字段形态，并在每个字段旁边写明含义；明确 `disks[].progress`、`disks[].current_object`、`disk_status_code`、`runtime_status` 的边界。
- 约束：未修改 `docs/v1.0冻结/`；仅新增补充文档，无代码改动。
- 验证：文档新增，无代码验证。

## 多窗口已验证改动主线收口合入

- 代理：Codex 多窗口集成/热修合入工程师。
- 范围：按主会话指派从 Edge 后端、Center 后端、Edge 前端、Center 前端和 Edge Dashboard 字段契约文档窗口选择性合入已验证代码/测试/脱敏记录；不部署、不 push、不碰真实磁盘。
- 合入内容：
  - Edge 后端：统一 `/api/edge/dashboard/summary` 与 `/ws/edge/progress` 的 `disks[]` DTO，补齐盘身份、文件系统、生命周期、运行态、容量、准入、错误和单盘 `progress/current_object` 字段；`DISK_REMOVED` 当前快照剔除已拔盘；Dashboard 只读 summary rescan 失败时返回最近运行账本快照。
  - Center 后端：新增 `GET /api/center/sync-records`、`GET /api/center/sync-records/{ledger_id}`、`GET /api/center/edge-sites`，从 `object_ledger LEFT JOIN edge_site` 读取真实账本数据，响应使用 `edge_status`，不返回 mock 数据。
  - Edge 前端：保留 `IMPORTED/SEALED` 等盘内生命周期展示，避免 `runtime_status` 覆盖 `disk_status_code`；WS 事件按当前 `disks[]` 合并/清空，避免拔盘后复活旧盘；补充页面与契约测试。
  - Center 前端：新增同步记录真实接口客户端与同步记录页面，入口支持 `/dashboard` 与 `/sync-records` 切换。
  - 文档：保留 `docs/Edge Dashboard实时快照JSONC字段契约.md` 补充文档及本日志记录。
- 明确排除：`.local.env`、`*.env.local`、`.codex-*`、`.codex-vmware-*`、`target/`、`node_modules/`、`dist/`、`coverage/`、截图/缓存、VM 临时脚本、部署二进制、SSH/私钥、密钥/token/密码/完整连接串，以及来源中的冻结文档、SQL、部署配置和无关旧脏改。
- 冲突/取舍：
  - Edge 后端来源 `server.rs/control.rs` 含旧 `CenterHmacClient` 绑定痕迹，本次仅摘取 DTO/summary fallback/测试结构字段，未带回运行期 Center verify/export-key 路径。
  - Center 前端来源 `vite.config.ts` 含本地代理倾向，未合入；仅合入页面运行所需源码和样式。
  - `work_sprint/TASKS.md` 相关任务当前已为 `[x]`，本次未制造重复状态改动。
- 验证方式：
  - `cargo fmt --all -- --check`：通过。
  - `cargo test -p rustfs-transfer-edge`：通过，72 个 lib/main/bin 单元测试、1 个 disk_worker 集成测试、2 个 scanner 集成测试和 doc tests 全部通过；仅 Windows linker stdout warning。
  - `cargo test -p rustfs-transfer-center`：通过，76 个 lib/main 单元测试、14 个 edge_auth 测试、15 个 import_worker 测试和 doc tests 全部通过；仅 Windows linker stdout warning。
  - `npm run typecheck`（`web/edge-web/`）：通过。
  - `npm run build`（`web/edge-web/`）：通过。
  - `npm ci`（`web/center-web/`）：通过；未审批 esbuild/vue-demi postinstall 脚本，`node_modules/` 不纳入 git。
  - `npm run typecheck`（`web/center-web/`）：通过。
  - `npm run build`（`web/center-web/`）：通过。
  - `git diff --check`：通过；仅既有 CRLF 提示。
  - `rg -n "OfflineDiskVerifier|CenterDiskVerifier|CenterHmacClient|export_key\(|/api/edge/auth|/api/disk/verify|/api/disk/export-key" crates/edge-backend/src crates/edge-backend/tests -S`：无结果。
- 待跟进：
  - Edge 后端拔盘和未初始化 ext4 候选盘展示仍需部署到 Edge VM 后做真实非破坏性插拔复测。
  - Center 同步记录页仍需部署或浏览器连接真实 Center 接口后做联调复核。

## Edge Dashboard 实时快照契约二次对齐主线合入

- 代理：Codex 多窗口集成/热修合入工程师。
- 来源窗口：
  - Edge 后端：`019ff1db-e185-7982-9602-8ee9372f143e`，来源 worktree `C:\Users\Beipy\.codex\worktrees\609c\RustFSDisk`。
  - Edge 前端：`019ff3bf-75d2-72b0-b85c-a74735697708`，来源 worktree `C:\Users\Beipy\.codex\worktrees\b5af\RustFSDisk`。
- 范围：在上一轮主线提交 `a29e412ad9c6991dedefc53994fdfcdf7e5c9111` 基础上，继续合入按 `docs/Edge Dashboard实时快照JSONC字段契约.md` 统一 `/api/edge/dashboard/summary` 与 `/ws/edge/progress` 字段结构的已验证改动；不部署、不 push、不碰真实磁盘。
- 合入内容：
  - Edge 后端 HTTP summary 与 WS progress 补齐统一快照骨架：`object_inventory`、`export_job`、`global`、`global_progress`、`disk_runtime`、`disks[]`。
  - `disks[]` 仅表示当前在位盘；WS 带 `disks[]` 时按完整当前快照发送，`disks: []` 表示清空旧盘。
  - 盘内生命周期继续由 `disk_status_code` 表达，不被 `runtime_status` 覆盖；`runtime_status` 只保留本地运行/准入含义。
  - 单盘字段补齐 `progress.percent`、`export_job_id`、`seal_id`、`current_file`、`current_file_size`、`current_file_done` 和 `current_object`。
  - Edge 前端统一用 `normalizeEdgeDashboardSummary` 消费 HTTP/WS 快照，Dashboard 展示改读 `object_inventory`、`export_job`、`global_progress` 和单盘 `progress/current_object/current_file*`。
- 冲突/取舍：
  - 后端来源仍含旧 `CenterHmacClient` 注入痕迹；本次合入后手工移除 `server.rs/control.rs` 中相关构造和测试夹具字段，保留 Edge 离线导出主流程。
  - 未合入来源中的 `.codex-vmware-*`、冻结文档、SQL、部署配置、Center 改动、VM 构建 target/temp、截图、缓存或本地环境文件。
- 验证方式：
  - `cargo fmt --all -- --check`：通过。
  - `cargo test -p rustfs-transfer-edge`：通过，72 个 lib/main/bin 单元测试、1 个 disk_worker 集成测试、2 个 scanner 集成测试和 doc tests 全部通过；仅 Windows linker stdout warning。
  - `npm run typecheck`（`web/edge-web/`）：通过。
  - `npm run test:unit`（`web/edge-web/`）：通过，25 个用例全部通过。
  - `npm run build`（`web/edge-web/`）：通过。
  - `git diff --check`：通过；仅既有 CRLF 提示。
- 待跟进：
  - 需要部署 Edge VM 后用真实 HTTP/WS/页面确认新契约字段在运行环境中一致，尤其拔盘后 `disks[]` 清空和历史账本不污染当前盘列表。

## Edge Dashboard 旧业务顶层字段硬删除主线合入

- 代理：Codex 多窗口集成/热修合入工程师。
- 来源窗口：Edge 后端 `019ff1db-e185-7982-9602-8ee9372f143e`，来源 worktree `C:\Users\Beipy\.codex\worktrees\609c\RustFSDisk`。
- 范围：按主会话更新口径选择性合入 Edge 后端已部署验证的 Dashboard HTTP/WS 旧顶层业务字段硬删除改动；本轮主工作区不部署、不 push、不碰真实磁盘。
- 合入内容：
  - HTTP `/api/edge/dashboard/summary` 顶层移除旧兼容业务字段 `export_job_id`、`export_job_status`、`scan`、`latest_export_job`，只保留 JSONC 契约主结构。
  - WS `/ws/edge/progress` 顶层移除旧业务状态副本 `export_job_id`、`export_job_status`；`event_type/event_time` 仅作为消息元信息保留。
  - 导出任务状态保留在 `export_job.export_job_status`，单盘任务绑定保留在 `disks[].export_job_id`，盘内生命周期继续由 `disks[].disk_status_code` 表达。
  - 同步更新 `control.rs`、`progress.rs`、`realtime.rs`、`server.rs`、`auto_export.rs` 中的 DTO 构造与测试断言。
- 冲突/取舍：
  - 来源后端仍含旧 `CenterHmacClient` 注入痕迹；主线已移除 Edge 主流程 Center verify/export-key，本次仅摘取旧字段硬删除与测试结构改动，未带回相关运行路径。
  - 未合入来源中的冻结文档、SQL、Center 改动、部署配置、`.codex-*`、`.codex-vmware-*`、本地环境文件、缓存、构建产物、VM 临时文件或任何敏感配置。
- 验证方式：
  - `cargo fmt --all -- --check`：通过。
  - `cargo test -p rustfs-transfer-edge`：通过，72 个 lib/main/bin 单元测试、1 个 disk_worker 集成测试、2 个 scanner 集成测试和 doc tests 全部通过；仅 Windows linker stdout warning。
  - 静态核验：`EdgeControlSummary` 与 `CopyProgressEvent` 结构中不存在旧顶层业务字段；测试断言 HTTP/WS 顶层旧字段不存在。
  - `git diff --check`：通过；仅既有 CRLF 提示。
