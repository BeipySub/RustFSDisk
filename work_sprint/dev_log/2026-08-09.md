# 2026-08-09 开发任务记录

## 今日任务

- [x] 生成项目根目录 `AGENTS.md`，用于约束后续代理和研发协作规范。
- [x] 将开发任务目录统一为 `work_sprint/dev_log/`。
- [x] 添加项目测试目录。

## 开发记录

- 新增 `work_sprint/dev_log/` 目录，用于按日期存储每日开发任务文件。
- 明确 `docs/v1.0冻结/` 为冻结文档目录，禁止后续修改、移动、重命名或删除。
- 在 `AGENTS.md` 中补充冻结文档读取顺序、架构边界、状态字段命名、后端实现、数据库迁移、运输盘和前端约束。
- 将 `AGENTS.md` 中的开发日志路径更新为 `work_sprint/dev_log/`。
- 新增根目录测试说明入口 `tests/README.md`。
- 新增 Rust 后端测试目录：`crates/common/tests/`、`crates/center-backend/tests/`、`crates/edge-backend/tests/`。
- 新增前端测试目录：`web/center-web/src/__tests__/`、`web/edge-web/src/__tests__/`。
- 根据当前项目骨架和 2 天敏捷冲刺目标，新增 `work_sprint/TASKS.md`，拆分 10 个可并行领取的 Sprint 任务卡片。
- 在 `work_sprint/TASKS.md` 中补充 Day 1 / Day 2 排期、并行泳道、不可降级底线、可后置项和主闭环验收目标。
- 在 `AGENTS.md` 中新增“2 天 Sprint 任务协作”约束，明确任务领取、状态更新、并行边界、Day 1 / Day 2 优先级和完成条件。
- 更新 `work_sprint/README.md`，补充 `TASKS.md` 入口和任务状态说明。
- 从系统架构师角度复审任务拆分后，重排 `work_sprint/TASKS.md` 为 19 个更细的并行任务，拆开 Common 协议/加密/文件工具、Center 基础/HMAC/盘 API、Edge 基础/磁盘/扫描/分配、双端 Web，并将测试 fixture 前置到 Day 1。
- 在 `AGENTS.md` 中补充任务总览行和任务卡片状态必须同步更新的规则。
- 在 `work_sprint/TASKS.md` 增加 `P0 解阻塞`、`P1 主闭环`、`P2 交付增强` 优先级和阻塞解除顺序，避免多代理并行时等待上游接口、DDL、fixture 或 adapter。
- 在 `AGENTS.md` 中补充优先级执行规则：优先领取 P0，P0 未完成时下游只能做 mock、测试草案或不阻塞的局部实现。
- 在 `AGENTS.md` 中将任务状态同步要求升级为“状态强同步规则”，要求同一 Commit 同步更新 `TASKS.md` 顶部任务总览和底部任务卡片状态，严禁二者不一致。
- 按架构审查结果修正 Sprint 基线：README 参考文档路径改为 `docs/v1.0冻结/`，AGENTS 增加提交前验证矩阵，Rust workspace 补齐后端依赖基线，双端前端补齐 Vue/Vite/TypeScript 依赖和最小 DashboardView 构建入口。
- 补齐双端 PostgreSQL 首版 DDL，包括中控导入并发唯一约束、对象账本去重唯一约束、边缘多盘分配唯一约束和 AES-GCM nonce 唯一约束。
- 调整 udev 规则为通用 `rustfs-transfer-disk-rescan@.service` rescan 通知，不再只唤起 edge service。
- 新增 `rust-toolchain.toml`，固定 Rust stable 工具链并要求 rustfmt/clippy。

## 验证方式

- 已读取 `AGENTS.md` 内容确认生成成功。
- 已检查 `AGENTS.md` 不再引用旧开发目录路径。
- 已检查 `work_sprint` 下当天日志内容已更新。
- 已检查新增测试目录文件可被 Git 跟踪。
- 已确认 `work_sprint/TASKS.md` 使用 `[ ]`、`[~]`、`[x]` 三种任务状态。
- 已确认未修改 `docs/v1.0冻结/` 下冻结文档。
- 已确认修正后的任务表包含 Common 拆分、Center/Edge 拆分、双端 Web 拆分、Day 1 测试 fixture 和集成风险登记。
- 已确认任务总览包含优先级列，并覆盖 P0/P1/P2 三类任务。
- 已确认 `AGENTS.md` 包含“状态强同步规则”。
- 已生成 `web/center-web/package-lock.json` 和 `web/edge-web/package-lock.json`。
- 已运行 `npm run build` 验证 `web/center-web` 和 `web/edge-web` 构建通过。
- 未能生成 `Cargo.lock` 或运行 `cargo fmt/test`：当前环境找不到可用 `cargo`。
- 未能实际执行 PostgreSQL 迁移：当前环境未确认可用 `psql`/PostgreSQL 测试库。

## 待跟进

- [ ] 后续开发任务开始前持续更新当天开发日志。

## 追加记录：并行 worktree 串行合入

- 已按“并行准备，串行合入，合入后每步跑对应验证”执行主工作区合入。
- 已合入 Common：
  - 协议/API/WS payload、错误码、golden JSON 测试。
  - HMAC/SHA256/AES-256-GCM/nonce/base64/hex 工具。
  - 运输盘协议目录、manifest 校验、原子写、fsync、`.partial` 扫描。
- 已合入 Center：
  - 运输盘注册、初始化、verify、export-key 主体。
  - 导入 worker、manifest 校验、解密、去重账本、chunk 缺块处理。
  - 导入后清理、重新初始化、密钥退役和失败恢复。
- 已合入 Edge：
  - 服务骨架、配置、中控 HMAC client、adapter。
  - 磁盘检测、ext4 校验、恢复入口。
  - RustFS 扫描、对象稳定性判断、导出 planner、多盘分配 SQL 骨架。
  - DiskWorker、加密写盘、manifest、封盘和进度聚合。
- 已合入 Web / Deploy：
  - 双端 DashboardView、HTTP/WS 前端接入模块。
  - systemd、udev、示例配置、本地联调和部署检查脚本。
- 未完整合入项：
  - `TASK-S1-CENTER-002`：中控 `/api/edge/auth` worktree 使用旧 Common 函数名且与当前 `AppState` 命名冲突，未硬塞进主线，保持 `[~]`。
  - `TASK-S1-CENTER-001`：对应 worktree 路径当前不是有效 git worktree，主线已有部分 Center 骨架能力，但任务保持 `[~]`。
  - `TASK-S1-TEST-001`：已通过 crate 内 fixture/manifest/HMAC 测试覆盖主路径，但一键 fixture 生成器未合入，保持 `[~]`。

## 追加验证：串行合入门禁

- Common 合入后通过：
  - `cargo fmt --all -- --check`
  - `cargo test -p rustfs-transfer-common`
- Center 合入后通过：
  - `cargo fmt --all -- --check`
  - `cargo test -p rustfs-transfer-center`
- Edge 合入后通过：
  - `cargo fmt --all -- --check`
  - `cargo test -p rustfs-transfer-edge`
- Web / Deploy 合入后通过：
  - `web/center-web`: `npm ci`, `npm run typecheck`, `npm run build`
  - `web/edge-web`: `npm ci`, `npm run typecheck`, `npm run build`
  - `powershell -ExecutionPolicy Bypass -File scripts/check-deploy.ps1`
- 最终全量验证通过：
  - `cargo fmt --all -- --check`
  - `cargo test --workspace`
  - `web/center-web`: `npm run typecheck`, `npm run build`
  - `web/edge-web`: `npm run typecheck`, `npm run build`
  - `powershell -ExecutionPolicy Bypass -File scripts/check-deploy.ps1`

## 追加记录：剩余闭环缺口修补

- 已读取 `.local.env`，确认中控 VM、边缘 VM、PostgreSQL、RustFS endpoint/端口和测试凭据已在本地环境文件提供；后续真实联调直接使用该文件，不再重复向用户索要。
- 已补齐 `TASK-S1-CENTER-002`：
  - 新增中控 `/api/edge/auth` HMAC 鉴权模块。
  - 校验 `X-Edge-Code`、`X-Auth-Key-Id`、`X-Timestamp`、`X-Nonce`、`X-Body-SHA256`、`X-Signature`。
  - 增加 300 秒时间偏移限制、进程内 nonce TTL 防重放、body sha256 校验、禁用边缘拒绝和 body/header `edge_code` 一致性校验。
  - 鉴权失败返回 `UNAUTHORIZED`，非法请求返回 `INVALID_REQUEST`，日志包含 request_id、edge_code、auth_key_id 和拒绝原因。
- 已补齐 `TASK-S1-CENTER-001` 缺口：
  - 中控 router 增加 `/healthz` 和 `/readyz`。
  - Center store 查询补齐 edge auth 所需 `edge_name`、`auth_key_id`、`auth_secret`。
- 已补齐 `TASK-S1-TEST-001`：
  - 新增 `crates/center-backend/examples/generate_fixture.rs`。
  - 新增 `scripts/generate-fixtures.ps1`，可一键生成 `target/fixtures/initialized-disk`、`target/fixtures/sealed-disk` 和 HMAC 请求样例。
  - SEALED fixture 生成真实 AES-256-GCM 密文、tag、manifest sha256 和 `fixture_import_meta.json`。
- 验证：
  - `cargo fmt --all -- --check`
  - `cargo test -p rustfs-transfer-center`
  - `powershell -ExecutionPolicy Bypass -File scripts/generate-fixtures.ps1`

## 追加记录：SQL 备注补齐

- 已补齐 `sql/center/0001_init_center.sql` 中所有中控端业务表的表备注和字段备注，覆盖 `disk_list`、`edge_site`、`center_config`、`signature_key`、`data_key`、`import_job`、`chunk_import_group`、`chunk_import_part`、`object_ledger`。
- 已补齐 `sql/edge/0001_init_edge.sql` 中所有边缘端业务表的表备注和字段备注，覆盖 `local_object_snapshot`、`export_job`、`export_object`、`disk_runtime`。
- 备注内容按冻结数据库设计描述字段含义、权威边界、安全约束和 API/WS 序列化命名约束，未修改 `docs/v1.0冻结/`。

## 追加验证

- 已通过 `Select-String` 检查两个初始化迁移均包含完整 `COMMENT ON COLUMN` 语句。
- 未实际执行 PostgreSQL 迁移：当前环境未确认可用 `psql`/PostgreSQL 测试库。

## 追加记录：状态枚举备注补强

- 已补强所有 `status` 字段备注，明确枚举值与业务含义：
  - 中控端：`disk_list.status`、`edge_site.status`、`signature_key.status`、`data_key.status`、`import_job.status`、`chunk_import_group.status`。
  - 边缘端：`export_job.status`、`export_object.status`、`disk_runtime.status`。
- 已在备注中继续保留 API/WS 序列化命名约束，例如 `edge_status`、`import_job_status`、`export_job_status`、`object_status`、`runtime_status`。

## 追加记录：冻结文档一致性复核修正

- 复核 `docs/v1.0冻结/数据库设计.md` 后发现并修正迁移偏移：
  - `chunk_import_group.status` 从临时枚举修正为 `WAITING_PARTS`、`READY_TO_MERGE`、`MERGING`、`DONE`、`FAILED`、`CANCELLED`。
  - `chunk_import_group` 补齐 `plaintext_sha256`、`import_bucket`、`import_key`、`created_at`、`updated_at`、`finish_time`、`error_code`。
  - `chunk_import_part` 补齐加密、暂存、状态和校验字段，并增加 `REGISTERED`、`VERIFIED`、`MERGED`、`FAILED` 状态约束。
  - `object_ledger` 修正为冻结文档字段：`import_bucket`、`import_key`、`export_job_id`、`imported_at`，并补齐 `ciphertext_sha256`、`chunk_group_id`、`data_key_id`、`nonce` 及对应唯一约束。
  - `local_object_snapshot` 将 `scan_time` 修正为 `scanned_at`，并补齐 `stable_status`。
  - `export_job.status` 移除非冻结枚举 `PLANNING`，补齐 `disk_id`。
  - `export_object` 移除非冻结字段 `create_time`、`update_time`。
- 已用脚本按表提取冻结数据库文档第一张字段表，并与 SQL `CREATE TABLE` 字段逐表比对，当前无缺失字段、无额外字段。

## 追加记录：Rust 工具链验证推进

- 用户已安装 Rustup/Rust/Cargo，版本为 `rustc 1.97.1`、`cargo 1.97.1`、`rustup 1.29.0`。
- 当前 Codex 会话 PATH 未自动刷新，已临时追加 `%USERPROFILE%\.cargo\bin` 执行 Cargo。
- 已生成 `Cargo.lock`。
- 已运行 `cargo fmt --all` 修复 Rust 文件末尾格式问题，并确认 `cargo fmt --all -- --check` 通过。
- `cargo test --workspace` 已能下载依赖并开始编译，但阻塞于 Windows MSVC linker 缺失：`link.exe not found`。需要安装 Visual Studio Build Tools 的 C++ 构建工具后重跑。

## 追加验证：PostgreSQL 迁移实库验证

- 用户已通过 Navicat Premium 17 创建本地空库：`rustfs_transfer_center_dev`、`rustfs_transfer_edge_dev`。
- 已新增本地忽略文件 `.local.db.env` 保存本机数据库连接串，并在 `.gitignore` 中补充 `.local.*`，避免本地账号密码误提交。
- 已通过本机 Docker `postgres:16` 镜像运行 `psql` 客户端，分别执行：
  - `sql/center/0001_init_center.sql`
  - `sql/center/0002_seed_required_enum.sql`
  - `sql/edge/0001_init_edge.sql`
  - `sql/edge/0002_seed_required_enum.sql`
- 验证结果：
  - 中控库成功创建 9 张业务表，关键唯一索引核验 6 个。
  - 边缘库成功创建 4 张业务表，关键唯一索引核验 4 个。
- 已将 `TASK-S1-DB-001` 在 `work_sprint/TASKS.md` 顶部任务总览和任务卡片中同步标记为 `[x]`。
- 待跟进：Rust 单元测试仍受 Windows MSVC linker `link.exe` 缺失阻塞。

## 追加记录：真实 VM 部署窗口预检

- 已按部署窗口要求读取 `AGENTS.md`、`work_sprint/TASKS.md` 和冻结文档，确认本次只做真实 Center/Edge VM 部署与启动联调准备，不修改冻结文档、不更新 `TASKS.md`。
- 已读取 `.local.env` 的可用配置键名，确认本地已有 VM、PostgreSQL、RustFS 相关连接配置；未在日志或汇报中记录任何密码、access key 或 secret 明文。
- 已检查部署文件：
  - `deploy/systemd/rustfs-transfer-center.service`
  - `deploy/systemd/rustfs-transfer-edge.service`
  - `deploy/systemd/rustfs-transfer-disk-rescan@.service`
  - `deploy/udev/99-rustfs-transfer-disk.rules`
- udev 规则符合底线：只通过 systemd 触发磁盘 rescan 通知，不直接执行导出、导入、清理或重新初始化业务。
- 已运行 `cargo build --workspace --bins`，构建通过。
- 阻塞发现：当前 `crates/center-backend/src/main.rs` 和 `crates/edge-backend/src/main.rs` 仅打印服务名并退出，没有启动 axum 服务、监听端口或挂载 `/healthz`、`/readyz`。
- 已本地执行构建产物确认：
  - `target/debug/rustfs-transfer-center.exe` 输出服务名后退出。
  - `target/debug/rustfs-transfer-edge.exe` 输出服务名后退出。
- 结论：按“如发现部署脚本缺陷或必须修改代码则停止并汇报主窗口”的约束，本窗口未向真实 VM 安装该产物，避免 systemd 进入反复重启或误报可用状态。
- 下一步：需主窗口另派编码窗口补齐两个后端二进制入口，使其加载配置、初始化 AppState、启动 axum server，并暴露 `/healthz`、`/readyz` 后，再恢复真实 VM 部署。

## 追加记录：真实 VM 部署恢复尝试

- 已确认主线中 `crates/center-backend/src/main.rs` 和 `crates/edge-backend/src/main.rs` 已改为常驻 axum server 入口，会加载配置、初始化状态并监听服务端口。
- 已再次读取 `.local.env` 的键名和段落结构，部署过程中未输出任何数据库密码、RustFS access key、secret 或 RPC secret 明文。
- 已成功通过 SSH 探测到：
  - Center：`control@192.168.128.131`，hostname `control-VMware-Virtual-Platform`，sudo 免密可用。
  - Edge：`edge@192.168.128.130`，hostname `edge-VMware-Virtual-Platform`，sudo 免密可用。
- 已检查两台 VM 初始均无 Rust 工具链，且 `/` 空间充足。
- 已在 Center VM 触发 apt 安装 `cargo`、`rustc`、`pkg-config`、`libssl-dev`，安装完成后 Center 上 apt 版 `cargo 1.75.0` 可用。
- 已按 F: 盘空间限制生成本机临时脱敏源码包，明确排除 `.git`、`target`、`node_modules`、`.local.env`、`.local.*`、`*.pem`、`*.key`。
- Center 远端构建首次失败：Ubuntu apt 版 Cargo 1.75 无法解析当前 `Cargo.lock` v4，需要 rustup/current stable Cargo。
- 随后准备在 Center 安装 rustup stable 继续构建时，Center 和 Edge 均变为 SSH/ping 不可达。
- 宿主侧 `vmrun list` 显示 `Total running VMs: 0`，与两台 VM 网络不可达一致；未擅自启动未知 VM。
- 本轮未完成项：
  - 未安装 `/opt/rustfs-transfer` 二进制。
  - 未安装或更新 `/etc/rustfs-transfer` 配置。
  - 未安装 systemd unit、udev rule 或 rescan helper。
  - 未能执行 `systemctl is-active/status`、`/healthz`、`/readyz` 和日志检查。
- 风险：`deploy/systemd/rustfs-transfer-disk-rescan@.service` 指向 `/opt/rustfs-transfer/bin/rustfs-transfer-rescan`，当前仓库未发现对应可执行产物；真实部署时需要提供该 helper，或确认后端启动主动扫描足以覆盖演示路径并把 udev rescan 缺口登记为风险。

## 追加记录：真实 VM 服务部署完成

- 用户重启两台 VM 后已复测：
  - Center `192.168.128.131` ping/SSH 可达，hostname `control-VMware-Virtual-Platform`，sudo 免密可用。
  - Edge `192.168.128.130` ping/SSH 可达，hostname `edge-VMware-Virtual-Platform`，sudo 免密可用。
- 构建与产物：
  - 已在两台 VM 安装 rustup stable `rustc 1.97.1` / `cargo 1.97.1`。
  - Center VM 安装 Linux build 依赖后，从脱敏源码包构建 `cargo build --workspace --bins --release` 成功。
  - 构建产物：`rustfs-transfer-center`、`rustfs-transfer-edge`。
- 已安装组件：
  - Center `/opt/rustfs-transfer/rustfs-transfer-center`
  - Edge `/opt/rustfs-transfer/rustfs-transfer-edge`
  - `/etc/rustfs-transfer/center.toml`、`center.env`
  - `/etc/rustfs-transfer/edge.toml`、`edge.env`
  - `/etc/systemd/system/rustfs-transfer-center.service`
  - `/etc/systemd/system/rustfs-transfer-edge.service`
  - `/etc/systemd/system/rustfs-transfer-disk-rescan@.service`
  - `/etc/udev/rules.d/99-rustfs-transfer-disk.rules`
  - `/opt/rustfs-transfer/bin/rustfs-transfer-rescan`
- 安全处理：
  - 配置文件权限为 `root:rustfs-transfer 0640`，未输出数据库密码、RustFS access key、secret 或 HMAC secret 明文。
  - udev helper 当前只记录 rescan 请求到系统日志，不直接执行导出、导入、清理或重新初始化业务。
  - Center DB 原无 `edge-demo` 注册；已新增 `edge_site(edge_code='edge-demo', edge_name='Edge Demo', status='ACTIVE')` 和部署期 HMAC 凭据，未重置已有站点密钥。
  - Center DB 原 `center_config` 为空，导致 `/readyz` 503；已新增一条非敏感中心身份配置后恢复 ready。
- 验证结果：
  - `systemctl is-active rustfs-transfer-center.service` => `active`
  - `systemctl is-active rustfs-transfer-edge.service` => `active`
  - Center `/healthz` => 200，`{"ok":true,"service":"rustfs-transfer-center"}`
  - Center `/readyz` => 200，`{"ok":true,"service":"rustfs-transfer-center"}`
  - Edge `/healthz` => 200，包含 `edge_code=edge-demo`
  - Edge `/readyz` => 200，`database_ok=true`、`rustfs_ok=true`，mount roots 为 `/media/edge`、`/mnt/rustfs-transfer`
  - Edge -> Center `/api/edge/auth` HMAC smoke => 200，`allowed=true`、`edge_status=ACTIVE`
  - Center RustFS `/minio/health/ready` => 200 ready
  - Edge RustFS `/minio/health/ready` => 200 ready
  - 双端服务重启后 30 秒日志窗口未发现 `panic`、`fatal`、`error`、`failed`。
- 磁盘与测试文件：
  - Edge 当前可见 `/dev/sdb1` ext4，label `FUSTFS-A-DRILL`，mount `/media/edge/FUSTFS-A-DRILL1`。
  - Edge 另可见 `/dev/sdc1` ext4，label `FUSTFS-TST-A`，mount `/media/edge/FUSTFS-TST-A`。
  - 已只读检查 `/home/edge/下载`，目录存在，但未发现普通测试文件；未上传、删除或清空任何源对象或真实运输盘数据。
- 剩余风险/待跟进：
  - `rustfs-transfer-rescan` 当前为部署期最小 helper，只记录 udev rescan 请求；后续如需热插拔即时触发后端扫描，应由编码窗口补齐真实 daemon 通知机制。
  - Edge apt 源存在 `bzip2/libbz2` 版本不一致，导致 `build-essential` 无法在 Edge 安装；本次通过 Center 构建 Edge 二进制绕过，不影响当前服务运行。

## 追加记录：串行合入 P0 启动入口与硬盘身份展示

- 已按串行合入窗口要求读取 `AGENTS.md`、`work_sprint/TASKS.md` 和冻结文档，确认本次涉及中控端、边缘端、前端、部署验证和运输盘身份展示；未修改 `docs/v1.0冻结/`。
- 已检查主工作区存在真实 VM 部署预检窗口写入的 dev log 记录，并保留该记录不回滚。
- 已检查两个待合入 worktree：
  - `C:\Users\Beipy\.codex\worktrees\3882\RustFSDisk`：P0 双端后端启动入口成果为未提交工作区差异。
  - `C:\Users\Beipy\.codex\worktrees\dc10\RustFSDisk`：硬盘身份展示闭环成果为未提交工作区差异。
- P0 启动入口合入：
  - `crates/center-backend/src/main.rs` 从打印后退出改为加载配置、初始化 PostgreSQL pool、构造 `CenterService` / `AppState` 并启动 axum server。
  - `crates/edge-backend/src/main.rs` 从打印后退出改为加载配置、初始化 `AppState` 并启动 axum server。
  - 新增 `crates/center-backend/src/config.rs`，支持 TOML 配置和 `RUSTFS_TRANSFER__` 环境变量覆盖。
  - `crates/center-backend/src/lib.rs` 导出 `CenterConfig`。
- 硬盘身份展示合入：
  - `web/edge-web/src/api/edgeDashboard.ts` 补齐硬件序列、`ID_SERIAL`、稳定硬件 ID、设备路径、文件系统、FS UUID、PARTUUID、厂商、型号和传输类型字段。
  - `web/edge-web/src/views/DashboardView.vue` 展示上述硬盘身份字段。
  - `web/center-web/src/views/DashboardView.vue` 在中控端运输盘明细中展示同样的硬盘身份字段。
- 冲突情况：
  - 未产生 Git 冲突标记；主线已包含大部分成果，本次只做缺口补齐。
  - Cargo 在主工作区 `F:\Project\RustFSDisk\target` 链接 Center 测试时受 F: 盘剩余空间不足影响失败；后续验证改用 Codex 可写临时 target 目录 `C:\Users\Beipy\.codex\visualizations\2026\08\09\019fe57e-f756-7b91-bbdb-92c59b9743e4\cargo-target`。

## 追加验证：串行合入最终门禁

- P0 启动入口后验证：
  - `cargo fmt --all -- --check`：通过。
  - `cargo test -p rustfs-transfer-edge`：通过。
  - `cargo test -p rustfs-transfer-center --target-dir C:\Users\Beipy\.codex\visualizations\2026\08\09\019fe57e-f756-7b91-bbdb-92c59b9743e4\cargo-target`：通过。
  - `cargo build --workspace --bins --target-dir C:\Users\Beipy\.codex\visualizations\2026\08\09\019fe57e-f756-7b91-bbdb-92c59b9743e4\cargo-target`：通过。
- 硬盘身份展示后验证：
  - `cargo fmt --all -- --check`：通过。
  - `cargo test -p rustfs-transfer-common --target-dir C:\Users\Beipy\.codex\visualizations\2026\08\09\019fe57e-f756-7b91-bbdb-92c59b9743e4\cargo-target`：通过。
  - `cargo test -p rustfs-transfer-edge --target-dir C:\Users\Beipy\.codex\visualizations\2026\08\09\019fe57e-f756-7b91-bbdb-92c59b9743e4\cargo-target`：通过。
  - `web/center-web`: `npm run typecheck`、`npm run build`：通过。
  - `web/edge-web`: `npm run typecheck`、`npm run build`：通过。
- 最终全量验证：
  - `cargo test --workspace --target-dir C:\Users\Beipy\.codex\visualizations\2026\08\09\019fe57e-f756-7b91-bbdb-92c59b9743e4\cargo-target`：通过。
  - `powershell -ExecutionPolicy Bypass -File scripts\check-deploy.ps1`：通过，输出 `deploy static checks passed`。
  - `git diff --name-only -- docs\v1.0冻结`：无输出，冻结文档未被修改。
- 结论：此前真实 VM 部署预检发现的“二进制只打印后退出”阻塞已解除；当前可恢复 VM 部署窗口继续安装、启动和 `/healthz` smoke。
