# 2026-08-11 开发日志

### TASK-S1-SECURITY-HOTFIX-006：AES-GCM AAD 同源构造与全加密面审计

- 范围：仅代码、测试、TASKS 与本日志记录；未部署、未调用真实 API/VM，未操作硬盘、数据库或生产 SQL。
- 冻结协议依据：用户已更新 `docs/v1.0冻结/开发协议契约.md`，明确 `center` 不属于 `center_signature` 覆盖字段；本轮只读取冻结文档，不修改、不暂存冻结目录。
- 全加密面审计：
  - `center_signature` canonical 只覆盖 `protocol`、`disk`、`security.center_key_id`、`security.signature_alg`、`security.data_key_id`；初始化、导入、重初始化验签均走 `CenterSecurity` 的同一真实 HMAC 实现。
  - Center `disk_data_key` 包裹/解包继续使用本机主密钥与 `disk_id + data_key_id` AAD，不包含整个 `center` 对象。
  - Edge HTTP HMAC 继续使用 method、canonical path/query、timestamp、nonce、body sha256；`edge_auth_secret` 未复用为对象加密密钥。
  - Edge 对象写盘与 Center 导入解密使用 AES-256-GCM；修补后 Common AAD 绑定 `disk_id`、`seal_id`、`export_job_id`、`bucket`、`key`、`chunk_group_id`、`chunk_index`、`chunk_total`、`chunk_offset_bytes`。
  - Web、deploy、scripts 检索未发现明文 `disk_data_key`、控制 token、签名密钥或真实密钥落入浏览器响应/示例配置；示例仍使用占位值。
- 修补内容：
  - `crates/common/src/crypto/mod.rs`：补齐 `ObjectAad` 字段并统一 AAD 字符串构造。
  - `crates/edge-backend/src/disk_worker.rs`：Edge manifest AAD 改为复用 Common 构造。
  - `crates/center-backend/src/import_worker.rs`：Center 导入前按 manifest 业务字段复算 AAD，不匹配时以 `MANIFEST_INVALID` 拒绝，不进入 AES-GCM 解密。
  - 测试和 fixture 同步：Common crypto/golden、Edge DiskWorker manifest AAD 断言、Center ImportWorker AAD 篡改拒绝。
- 验证结果：
  - `cargo fmt --all -- --check`：通过。
  - `cargo test -p rustfs-transfer-common`：普通沙箱因 `target/debug/.cargo-build-lock` 权限拒绝未启动；最小权限重跑通过。
  - `cargo test -p rustfs-transfer-edge`：普通沙箱因同一构建锁权限拒绝未启动；最小权限重跑通过，包含 Edge DiskWorker manifest AAD 断言。
  - `cargo test -p rustfs-transfer-center`：最小权限重跑通过，包含 Center ImportWorker AAD 篡改拒绝。
  - `cargo test --workspace`：最小权限重跑通过。
  - `powershell -ExecutionPolicy Bypass -File scripts\check-deploy.ps1`：通过。
  - `git diff --check`：通过；冲突标记检查无命中；敏感抽查仅命中检查脚本、环境变量名、`CHANGE_ME` 示例和代码变量名，未发现真实密钥。
  - 工作区仍有用户本人更新的 `docs/v1.0冻结/开发协议契约.md` diff，以及 2026-08-10 VM 日志和原型图片等并发改动；本提交不暂存、不修改、不覆盖这些内容。

### 原型图 1920x1080 等比适配

- 范围：仅处理 `docs/04-原型设计/` 下两张 Codex 原型截图，生成独立 1920x1080 成稿；不修改源图，不触碰 `docs/v1.0冻结/`。
- 输出：
  - `codex-clipboard-041d3a8b-2692-40fb-a36c-34acbb591a3f-1920x1080.png`
  - `codex-clipboard-99c9e886-e501-40b6-9173-fa0b7f2547c8-1920x1080.png`
- 处理方式：主体图按高度等比 contain 到 1080，高度完整保留；左右用同源深色模糊延展补足 1920 宽，避免字体、图标横向拉伸或主体裁切。
- 验证结果：使用图片尺寸读取确认两张输出均为 `1920x1080`；人工查看确认顶部、底部、左右核心 UI 区域完整可见，无明显拉伸或裁切。
### VM b358efec backend deploy and Center zero-disk initialize

- Scope: real Center and Edge backend deployment plus Center-only transport disk registration/initialization. Did not deploy frontends, format disks, run broad SQL, call Edge scan/export, import, touch source objects, archive objects, unrelated disks, or FUSTFS-A-DRILL.
- Source: deployed current main commit `b358efec577eb6b3ee38f1c61bff83c648304281` (`修补 AES-GCM AAD 同源校验`), which includes the prior center_signature scope convergence.
- Build/deploy: built Center release on Center VM using `/home/control/.cargo/bin/rustup run stable` (`rustc 1.97.1`, `cargo 1.97.1`) and installed `/opt/rustfs-transfer/rustfs-transfer-center`, SHA256 `1382f601922f9a1fa4ca46068dfdcb0c5072db5f9cfe3e380fb8adad811dec6c`. Edge VM lacked `cc`, so Edge binaries were built on the same-arch Center VM (`x86_64`) and deployed to Edge: `rustfs-transfer-edge` SHA256 `a41c85072bc07055f8715b42d35f5edf81bca2642459177918b2e9e8147d621a`, `rustfs-transfer-rescan` SHA256 `f80d5a68fd8b76a03f8dde973e1dc6e5338b308587b2e78ffd818d91b573bf5c`.
- Service validation: Center and Edge systemd services were active after restart. Center `/healthz` and `/readyz` returned 200; Edge `/healthz` and `/readyz` returned 200. Log scan found no business panic/error markers; only existing system journal truncation warnings appeared.
- Center pre-write disk gate before registration and again before initialization: target `/dev/sdc1`, parent `/dev/sdc`, SN `323533383650343031383331`, FS UUID `2cf552ec-62b8-4e75-84df-eb8da998d2ec`, label `RFS-ZERO-FRESH`, ext4, size `1000202788864`, mount `/media/control/RFS-ZERO-FRESH`; non-system/non-optical checks passed; protocol directory was absent; `.partial=0`; service user had root write permission.
- Center DB baseline before registration: `disk_list=0`, `data_key=0`, `import_job=0`, `object_ledger=0`, `chunk_import_group=0`, `chunk_import_part=0`, `edge_site=1`, same-SN disk registration `0`.
- Controlled API result: registered new disk_id `79bafb4e-4aba-4492-b032-3abf73dc03d2`; initialized it with new data_key_id `48c2bba2-0908-4605-9c0f-f604b334acfa`; API status_code was `INITIALIZED`.
- Post-initialize validation: disk_info fields match disk_id/SN/capacity/protocol `1.0`; status `INITIALIZED`, sealed=false, imported=false, reusable=true; center_key_id matched current config-derived key id; center_signature verification passed with signature length 32 and covered paths `disk`, `protocol`, `security.center_key_id`, `security.data_key_id`, `security.signature_alg`. No key, signature value, or plaintext secret was logged.
- DB postcheck: `disk_list=1`, `data_key=1`, `import_job=0`, `object_ledger=0`, `chunk_import_group=0`, `chunk_import_part=0`, `edge_site=1`. New data_key is `ACTIVE`, bound to the new disk, and not bound to edge/export yet. Protocol data/meta/manifests payload entry count is 0 and `.partial=0`.
- Stop point: initialized disk is ready for the user to physically move to Edge. No Edge scan/export/import was started.

### Edge zero-disk readonly gate stopped before scan/export

- Scope: real Edge VM readonly gate for the Center-initialized transport disk. Did not trigger Edge scan, create/start export jobs, modify source objects, format/clean/reinitialize the disk, run broad SQL, touch archive/Center import, or write disk protocol payloads.
- Edge service health: `rustfs-transfer-edge` was active; `/healthz` and `/readyz` returned 200.
- Physical disk gate passed on actual Edge device: `/dev/sdb1`, parent `/dev/sdb`, SN `323533383650343031383331`, FS UUID `2cf552ec-62b8-4e75-84df-eb8da998d2ec`, label `RFS-ZERO-FRESH`, ext4, partition size `1000202788864`, mount `/media/edge/RFS-ZERO-FRESH`; non-system/non-optical checks passed.
- Disk protocol gate passed: disk_info disk_id `79bafb4e-4aba-4492-b032-3abf73dc03d2`, data_key_id `48c2bba2-0908-4605-9c0f-f604b334acfa`, status `INITIALIZED`, signature length 32, `.partial=0`, protocol payload entry count 0, canonical SHA256 `59492802fcae2e962f3a954637c1a671ac8350ece7348e47d91997d5a89283ef`.
- Center-side true HMAC verification of the Edge disk_info passed using the deployed Center signing configuration: center_signature_valid=true, center_key_id_match=true, covered paths `disk`, `protocol`, `security.center_key_id`, `security.data_key_id`, `security.signature_alg`. No key, token, signature value, or full JSON was logged.
- Stop reason: Edge control summary / local DB still had stale runtime state for the same mount and SN: disk_runtime row id 12 had old disk_id `eeca55c4-ee87-4bcb-b797-7ef83899e172`, device `/dev/sdb1`, mount `/media/edge/RFS-ZERO-FRESH`, status `DONE`, last_seen_at `2026-08-10T11:51:33.711602`. Current disk_info disk_id is `79bafb4e-4aba-4492-b032-3abf73dc03d2`. Old Edge tables also still contained one `SEALED` export_job and 57 `EXPORTED` export_object rows bound to the old disk_id.
- Safety decision: stopped before `/api/edge/scan` and before any export API because runtime identity was not READY and did not match the current disk_info. Current run caused no Edge scan/export writes; only temporary diagnostic scripts and readonly disk_info copies were created under `/tmp`.

### Edge 7d658b6 deploy attempt stopped by build-scope/toolchain blocker

- Scope requested: deploy only Edge backend commit `7d658b69249ae97a14974416d35f31a23a265d0c` and then perform readonly health/rescan-summary/current disk checks. No frontend, scan, export, source object, Center, archive, object_ledger, or disk payload writes were authorized.
- Local source state: `HEAD` was `7d658b69249ae97a14974416d35f31a23a265d0c` (`修补 Edge 运行态可重建准入策略`).
- Blocker: Edge VM is `x86_64` with Rust stable `rustc 1.97.1` / `cargo 1.97.1`, but has no `cc`, `gcc`, `clang`, `musl-gcc`, or equivalent C compiler available. Only `/usr/bin/ld` exists. Because the current authorization was Edge-only and prohibited extra system changes, no package installation or Center-side build was performed. Therefore the Edge binary was not replaced with the `7d658b6` build.
- Current deployed Edge binary remained the previous `b358efec` build: `rustfs-transfer-edge` SHA256 `a41c85072bc07055f8715b42d35f5edf81bca2642459177918b2e9e8147d621a`, `rustfs-transfer-rescan` SHA256 `f80d5a68fd8b76a03f8dde973e1dc6e5338b308587b2e78ffd818d91b573bf5c`.
- Readonly Edge health/current disk check: service active; `/healthz=200`; `/readyz=200`. Current disk `/dev/sdb1` under `/dev/sdb`, SN `323533383650343031383331`, FS UUID `2cf552ec-62b8-4e75-84df-eb8da998d2ec`, label `RFS-ZERO-FRESH`, ext4, size `1000202788864`, mount `/media/edge/RFS-ZERO-FRESH`; disk_info disk_id `79bafb4e-4aba-4492-b032-3abf73dc03d2`, data_key_id `48c2bba2-0908-4605-9c0f-f604b334acfa`, status `INITIALIZED`, signature length 32, `.partial=0`, protocol payload entries 0.
- Readonly Edge summary/DB check: current summary reported one disk with disk_id `79bafb4e-4aba-4492-b032-3abf73dc03d2`, runtime_status `READY`, disk_status_code `INITIALIZED`, last_error null. Edge DB counts: `disk_runtime=1`, `local_object_snapshot=0`, `export_job=0`, `export_object=0`.
- No rescan/scan/export was triggered in this attempt; no DB/disk/source/Center/archive/object_ledger writes were performed in this attempt.

### Edge 7d658b6 real VM build/deploy

- Scope: Edge VM backend only. Built `rustfs-transfer-edge` from clean local `HEAD` archive at commit `7d658b69249a` in `/tmp` on Edge, deployed to `/opt/rustfs-transfer/rustfs-transfer-edge`, and restarted only `rustfs-transfer-edge`. No rescan, scan, export, import, cleanup, Center operation, source-object write, archive/object_ledger write, or transport-disk payload write was performed.
- Build result: `cargo build --release -p rustfs-transfer-edge --locked` completed successfully on Edge using Rust stable `1.97.1`.
- Binary deployment: old Edge binary SHA256 `a41c85072bc07055f8715b42d35f5edf81bca2642459177918b2e9e8147d621a` was backed up to `/opt/rustfs-transfer/rustfs-transfer-edge.backup-7d658b6-20260811T010242Z`; new binary SHA256 `ab9a38a9e2a61e122a7f73fe4e68c01b07e18e8df1693a4acb8acc1c238f480f` installed with owner `root:rustfs-transfer` and mode `0750`.
- Service validation: `systemctl is-active rustfs-transfer-edge` returned `active`; `/healthz=200`; `/readyz=200`.
- Readonly disk gate after deploy: current mount `/media/edge/RFS-ZERO-FRESH` is `/dev/sdb1`, ext4, FS UUID `2cf552ec-62b8-4e75-84df-eb8da998d2ec`, label `RFS-ZERO-FRESH`, size `915.8G`; parent `/dev/sdb` SN `323533383650343031383331`, size `931.5G`. Disk protocol status is `INITIALIZED`, disk_id `79bafb4e-4aba-4492-b032-3abf73dc03d2`, `.partial=0`.
- Log note: recent post-restart journal grep found no new business panic/error/failed markers; system journal still reports an unrelated truncated old journal file warning.

### Edge controlled export and seal

- Scope: Edge VM controlled export for the current transport disk only. Used existing Edge control API paths for scan, export job create, and start. Did not call Center import/cleanup, did not format/reinitialize, did not delete or modify source objects, and did not touch archive/object_ledger or unrelated disks.
- Pre-write gate: service active, `/healthz=200`, `/readyz=200`; mount `/dev/sdb1` at `/media/edge/RFS-ZERO-FRESH`, ext4, FS UUID `2cf552ec-62b8-4e75-84df-eb8da998d2ec`, label `RFS-ZERO-FRESH`; parent SN `323533383650343031383331`; disk_id `79bafb4e-4aba-4492-b032-3abf73dc03d2`; disk status `INITIALIZED`; `.partial=0`.
- Scan result: `scan_status=DONE`, `object_seen=57`, `stable_object_count=57`, `source_changed_count=0`, `total_bytes=951301462`.
- Export job: `export_job_id=2f88cd24-4176-43da-9dc8-2de0042ad5d3`; create response reported `object_count=114`, `total_bytes=1902602924`; start assigned and copied `57` objects / `951301462` bytes with `worker_started=1`, `worker_failed=0`.
- Final Edge API/DB readonly check: job status `SEALED`; `export_object` rows for this job are `EXPORTED=57`, all bound to disk_id `79bafb4e-4aba-4492-b032-3abf73dc03d2`, bytes `951301462`; `local_object_snapshot=114`; total jobs by status: `SEALED=1`. The job aggregate count/bytes retained the doubled snapshot count, while actual exported rows and copied bytes are 57 / 951301462.
- Disk validation: disk_info status `SEALED`, sealed=true, imported=false, reusable=false; `.partial=0`; protocol counts after seal: `manifest_files=2`, `data_files=57`, `meta_files=57`.
- Service/log validation: final `/healthz=200`, `/readyz=200`; no new business panic/error/failed markers were found in the export window, only the pre-existing truncated journal-file warning.

### Center controlled import of sealed disk

- Scope: Center VM controlled import for the current sealed transport disk only. Used existing `POST /api/center/import-jobs/start` with the real mount path. Did not run cleanup/reinitialize, did not touch Edge, unrelated disks, or perform manual SQL/disk edits.
- Pre-import gate: Center service active, `/healthz=200`, `/readyz=200`; target `/dev/sdc1` mounted at `/media/control/RFS-ZERO-FRESH`, ext4, FS UUID `2cf552ec-62b8-4e75-84df-eb8da998d2ec`, label `RFS-ZERO-FRESH`; parent `/dev/sdc` SN `323533383650343031383331`; disk_id `79bafb4e-4aba-4492-b032-3abf73dc03d2`; disk status `SEALED`; `.partial=0`; payload counts before import `manifest_files=2`, `data_files=57`, `meta_files=57`.
- Signature gate: center_signature verification passed with `key_id_match=true`, signature length `32`, canonical SHA256 `59492802fcae2e962f3a954637c1a671ac8350ece7348e47d91997d5a89283ef`, covered paths `disk`, `protocol`, `security.center_key_id`, `security.data_key_id`, `security.signature_alg`.
- Import result: `import_job_id=223ae407-25c0-4776-a3a1-5158a40142d1`, status `DONE`, outcome `IMPORTED`, objects `57/57`, bytes `951301462/951301462`.
- DB readonly validation: import_job row is `DONE`, bound to disk_id `79bafb4e-4aba-4492-b032-3abf73dc03d2`, seal_id `03aeab52-15e7-4b55-b725-74ebfb1681aa`, export_job_id `2f88cd24-4176-43da-9dc8-2de0042ad5d3`; `object_ledger` rows for this import job are `57`, bytes `951301462`, one archive bucket and 57 archive keys. data_key `48c2bba2-0908-4605-9c0f-f604b334acfa` is `SEALED_READONLY` and bound to the same export_job_id/seal_id.
- Post-import disk validation: disk_info status `IMPORTED`, sealed=true, imported=true, reusable=true, import_job_id `223ae407-25c0-4776-a3a1-5158a40142d1`; `.partial=0`; protocol payload remains `manifest_files=2`, `data_files=57`, `meta_files=57`; post-import center_signature verification still passed.
- Service/log validation: final `/healthz=200`, `/readyz=200`; no new business panic/error/failed markers were found in the import window, only the pre-existing truncated journal-file warning.

### Center controlled reinitialize stopped by signature preflight

- Scope: attempted the existing controlled `POST /api/center/disks/{disk_id}/reinitialize` path for the current imported disk only. There is no separate cleanup HTTP route in the deployed Center service; this API performs cleanup and reinitialize in one controlled state machine. No manual SQL, manual disk edit, cleanup workaround, or retry was performed.
- Pre-write gate passed: Center service active, `/healthz=200`, `/readyz=200`; target `/dev/sdc1` mounted at `/media/control/RFS-ZERO-FRESH`, ext4, FS UUID `2cf552ec-62b8-4e75-84df-eb8da998d2ec`, label `RFS-ZERO-FRESH`; parent `/dev/sdc` SN `323533383650343031383331`; disk_id `79bafb4e-4aba-4492-b032-3abf73dc03d2`; seal_id `03aeab52-15e7-4b55-b725-74ebfb1681aa`; disk status `IMPORTED`; `.partial=0`.
- API result: stopped with HTTP `400`, `error_code=INVALID_REQUEST`, message `disk_info signature invalid: disk_info center_signature verification failed`. Per runbook, no destructive retry or bypass was attempted.
- Zero-write evidence after failure: disk_info still `IMPORTED`; `.partial=0`; payload still `data_files=57`, `meta_files=57`, `manifest_files=2`; disk_info SHA256 `82e3d42889040ca5ee9ae1a7217a62d011bb1b41b72cb8a99973380e11847b3b`.
- DB readonly evidence after failure: data_key rows for this disk remain `SEALED_READONLY=1`; old data_key `48c2bba2-0908-4605-9c0f-f604b334acfa` remains bound to export_job `2f88cd24-4176-43da-9dc8-2de0042ad5d3` and seal_id `03aeab52-15e7-4b55-b725-74ebfb1681aa`; import_job `223ae407-25c0-4776-a3a1-5158a40142d1` remains `DONE` with `57/57` objects and `951301462/951301462` bytes; object_ledger for the job remains `57` rows / `951301462` bytes.
- Service validation after failure: `/healthz=200`, `/readyz=200`; only the pre-existing truncated journal-file warning was observed in the readonly log check.

### TASK-S1-EDGE-HOTFIX-003：Edge disk_runtime 可重建准入修补

- 范围：仅代码、测试、TASKS 与本日志记录；未部署、未调用真实 API/VM，未操作硬盘、数据库或生产 SQL，未修改冻结文档。
- 架构口径：用户明确 `disk_runtime` 仅代表“当前插入、当前可运行”的临时运行态；新盘发现、scan、创建/启动导出任务不得以旧 `disk_runtime.disk_id` 与盘内 `disk_info.disk_id` 匹配作为准入条件。
- 修补内容：
  - `crates/edge-backend/src/server.rs`、`crates/edge-backend/src/rescan.rs`：受控 scan、创建导出任务、启动导出任务前同步执行一次当前运输盘发现；发现失败则在写任务前拒绝。
  - `crates/edge-backend/src/disk_detection.rs`：当前盘发现成功时，先按当前 `device_path`/`mount_path` 和当前盘 `disk_id` 替换同盘旧 runtime 快照，再写入本次发现结果；旧缓存不参与盘身份选择或拒绝。
  - `crates/edge-backend/src/disk_worker.rs`、`crates/edge-backend/src/export_runtime.rs`：导出成功、manifest 与封盘写入完成后，在同一受控完成流程中删除该盘对应 `disk_runtime`；不删除 `export_job`、`export_object`、manifest 或 seal 历史。
  - `crates/edge-backend/src/control.rs`：控制查询仍只读取当前 runtime 表内 READY 行，不使用物理槽位排序作为盘身份验收门禁。
- 安全边界：
  - 盘内 `disk_info.disk_id` 仍必须经 Center HMAC `/api/disk/verify` 与登记状态校验，且盘内状态必须是 `INITIALIZED`。
  - 非 ext4、缺硬件 SN、`.partial` 残留、`EDGE_COPYING`、Center 签名/登记拒绝仍不得进入 READY。
  - 历史 `export_job`、`export_object`、manifest、seal 和真实导出审计记录不作为新盘准入前置，也不会被本补丁清理。
- 验证结果：
  - `cargo fmt --all -- --check`：通过。
  - `cargo test -p rustfs-transfer-edge`：普通沙箱因 `target/debug/.cargo-build-lock` 权限拒绝未启动；最小权限重跑通过，覆盖旧 runtime 不阻断新盘、封盘成功后 runtime 删除且历史保留、Center 签名拒绝/非 ext4/`.partial` 阻断。
  - `powershell -ExecutionPolicy Bypass -File scripts\check-deploy.ps1`：通过。
  - 真实 Edge VM 尚未部署本补丁；需部署窗口更新 Edge 后先执行只读 rescan/summary 核验，再触发 scan/export。

### TASK-S1-SECURITY-HOTFIX-007：center_signature 同源签名/验签 API 与 reinitialize 分叉修复

- 范围：仅代码、测试、TASKS 与本日志记录；未部署 Center/Edge VM，未调用真实 API，未操作运输盘、数据库、RustFS 源对象、archive 或 object_ledger，未修改冻结文档。
- 真实阻塞背景：本轮真实闭环已完成 Edge 导出封盘与 Center 导入，盘内 `IMPORTED` 后受控 reinitialize 在写入前因 `disk_info center_signature verification failed` 拒绝；导入路径验签通过而 reinitialize 前置验签失败，定位为签名输入口径分叉。
- 修补内容：
  - `crates/common/src/crypto/mod.rs`：新增唯一 `center_signature` payload/canonicalize/sign/verify API。覆盖字段严格为 `protocol`、`disk`、`security.center_key_id`、`security.signature_alg`、`security.data_key_id`；不覆盖 `center`、`status`、`edge`、`manifest`、`security.center_signature` 或 `updated_at`。
  - `crates/center-backend/src/center_security.rs`：Center 签名、验签与 canonical JSON 包装统一转调 Common API，保留密钥管理与调用者接口。
  - `crates/center-backend/src/reinitializer.rs`：reinitialize 前置验签改为验证原始 `disk_info` 文档并复用同一 CenterSecurity/Common API，不再在流程内单独裁剪或补写签名输入。
  - `crates/center-backend/src/reinitialize_runtime.rs` 与 Common crypto 测试：补齐跨流程验签测试，覆盖同一 `disk_info` 可被导入和 reinitialize 验证、非覆盖字段变化不影响、覆盖字段变化必失败、`center` 对象变化不影响。
- 安全边界：
  - 未跳过验签，reinitialize 仍必须通过真实 `center_signature`。
  - 初始化、导入验签、清理/重初始化前验签、重新初始化签名通过 CenterSecurity 复用 Common 同源 API，禁止各流程自行拼 JSON。
  - Edge Dashboard 只读接口改动仍留在当前工作区但本轮未部署；本 P0 修补不触发扫描、导出、导入、清理或重新初始化。
- 验证结果：
  - `cargo fmt --all -- --check`：通过。
  - `cargo test -p rustfs-transfer-common`：通过。
  - `cargo test -p rustfs-transfer-center`：通过。
  - `cargo test -p rustfs-transfer-edge`：通过。
  - 普通 Windows 沙箱运行部分 Rust 测试/格式化时遇到构建目录权限拒绝；使用临时 target 与最小提权重跑后通过。
- 后续真实环境建议：受控重试 reinitialize 前必须先部署包含本修补的 Center 后端，并做只读健康、目标盘身份、`IMPORTED`、`.partial=0`、服务日志门禁；Edge 仅在需要上线 Dashboard 只读接口或保持双端同版时再部署。

### Center signature hotfix deploy, reinitialize gate stopped

- Scope: real Center VM backend deployment only, followed by readonly reinitialize gate. Did not deploy Edge/frontend, did not call reinitialize, did not write transport disk, DB, RustFS source objects, archive, object_ledger, or unrelated disks.
- Deployment input: clean `HEAD` source archive plus only the five signature hotfix files copied into the remote `/tmp` build tree: `crates/common/src/crypto/mod.rs`, `crates/common/tests/crypto_tests.rs`, `crates/center-backend/src/center_security.rs`, `crates/center-backend/src/reinitializer.rs`, `crates/center-backend/src/reinitialize_runtime.rs`. Edge Dashboard and frozen-doc working-tree changes were not included in the Center build.
- Build note: an initial remote `git apply` attempt failed and accidentally built unpatched `HEAD` because the shell did not stop after patch failure; that artifact was not deployed. The deployed artifact was rebuilt after directly copying the five hotfix files into the remote temporary source tree and checking for the new Common API/test marker before build.
- Deployed binary: `/opt/rustfs-transfer/rustfs-transfer-center` SHA256 `c750eafb9b6ac7fca75c3bf086e2711561803adabedadc1a90320721b5fb484d`; previous binary was backed up under `/opt/rustfs-transfer/rustfs-transfer-center.backup-signature-p0-*`; owner/mode installed as `root:rustfs-transfer` `0750`.
- Service validation after restart: `rustfs-transfer-center` active; `/healthz=200`; `/readyz=200`; recent 5-minute log scan found no new business panic/error/failed markers, only the pre-existing system journal truncation warning.
- Reinitialize pre-write gate: stopped before API call because Center currently does not see the imported transport disk. `lsblk` showed only system disk `/dev/sda` with root ext4 and optical `/dev/sr0`; no `/dev/sdb`/`/dev/sdc` transport partition, no mounted `RFS-ZERO-FRESH`, and `/media/control/RFS-ZERO-FRESH/rustfs-transfer/disk_info.json` was absent. `.partial` count under that absent target path was 0.
- Result: no cleanup/reinitialize write was attempted. The next controlled retry requires the user to attach the imported transport disk back to Center, then rerun readonly identity/lifecycle gate before calling the controlled API.

### Center reinitialize retry gate repeated after user disk-insert confirmation

- Scope: readonly Center gate only after user confirmed the transport disk was attached. Did not call reinitialize, did not write disk, DB, RustFS source objects, archive, object_ledger, or unrelated disks.
- Center service remained healthy: `rustfs-transfer-center` active; `/healthz=200`; `/readyz=200`.
- Gate result: Center still did not expose the target transport disk. `lsblk` again showed only system disk `/dev/sda` with root ext4 and optical `/dev/sr0`; `findmnt` had no transport-disk mount; no `RFS-ZERO-FRESH` mount; `/media/control/RFS-ZERO-FRESH/rustfs-transfer/disk_info.json` was absent; `.partial` count under that absent target path was 0.
- Decision: stopped at pre-write gate with zero reinitialize/API writes. User/VM side needs to make the transport disk visible to Center before the controlled retry can proceed.

### Center controlled reinitialize succeeded after target disk gate

- Scope: single Center controlled reinitialize for the unique mounted protocol transport disk only. Did not touch Edge, source objects, unrelated archive/object_ledger rows, other disks, or run broad SQL.
- Pre-write gate: target protocol disk was visible on Center, mounted as ext4, `disk_info.json` present, lifecycle `IMPORTED`, `.partial=0`, Center service active with `/healthz=200` and `/readyz=200`.
- API action: called existing `POST /api/center/disks/{disk_id}/reinitialize` once with the real mount path, seal id from disk_info, `expected_status_code=IMPORTED`, `confirm_reinitialize=true`, and an audit reason. Control token was read from restricted Center env and not logged.
- API result: HTTP 200 with resulting disk lifecycle `INITIALIZED`.
- Postcheck: disk_info lifecycle `INITIALIZED`; `.partial=0`; protocol data/meta/manifests file count 0; Center service active with `/healthz=200` and `/readyz=200`; recent log scan found no new business panic/error/failed markers, only the pre-existing truncated journal warning.
- Local hygiene: temporary local helper script was removed from the workspace after use.

### Edge controlled scan/export sealed after Center reinitialize

- Scope: single Edge controlled scan/export for the current initialized transport disk only. Did not modify/delete RustFS source objects, did not touch Center/import/archive/object_ledger, unrelated disks, or run broad SQL.
- Pre-write gate: target disk mounted on Edge as ext4, protocol `disk_info.json` present, lifecycle `INITIALIZED`, `.partial=0`, Edge service active with `/healthz=200` and `/readyz=200`.
- Scan result: HTTP 200; `bucket_count=3`, `object_seen=59`, `stable_object_count=59`, `source_changed_count=0`, `total_bytes=1428414961`.
- Export job: created with scan reuse (`run_scan=false`) and started through the existing controlled API. Create response reported `object_count=173`, `total_bytes=3331017885`; start assigned `59` objects / `1428414961` bytes to one disk, `worker_started_count=1`, `worker_failed_count=0`.
- Final result: export job reached `SEALED`; final copied count `59`, copied bytes `1428414961`.
- Disk postcheck: lifecycle `SEALED`; `.partial=0`; protocol payload counts `data_files=59`, `meta_files=59`, `manifest_files=2`; Edge service remained healthy. Recent log scan found no new business panic/error/failed markers, only the pre-existing truncated journal warning.
- Local hygiene: temporary local helper script was removed from the workspace after use.

### Center controlled import attempt stopped on API rejection

- Scope: Center controlled import attempt for the current sealed transport disk only. Did not run cleanup/reinitialize, did not touch Edge, source objects, unrelated disks, or perform manual SQL/disk edits.
- Pre-import gate: target disk visible and mounted on Center as ext4; `disk_info.json` present; lifecycle `SEALED`; `.partial=0`; Center service active with `/healthz=200` and `/readyz=200`.
- Manifest/payload gate: `object_count=59`, `total_bytes=1428414961`, `data_files=59`, `meta_files=59`, `manifest_files=2`; disk_info manifest counter, export_manifest object count, data count, and meta count were consistent.
- API action: called existing `POST /api/center/import-jobs/start` once with the real mount path and restricted Center control token; token was not logged.
- API result: HTTP 400, `error_code=INVALID_REQUEST`, message `import_job_status=`. No destructive retry or workaround was attempted.
- Post-failure readonly check: disk lifecycle remained `SEALED`; `.partial=0`; Center service remained healthy. DB impact count was not confirmed because direct container psql attempts did not reach the Center business tables/role cleanly; no manual SQL writes were attempted.
- Local hygiene: temporary local helper scripts were removed from the workspace after use.

### Edge 自动流程上线要求文档整理

- 范围：仅更新 `docs/上线要求.md` 和本日志；未修改冻结文档，未改代码，未操作 VM、硬盘、数据库、RustFS 源对象、archive 或 object_ledger。
- 内容：整理 Edge 端插入硬盘后的完整自动流程，明确服务启动扫描、udev rescan、硬盘准入、Center HTTP 校验、自动 RustFS 扫描、自动创建/启动导出、DiskWorker 写盘封盘、前端即时视觉反馈和全流程状态展示要求。
- 联调契约：补充 Edge Dashboard 只读 HTTP、Edge WebSocket 事件、状态字段命名、页面刷新恢复、多盘 `disks[]` 独立进度、异常错误码展示和危险操作入口限制。
- 验证方式：文档自查，确认未引入 Edge 端初始化/清理/格式化/导入职责，未要求 WebSocket 承担 Center/Edge 数据同步，未使用裸 `status` 作为 API/WS 字段命名。
- 待跟进：如后续实现自动触发 scan/export，需要按本文清单做真实 Edge 前后端联调，并验证插盘 1 秒内 Dashboard 可见反馈。
