# 2026-08-11 开发日志
### TASK-S1-EDGE-AUTO-DEPLOY-001：Edge 自动流程部署开关领取

- 开始：Codex / Agent F1 领取 P1 任务 `TASK-S1-EDGE-AUTO-DEPLOY-001`，同步 `work_sprint/TASKS.md` 顶部总览与任务卡状态为开发中。
- 范围：仅 Edge 自动流程部署配置开关、systemd/udev 静态检查、部署前检查、上线/回滚说明、TASKS 与本日志；不改冻结文档，不改前端和 WebSocket 契约，不部署，不启用真实 Edge `auto_export`，不操作真实盘、RustFS 源对象、归档、对象账本或宽范围数据库。
- 冻结依据：Edge 自动编排只能在常驻服务内由配置显式开启；默认关闭；udev 只触发 rescan，不直接执行 export/import/cleanup/reinitialize/mkfs/format；Edge 不清盘、不初始化、不导入、不修改源对象。

### TASK-S1-EDGE-AUTO-DEPLOY-001：Edge 自动流程部署开关完成

- 完成内容：
  - `deploy/config/edge.example.toml` 新增 `[auto_export]` 示例，默认 `enabled=false`、`start_on_ready=false`、`min_ready_disk_count=1`、`cooldown_seconds=60`。
  - `deploy/config/edge.env.example` 新增同名 `RUSTFS_TRANSFER__AUTO_EXPORT__*` 覆盖变量，默认关闭，便于灰度启用和回滚。
  - `crates/edge-backend/src/config.rs` 支持 `RUSTFS_TRANSFER__AUTO_EXPORT__ENABLED`、`START_ON_READY`、`MIN_READY_DISK_COUNT`、`COOLDOWN_SECONDS` 环境变量覆盖，并补充回归测试确认可开启、可关回。
  - `scripts/check-deploy.ps1` 补齐 Edge systemd ExecStart/配置路径/edge.env 校验、auto_export 默认关闭校验、udev 非注释规则行和 rescan 模板不得直调业务 API 或 export/import/cleanup/reinitialize/mkfs/format 的检查。
  - `docs/上线要求.md` 与 `deploy/systemd/README.md` 补充启用前检查、灰度开启、回滚和排障步骤；回滚后恢复到受控 API 模式。
- 可核验结论：
  - udev 规则只拉起 `rustfs-transfer-disk-rescan@%k.service`；业务词仅出现在注释的禁止说明中，非注释规则行未包含业务动作。
  - rescan 模板只执行 `/opt/rustfs-transfer/bin/rustfs-transfer-rescan --device /dev/%I --trigger udev`，不直接调用 scan/export/import/cleanup/reinitialize。
  - Edge service 使用 `/opt/rustfs-transfer/rustfs-transfer-edge`，配置路径为 `/etc/rustfs-transfer/edge.toml`，并加载 `/etc/rustfs-transfer/edge.env`。
  - 默认配置关闭自动导出；关闭后原 `POST /api/edge/scan`、`POST /api/edge/export-jobs`、`POST /api/edge/export-jobs/{export_job_id}/start` 受控 API 模式保持可用。
- 验证：
  - `cargo fmt --all -- --check`：通过。
  - `powershell -ExecutionPolicy Bypass -File scripts\check-deploy.ps1`：通过。
  - `CARGO_TARGET_DIR=target-codex-edge-auto cargo test -p rustfs-transfer-edge config::tests::auto_export_env_overrides_support_gray_enable_and_rollback -j1`：通过。
  - `CARGO_TARGET_DIR=target-codex-edge-auto cargo test -p rustfs-transfer-edge -j1`：通过，65 个库测试、1 个 DiskWorker 集成测试、2 个 scanner 测试全部通过。
  - `git diff --check`：通过；仅报告 CRLF 提示，无 whitespace error。
- 未覆盖项：未部署 Edge VM，未启用真实 `auto_export`，未操作真实盘或 RustFS 源对象；真实插盘自动流转和浏览器端到端验收留给 `TASK-S1-EDGE-REALTIME-QA-001`。
- 状态同步：`work_sprint/TASKS.md` 顶部总览与本任务卡均已同步为 `[x]`，验收清单均已勾选。

### TASK-S1-EDGE-AUTO-DEPLOY-001：续办静态复核

- 范围：按委派要求仅复核 Edge 自动流程部署开关、配置示例、systemd/udev 静态边界、回滚说明和任务记录；未部署、未重启服务、未启用自动导出、未操作真实盘、数据库或 RustFS 源对象，未领取前端或真实 QA。
- 复核结论：
  - `deploy/config/edge.example.toml` 与 `deploy/config/edge.env.example` 仍保持 `auto_export.enabled=false`、`start_on_ready=false` 默认关闭，并提供灰度启用/回滚覆盖变量。
  - `deploy/systemd/rustfs-transfer-edge.service` 使用 `/opt/rustfs-transfer/rustfs-transfer-edge`，配置路径为 `/etc/rustfs-transfer/edge.toml`，并加载 `/etc/rustfs-transfer/edge.env`。
  - `deploy/udev/99-rustfs-transfer-disk.rules` 只拉起 `rustfs-transfer-disk-rescan@%k.service`；`rustfs-transfer-disk-rescan@.service` 只执行 rescan 通知入口，不直调 scan/export/import/cleanup/reinitialize/mkfs/format。
  - `docs/上线要求.md` 和 `deploy/systemd/README.md` 已说明默认关闭、启用前检查、灰度开启、关闭回滚和恢复受控 API 模式。
- 本轮验证：
  - `powershell -ExecutionPolicy Bypass -File scripts\check-deploy.ps1`：通过。
  - `git diff --check`：通过；仅有 LF/CRLF 提示，无 whitespace error。
  - `cargo fmt --all -- --check`：通过。
  - `CARGO_TARGET_DIR=target-codex-edge-auto cargo test -p rustfs-transfer-edge config::tests::auto_export_env_overrides_support_gray_enable_and_rollback -j1`：通过，1 个聚焦测试通过。
- 未覆盖项：未做真实 Edge VM 部署、服务重启、自启动验证、真实 udev 事件、真实插盘自动流转、浏览器 1 秒反馈或硬件验收；这些仍应由 `TASK-S1-EDGE-REALTIME-QA-001` 或部署验收窗口覆盖。

### TASK-S1-WEB-EDGE-TIMELINE-001：Edge Dashboard 时间线领取

- 开始：Codex / Agent E4 领取 P1 任务 `TASK-S1-WEB-EDGE-TIMELINE-001`，同步 `work_sprint/TASKS.md` 顶部总览与任务卡状态为开发中。
- 范围：仅 `web/edge-web` 的真实 HTTP summary 与本端 WebSocket 消费、插盘即时反馈、每盘时间线、扫描/导出/可拔盘/错误态展示、断线重连视觉反馈，以及本任务 TASKS/dev log 状态；不改后端契约、不部署、不操作真实盘、不新增假数据或浏览器写操作。
- 协议依据：已阅读冻结协议中 WebSocket/HTTP 边界、`disks[]` 分盘进度来源、`disk_status_code`/`runtime_status`/`export_job_status`/`object_status` 命名约束，以及 `SEAL_DONE`/`SEALED + DONE` 才能展示可拔盘的约束。

### TASK-S1-WEB-EDGE-TIMELINE-001：Edge Dashboard 时间线完成

- 完成内容：
  - `edgeCopyProgress.ts` 接入冻结 WS 事件 `SCAN_STARTED`、`SCAN_PROGRESS`、`SCAN_DONE`、`COPY_STARTED`、`COPY_PROGRESS`、`COPY_DONE`、`SEAL_DONE`、`DISK_*` 和 `ERROR`，扫描事件更新扫描汇总，盘级事件按 `disks[]` 更新独立盘位。
  - `api/edgeDashboard.ts` 为未注册/异常盘的空 `disk_id` 使用后端返回的 `mount_path`/`disk_sn` 作为前端展示键，避免插盘即时反馈因空 key 合并丢失；不创造假盘或成功态。
  - `DashboardView.vue` 新增选中盘固定九节点时间线：已检测、校验中、已授权、扫描 RustFS、分配对象、写盘中、生成清单、封盘完成、可拔盘；错误/拒绝态显示 `last_error_code` 与后端 message。
  - 可拔盘态严格限定为收到 `SEAL_DONE`，或后端 summary/WS 确认 `disk_status_code=SEALED` 且 `runtime_status=DONE`；封盘前只显示不可拔盘。
  - 扫描面板展示扫描事件、bucket/对象数、稳定对象数和容量；导出区域保留全局进度、每盘进度、当前对象、速度和剩余字节。
  - 移除 Dashboard 残留的禁用“执行恢复检查”按钮，浏览器界面继续只读观察，不调用受控写接口。
- 验证：
  - `pnpm test:unit`：通过，13 个用例通过。
  - `pnpm typecheck`：通过。
  - `pnpm build`：沙箱内因 Vite 写 `node_modules/.vite-temp` 权限失败；按权限规则使用沙箱外 `npm run build` 验证通过。
- 状态同步：`work_sprint/TASKS.md` 顶部总览与本任务卡均已同步为 `[x]`，本任务验收清单均已勾选。
- 真实联调前未覆盖项：本轮未部署 Edge VM、未操作真实运输盘、未做浏览器真机截图；插盘 1 秒内可见性、WS 断线后真实重连序列、移动端与 1440x810 实机视觉验收仍需由 `TASK-S1-EDGE-REALTIME-QA-001` 在真实环境确认。

### TASK-S1-SECURITY-HOTFIX-006：AES-GCM AAD 同源构造与全加密面审计

- 范围：仅代码、测试、TASKS 与本日志记录；未部署、未调用真实 API/VM，未操作硬盘、数据库或生产 SQL。
- 冻结协议依据：用户已更新 `docs/v1.0冻结/开发协议契约.md`，明确 `center` 不属于 `center_signature` 覆盖字段；本轮只读取冻结文档，不修改、不暂存冻结目录。
- 全加密面审计：
  - `center_signature` canonical 只覆盖 `protocol`、`disk`、`security.center_key_id`、`security.signature_alg`、`security.data_key_id`；初始化、导入、重初始化验签均走 `CenterSecurity` 的同一真实 HMAC 实现。
  - Center `disk_data_key` 包裹/解包继续使用本机主密钥与 `disk_id + data_key_id` AAD，不包含整个 `center` 对象。
  - Edge HTTP HMAC 继续使用 method、canonical path/query、timestamp、nonce、body sha256；`edge_auth_secret` 未复用为对象加密密钥。
  - Edge 对象写盘与 Center 导入解密使用 AES-256-GCM；修补后 Common AAD 绑定 `disk_id`、`seal_id`、`export_job_id`、`bucket`、`key`、`chunk_group_id`、`chunk_index`、`chunk_total`、`chunk_offset_bytes`。
  - Web、deploy、scripts 检索未发现明文 `disk_data_key`、控制 token、签名密钥或真实密钥落入浏览器响应/示例配置；示例仍使用占位值。
- 修补内容：
  - `crates/common/src/crypto/mod.rs`：补齐 `ObjectAad` 字段并统一 AAD 字符串构造。
  - `crates/edge-backend/src/disk_worker.rs`：Edge manifest AAD 改为复用 Common 构造。
  - `crates/center-backend/src/import_worker.rs`：Center 导入前按 manifest 业务字段复算 AAD，不匹配时以 `MANIFEST_INVALID` 拒绝，不进入 AES-GCM 解密。
  - 测试和 fixture 同步：Common crypto/golden、Edge DiskWorker manifest AAD 断言、Center ImportWorker AAD 篡改拒绝。
- 验证结果：
  - `cargo fmt --all -- --check`：通过。
  - `cargo test -p rustfs-transfer-common`：普通沙箱因 `target/debug/.cargo-build-lock` 权限拒绝未启动；最小权限重跑通过。
  - `cargo test -p rustfs-transfer-edge`：普通沙箱因同一构建锁权限拒绝未启动；最小权限重跑通过，包含 Edge DiskWorker manifest AAD 断言。
  - `cargo test -p rustfs-transfer-center`：最小权限重跑通过，包含 Center ImportWorker AAD 篡改拒绝。
  - `cargo test --workspace`：最小权限重跑通过。
  - `powershell -ExecutionPolicy Bypass -File scripts\check-deploy.ps1`：通过。
  - `git diff --check`：通过；冲突标记检查无命中；敏感抽查仅命中检查脚本、环境变量名、`CHANGE_ME` 示例和代码变量名，未发现真实密钥。
  - 工作区仍有用户本人更新的 `docs/v1.0冻结/开发协议契约.md` diff，以及 2026-08-10 VM 日志和原型图片等并发改动；本提交不暂存、不修改、不覆盖这些内容。

### 原型图 1920x1080 等比适配

- 范围：仅处理 `docs/04-原型设计/` 下两张 Codex 原型截图，生成独立 1920x1080 成稿；不修改源图，不触碰 `docs/v1.0冻结/`。
- 输出：
  - `codex-clipboard-041d3a8b-2692-40fb-a36c-34acbb591a3f-1920x1080.png`
  - `codex-clipboard-99c9e886-e501-40b6-9173-fa0b7f2547c8-1920x1080.png`
- 处理方式：主体图按高度等比 contain 到 1080，高度完整保留；左右用同源深色模糊延展补足 1920 宽，避免字体、图标横向拉伸或主体裁切。
- 验证结果：使用图片尺寸读取确认两张输出均为 `1920x1080`；人工查看确认顶部、底部、左右核心 UI 区域完整可见，无明显拉伸或裁切。
### VM b358efec backend deploy and Center zero-disk initialize

- Scope: real Center and Edge backend deployment plus Center-only transport disk registration/initialization. Did not deploy frontends, format disks, run broad SQL, call Edge scan/export, import, touch source objects, archive objects, unrelated disks, or FUSTFS-A-DRILL.
- Source: deployed current main commit `b358efec577eb6b3ee38f1c61bff83c648304281` (`修补 AES-GCM AAD 同源校验`), which includes the prior center_signature scope convergence.
- Build/deploy: built Center release on Center VM using `/home/control/.cargo/bin/rustup run stable` (`rustc 1.97.1`, `cargo 1.97.1`) and installed `/opt/rustfs-transfer/rustfs-transfer-center`, SHA256 `1382f601922f9a1fa4ca46068dfdcb0c5072db5f9cfe3e380fb8adad811dec6c`. Edge VM lacked `cc`, so Edge binaries were built on the same-arch Center VM (`x86_64`) and deployed to Edge: `rustfs-transfer-edge` SHA256 `a41c85072bc07055f8715b42d35f5edf81bca2642459177918b2e9e8147d621a`, `rustfs-transfer-rescan` SHA256 `f80d5a68fd8b76a03f8dde973e1dc6e5338b308587b2e78ffd818d91b573bf5c`.
- Service validation: Center and Edge systemd services were active after restart. Center `/healthz` and `/readyz` returned 200; Edge `/healthz` and `/readyz` returned 200. Log scan found no business panic/error markers; only existing system journal truncation warnings appeared.
- Center pre-write disk gate before registration and again before initialization: target `/dev/sdc1`, parent `/dev/sdc`, SN `323533383650343031383331`, FS UUID `2cf552ec-62b8-4e75-84df-eb8da998d2ec`, label `RFS-ZERO-FRESH`, ext4, size `1000202788864`, mount `/media/control/RFS-ZERO-FRESH`; non-system/non-optical checks passed; protocol directory was absent; `.partial=0`; service user had root write permission.
- Center DB baseline before registration: `disk_list=0`, `data_key=0`, `import_job=0`, `object_ledger=0`, `chunk_import_group=0`, `chunk_import_part=0`, `edge_site=1`, same-SN disk registration `0`.
- Controlled API result: registered new disk_id `79bafb4e-4aba-4492-b032-3abf73dc03d2`; initialized it with new data_key_id `48c2bba2-0908-4605-9c0f-f604b334acfa`; API status_code was `INITIALIZED`.
- Post-initialize validation: disk_info fields match disk_id/SN/capacity/protocol `1.0`; status `INITIALIZED`, sealed=false, imported=false, reusable=true; center_key_id matched current config-derived key id; center_signature verification passed with signature length 32 and covered paths `disk`, `protocol`, `security.center_key_id`, `security.data_key_id`, `security.signature_alg`. No key, signature value, or plaintext secret was logged.
- DB postcheck: `disk_list=1`, `data_key=1`, `import_job=0`, `object_ledger=0`, `chunk_import_group=0`, `chunk_import_part=0`, `edge_site=1`. New data_key is `ACTIVE`, bound to the new disk, and not bound to edge/export yet. Protocol data/meta/manifests payload entry count is 0 and `.partial=0`.
- Stop point: initialized disk is ready for the user to physically move to Edge. No Edge scan/export/import was started.

### Edge zero-disk readonly gate stopped before scan/export

- Scope: real Edge VM readonly gate for the Center-initialized transport disk. Did not trigger Edge scan, create/start export jobs, modify source objects, format/clean/reinitialize the disk, run broad SQL, touch archive/Center import, or write disk protocol payloads.
- Edge service health: `rustfs-transfer-edge` was active; `/healthz` and `/readyz` returned 200.
- Physical disk gate passed on actual Edge device: `/dev/sdb1`, parent `/dev/sdb`, SN `323533383650343031383331`, FS UUID `2cf552ec-62b8-4e75-84df-eb8da998d2ec`, label `RFS-ZERO-FRESH`, ext4, partition size `1000202788864`, mount `/media/edge/RFS-ZERO-FRESH`; non-system/non-optical checks passed.
- Disk protocol gate passed: disk_info disk_id `79bafb4e-4aba-4492-b032-3abf73dc03d2`, data_key_id `48c2bba2-0908-4605-9c0f-f604b334acfa`, status `INITIALIZED`, signature length 32, `.partial=0`, protocol payload entry count 0, canonical SHA256 `59492802fcae2e962f3a954637c1a671ac8350ece7348e47d91997d5a89283ef`.
- Center-side true HMAC verification of the Edge disk_info passed using the deployed Center signing configuration: center_signature_valid=true, center_key_id_match=true, covered paths `disk`, `protocol`, `security.center_key_id`, `security.data_key_id`, `security.signature_alg`. No key, token, signature value, or full JSON was logged.
- Stop reason: Edge control summary / local DB still had stale runtime state for the same mount and SN: disk_runtime row id 12 had old disk_id `eeca55c4-ee87-4bcb-b797-7ef83899e172`, device `/dev/sdb1`, mount `/media/edge/RFS-ZERO-FRESH`, status `DONE`, last_seen_at `2026-08-10T11:51:33.711602`. Current disk_info disk_id is `79bafb4e-4aba-4492-b032-3abf73dc03d2`. Old Edge tables also still contained one `SEALED` export_job and 57 `EXPORTED` export_object rows bound to the old disk_id.
- Safety decision: stopped before `/api/edge/scan` and before any export API because runtime identity was not READY and did not match the current disk_info. Current run caused no Edge scan/export writes; only temporary diagnostic scripts and readonly disk_info copies were created under `/tmp`.

### Edge 7d658b6 deploy attempt stopped by build-scope/toolchain blocker

- Scope requested: deploy only Edge backend commit `7d658b69249ae97a14974416d35f31a23a265d0c` and then perform readonly health/rescan-summary/current disk checks. No frontend, scan, export, source object, Center, archive, object_ledger, or disk payload writes were authorized.
- Local source state: `HEAD` was `7d658b69249ae97a14974416d35f31a23a265d0c` (`修补 Edge 运行态可重建准入策略`).
- Blocker: Edge VM is `x86_64` with Rust stable `rustc 1.97.1` / `cargo 1.97.1`, but has no `cc`, `gcc`, `clang`, `musl-gcc`, or equivalent C compiler available. Only `/usr/bin/ld` exists. Because the current authorization was Edge-only and prohibited extra system changes, no package installation or Center-side build was performed. Therefore the Edge binary was not replaced with the `7d658b6` build.
- Current deployed Edge binary remained the previous `b358efec` build: `rustfs-transfer-edge` SHA256 `a41c85072bc07055f8715b42d35f5edf81bca2642459177918b2e9e8147d621a`, `rustfs-transfer-rescan` SHA256 `f80d5a68fd8b76a03f8dde973e1dc6e5338b308587b2e78ffd818d91b573bf5c`.
- Readonly Edge health/current disk check: service active; `/healthz=200`; `/readyz=200`. Current disk `/dev/sdb1` under `/dev/sdb`, SN `323533383650343031383331`, FS UUID `2cf552ec-62b8-4e75-84df-eb8da998d2ec`, label `RFS-ZERO-FRESH`, ext4, size `1000202788864`, mount `/media/edge/RFS-ZERO-FRESH`; disk_info disk_id `79bafb4e-4aba-4492-b032-3abf73dc03d2`, data_key_id `48c2bba2-0908-4605-9c0f-f604b334acfa`, status `INITIALIZED`, signature length 32, `.partial=0`, protocol payload entries 0.
- Readonly Edge summary/DB check: current summary reported one disk with disk_id `79bafb4e-4aba-4492-b032-3abf73dc03d2`, runtime_status `READY`, disk_status_code `INITIALIZED`, last_error null. Edge DB counts: `disk_runtime=1`, `local_object_snapshot=0`, `export_job=0`, `export_object=0`.
- No rescan/scan/export was triggered in this attempt; no DB/disk/source/Center/archive/object_ledger writes were performed in this attempt.

### Edge 7d658b6 real VM build/deploy

- Scope: Edge VM backend only. Built `rustfs-transfer-edge` from clean local `HEAD` archive at commit `7d658b69249a` in `/tmp` on Edge, deployed to `/opt/rustfs-transfer/rustfs-transfer-edge`, and restarted only `rustfs-transfer-edge`. No rescan, scan, export, import, cleanup, Center operation, source-object write, archive/object_ledger write, or transport-disk payload write was performed.
- Build result: `cargo build --release -p rustfs-transfer-edge --locked` completed successfully on Edge using Rust stable `1.97.1`.
- Binary deployment: old Edge binary SHA256 `a41c85072bc07055f8715b42d35f5edf81bca2642459177918b2e9e8147d621a` was backed up to `/opt/rustfs-transfer/rustfs-transfer-edge.backup-7d658b6-20260811T010242Z`; new binary SHA256 `ab9a38a9e2a61e122a7f73fe4e68c01b07e18e8df1693a4acb8acc1c238f480f` installed with owner `root:rustfs-transfer` and mode `0750`.
- Service validation: `systemctl is-active rustfs-transfer-edge` returned `active`; `/healthz=200`; `/readyz=200`.
- Readonly disk gate after deploy: current mount `/media/edge/RFS-ZERO-FRESH` is `/dev/sdb1`, ext4, FS UUID `2cf552ec-62b8-4e75-84df-eb8da998d2ec`, label `RFS-ZERO-FRESH`, size `915.8G`; parent `/dev/sdb` SN `323533383650343031383331`, size `931.5G`. Disk protocol status is `INITIALIZED`, disk_id `79bafb4e-4aba-4492-b032-3abf73dc03d2`, `.partial=0`.
- Log note: recent post-restart journal grep found no new business panic/error/failed markers; system journal still reports an unrelated truncated old journal file warning.

### Edge controlled export and seal

- Scope: Edge VM controlled export for the current transport disk only. Used existing Edge control API paths for scan, export job create, and start. Did not call Center import/cleanup, did not format/reinitialize, did not delete or modify source objects, and did not touch archive/object_ledger or unrelated disks.
- Pre-write gate: service active, `/healthz=200`, `/readyz=200`; mount `/dev/sdb1` at `/media/edge/RFS-ZERO-FRESH`, ext4, FS UUID `2cf552ec-62b8-4e75-84df-eb8da998d2ec`, label `RFS-ZERO-FRESH`; parent SN `323533383650343031383331`; disk_id `79bafb4e-4aba-4492-b032-3abf73dc03d2`; disk status `INITIALIZED`; `.partial=0`.
- Scan result: `scan_status=DONE`, `object_seen=57`, `stable_object_count=57`, `source_changed_count=0`, `total_bytes=951301462`.
- Export job: `export_job_id=2f88cd24-4176-43da-9dc8-2de0042ad5d3`; create response reported `object_count=114`, `total_bytes=1902602924`; start assigned and copied `57` objects / `951301462` bytes with `worker_started=1`, `worker_failed=0`.
- Final Edge API/DB readonly check: job status `SEALED`; `export_object` rows for this job are `EXPORTED=57`, all bound to disk_id `79bafb4e-4aba-4492-b032-3abf73dc03d2`, bytes `951301462`; `local_object_snapshot=114`; total jobs by status: `SEALED=1`. The job aggregate count/bytes retained the doubled snapshot count, while actual exported rows and copied bytes are 57 / 951301462.
- Disk validation: disk_info status `SEALED`, sealed=true, imported=false, reusable=false; `.partial=0`; protocol counts after seal: `manifest_files=2`, `data_files=57`, `meta_files=57`.
- Service/log validation: final `/healthz=200`, `/readyz=200`; no new business panic/error/failed markers were found in the export window, only the pre-existing truncated journal-file warning.

### Center controlled import of sealed disk

- Scope: Center VM controlled import for the current sealed transport disk only. Used existing `POST /api/center/import-jobs/start` with the real mount path. Did not run cleanup/reinitialize, did not touch Edge, unrelated disks, or perform manual SQL/disk edits.
- Pre-import gate: Center service active, `/healthz=200`, `/readyz=200`; target `/dev/sdc1` mounted at `/media/control/RFS-ZERO-FRESH`, ext4, FS UUID `2cf552ec-62b8-4e75-84df-eb8da998d2ec`, label `RFS-ZERO-FRESH`; parent `/dev/sdc` SN `323533383650343031383331`; disk_id `79bafb4e-4aba-4492-b032-3abf73dc03d2`; disk status `SEALED`; `.partial=0`; payload counts before import `manifest_files=2`, `data_files=57`, `meta_files=57`.
- Signature gate: center_signature verification passed with `key_id_match=true`, signature length `32`, canonical SHA256 `59492802fcae2e962f3a954637c1a671ac8350ece7348e47d91997d5a89283ef`, covered paths `disk`, `protocol`, `security.center_key_id`, `security.data_key_id`, `security.signature_alg`.
- Import result: `import_job_id=223ae407-25c0-4776-a3a1-5158a40142d1`, status `DONE`, outcome `IMPORTED`, objects `57/57`, bytes `951301462/951301462`.
- DB readonly validation: import_job row is `DONE`, bound to disk_id `79bafb4e-4aba-4492-b032-3abf73dc03d2`, seal_id `03aeab52-15e7-4b55-b725-74ebfb1681aa`, export_job_id `2f88cd24-4176-43da-9dc8-2de0042ad5d3`; `object_ledger` rows for this import job are `57`, bytes `951301462`, one archive bucket and 57 archive keys. data_key `48c2bba2-0908-4605-9c0f-f604b334acfa` is `SEALED_READONLY` and bound to the same export_job_id/seal_id.
- Post-import disk validation: disk_info status `IMPORTED`, sealed=true, imported=true, reusable=true, import_job_id `223ae407-25c0-4776-a3a1-5158a40142d1`; `.partial=0`; protocol payload remains `manifest_files=2`, `data_files=57`, `meta_files=57`; post-import center_signature verification still passed.
- Service/log validation: final `/healthz=200`, `/readyz=200`; no new business panic/error/failed markers were found in the import window, only the pre-existing truncated journal-file warning.

### Center controlled reinitialize stopped by signature preflight

- Scope: attempted the existing controlled `POST /api/center/disks/{disk_id}/reinitialize` path for the current imported disk only. There is no separate cleanup HTTP route in the deployed Center service; this API performs cleanup and reinitialize in one controlled state machine. No manual SQL, manual disk edit, cleanup workaround, or retry was performed.
- Pre-write gate passed: Center service active, `/healthz=200`, `/readyz=200`; target `/dev/sdc1` mounted at `/media/control/RFS-ZERO-FRESH`, ext4, FS UUID `2cf552ec-62b8-4e75-84df-eb8da998d2ec`, label `RFS-ZERO-FRESH`; parent `/dev/sdc` SN `323533383650343031383331`; disk_id `79bafb4e-4aba-4492-b032-3abf73dc03d2`; seal_id `03aeab52-15e7-4b55-b725-74ebfb1681aa`; disk status `IMPORTED`; `.partial=0`.
- API result: stopped with HTTP `400`, `error_code=INVALID_REQUEST`, message `disk_info signature invalid: disk_info center_signature verification failed`. Per runbook, no destructive retry or bypass was attempted.
- Zero-write evidence after failure: disk_info still `IMPORTED`; `.partial=0`; payload still `data_files=57`, `meta_files=57`, `manifest_files=2`; disk_info SHA256 `82e3d42889040ca5ee9ae1a7217a62d011bb1b41b72cb8a99973380e11847b3b`.
- DB readonly evidence after failure: data_key rows for this disk remain `SEALED_READONLY=1`; old data_key `48c2bba2-0908-4605-9c0f-f604b334acfa` remains bound to export_job `2f88cd24-4176-43da-9dc8-2de0042ad5d3` and seal_id `03aeab52-15e7-4b55-b725-74ebfb1681aa`; import_job `223ae407-25c0-4776-a3a1-5158a40142d1` remains `DONE` with `57/57` objects and `951301462/951301462` bytes; object_ledger for the job remains `57` rows / `951301462` bytes.
- Service validation after failure: `/healthz=200`, `/readyz=200`; only the pre-existing truncated journal-file warning was observed in the readonly log check.

### TASK-S1-EDGE-HOTFIX-003：Edge disk_runtime 可重建准入修补

- 范围：仅代码、测试、TASKS 与本日志记录；未部署、未调用真实 API/VM，未操作硬盘、数据库或生产 SQL，未修改冻结文档。
- 架构口径：用户明确 `disk_runtime` 仅代表“当前插入、当前可运行”的临时运行态；新盘发现、scan、创建/启动导出任务不得以旧 `disk_runtime.disk_id` 与盘内 `disk_info.disk_id` 匹配作为准入条件。
- 修补内容：
  - `crates/edge-backend/src/server.rs`、`crates/edge-backend/src/rescan.rs`：受控 scan、创建导出任务、启动导出任务前同步执行一次当前运输盘发现；发现失败则在写任务前拒绝。
  - `crates/edge-backend/src/disk_detection.rs`：当前盘发现成功时，先按当前 `device_path`/`mount_path` 和当前盘 `disk_id` 替换同盘旧 runtime 快照，再写入本次发现结果；旧缓存不参与盘身份选择或拒绝。
  - `crates/edge-backend/src/disk_worker.rs`、`crates/edge-backend/src/export_runtime.rs`：导出成功、manifest 与封盘写入完成后，在同一受控完成流程中删除该盘对应 `disk_runtime`；不删除 `export_job`、`export_object`、manifest 或 seal 历史。
  - `crates/edge-backend/src/control.rs`：控制查询仍只读取当前 runtime 表内 READY 行，不使用物理槽位排序作为盘身份验收门禁。
- 安全边界：
  - 盘内 `disk_info.disk_id` 仍必须经 Center HMAC `/api/disk/verify` 与登记状态校验，且盘内状态必须是 `INITIALIZED`。
  - 非 ext4、缺硬件 SN、`.partial` 残留、`EDGE_COPYING`、Center 签名/登记拒绝仍不得进入 READY。
  - 历史 `export_job`、`export_object`、manifest、seal 和真实导出审计记录不作为新盘准入前置，也不会被本补丁清理。
- 验证结果：
  - `cargo fmt --all -- --check`：通过。
  - `cargo test -p rustfs-transfer-edge`：普通沙箱因 `target/debug/.cargo-build-lock` 权限拒绝未启动；最小权限重跑通过，覆盖旧 runtime 不阻断新盘、封盘成功后 runtime 删除且历史保留、Center 签名拒绝/非 ext4/`.partial` 阻断。
  - `powershell -ExecutionPolicy Bypass -File scripts\check-deploy.ps1`：通过。
  - 真实 Edge VM 尚未部署本补丁；需部署窗口更新 Edge 后先执行只读 rescan/summary 核验，再触发 scan/export。

### TASK-S1-SECURITY-HOTFIX-007：center_signature 同源签名/验签 API 与 reinitialize 分叉修复

- 范围：仅代码、测试、TASKS 与本日志记录；未部署 Center/Edge VM，未调用真实 API，未操作运输盘、数据库、RustFS 源对象、archive 或 object_ledger，未修改冻结文档。
- 真实阻塞背景：本轮真实闭环已完成 Edge 导出封盘与 Center 导入，盘内 `IMPORTED` 后受控 reinitialize 在写入前因 `disk_info center_signature verification failed` 拒绝；导入路径验签通过而 reinitialize 前置验签失败，定位为签名输入口径分叉。
- 修补内容：
  - `crates/common/src/crypto/mod.rs`：新增唯一 `center_signature` payload/canonicalize/sign/verify API。覆盖字段严格为 `protocol`、`disk`、`security.center_key_id`、`security.signature_alg`、`security.data_key_id`；不覆盖 `center`、`status`、`edge`、`manifest`、`security.center_signature` 或 `updated_at`。
  - `crates/center-backend/src/center_security.rs`：Center 签名、验签与 canonical JSON 包装统一转调 Common API，保留密钥管理与调用者接口。
  - `crates/center-backend/src/reinitializer.rs`：reinitialize 前置验签改为验证原始 `disk_info` 文档并复用同一 CenterSecurity/Common API，不再在流程内单独裁剪或补写签名输入。
  - `crates/center-backend/src/reinitialize_runtime.rs` 与 Common crypto 测试：补齐跨流程验签测试，覆盖同一 `disk_info` 可被导入和 reinitialize 验证、非覆盖字段变化不影响、覆盖字段变化必失败、`center` 对象变化不影响。
- 安全边界：
  - 未跳过验签，reinitialize 仍必须通过真实 `center_signature`。
  - 初始化、导入验签、清理/重初始化前验签、重新初始化签名通过 CenterSecurity 复用 Common 同源 API，禁止各流程自行拼 JSON。
  - Edge Dashboard 只读接口改动仍留在当前工作区但本轮未部署；本 P0 修补不触发扫描、导出、导入、清理或重新初始化。
- 验证结果：
  - `cargo fmt --all -- --check`：通过。
  - `cargo test -p rustfs-transfer-common`：通过。
  - `cargo test -p rustfs-transfer-center`：通过。
  - `cargo test -p rustfs-transfer-edge`：通过。
  - 普通 Windows 沙箱运行部分 Rust 测试/格式化时遇到构建目录权限拒绝；使用临时 target 与最小提权重跑后通过。
- 后续真实环境建议：受控重试 reinitialize 前必须先部署包含本修补的 Center 后端，并做只读健康、目标盘身份、`IMPORTED`、`.partial=0`、服务日志门禁；Edge 仅在需要上线 Dashboard 只读接口或保持双端同版时再部署。

### Center signature hotfix deploy, reinitialize gate stopped

- Scope: real Center VM backend deployment only, followed by readonly reinitialize gate. Did not deploy Edge/frontend, did not call reinitialize, did not write transport disk, DB, RustFS source objects, archive, object_ledger, or unrelated disks.
- Deployment input: clean `HEAD` source archive plus only the five signature hotfix files copied into the remote `/tmp` build tree: `crates/common/src/crypto/mod.rs`, `crates/common/tests/crypto_tests.rs`, `crates/center-backend/src/center_security.rs`, `crates/center-backend/src/reinitializer.rs`, `crates/center-backend/src/reinitialize_runtime.rs`. Edge Dashboard and frozen-doc working-tree changes were not included in the Center build.
- Build note: an initial remote `git apply` attempt failed and accidentally built unpatched `HEAD` because the shell did not stop after patch failure; that artifact was not deployed. The deployed artifact was rebuilt after directly copying the five hotfix files into the remote temporary source tree and checking for the new Common API/test marker before build.
- Deployed binary: `/opt/rustfs-transfer/rustfs-transfer-center` SHA256 `c750eafb9b6ac7fca75c3bf086e2711561803adabedadc1a90320721b5fb484d`; previous binary was backed up under `/opt/rustfs-transfer/rustfs-transfer-center.backup-signature-p0-*`; owner/mode installed as `root:rustfs-transfer` `0750`.
- Service validation after restart: `rustfs-transfer-center` active; `/healthz=200`; `/readyz=200`; recent 5-minute log scan found no new business panic/error/failed markers, only the pre-existing system journal truncation warning.
- Reinitialize pre-write gate: stopped before API call because Center currently does not see the imported transport disk. `lsblk` showed only system disk `/dev/sda` with root ext4 and optical `/dev/sr0`; no `/dev/sdb`/`/dev/sdc` transport partition, no mounted `RFS-ZERO-FRESH`, and `/media/control/RFS-ZERO-FRESH/rustfs-transfer/disk_info.json` was absent. `.partial` count under that absent target path was 0.
- Result: no cleanup/reinitialize write was attempted. The next controlled retry requires the user to attach the imported transport disk back to Center, then rerun readonly identity/lifecycle gate before calling the controlled API.

### Center reinitialize retry gate repeated after user disk-insert confirmation

- Scope: readonly Center gate only after user confirmed the transport disk was attached. Did not call reinitialize, did not write disk, DB, RustFS source objects, archive, object_ledger, or unrelated disks.
- Center service remained healthy: `rustfs-transfer-center` active; `/healthz=200`; `/readyz=200`.
- Gate result: Center still did not expose the target transport disk. `lsblk` again showed only system disk `/dev/sda` with root ext4 and optical `/dev/sr0`; `findmnt` had no transport-disk mount; no `RFS-ZERO-FRESH` mount; `/media/control/RFS-ZERO-FRESH/rustfs-transfer/disk_info.json` was absent; `.partial` count under that absent target path was 0.
- Decision: stopped at pre-write gate with zero reinitialize/API writes. User/VM side needs to make the transport disk visible to Center before the controlled retry can proceed.

### Center controlled reinitialize succeeded after target disk gate

- Scope: single Center controlled reinitialize for the unique mounted protocol transport disk only. Did not touch Edge, source objects, unrelated archive/object_ledger rows, other disks, or run broad SQL.
- Pre-write gate: target protocol disk was visible on Center, mounted as ext4, `disk_info.json` present, lifecycle `IMPORTED`, `.partial=0`, Center service active with `/healthz=200` and `/readyz=200`.
- API action: called existing `POST /api/center/disks/{disk_id}/reinitialize` once with the real mount path, seal id from disk_info, `expected_status_code=IMPORTED`, `confirm_reinitialize=true`, and an audit reason. Control token was read from restricted Center env and not logged.
- API result: HTTP 200 with resulting disk lifecycle `INITIALIZED`.
- Postcheck: disk_info lifecycle `INITIALIZED`; `.partial=0`; protocol data/meta/manifests file count 0; Center service active with `/healthz=200` and `/readyz=200`; recent log scan found no new business panic/error/failed markers, only the pre-existing truncated journal warning.
- Local hygiene: temporary local helper script was removed from the workspace after use.

### Edge controlled scan/export sealed after Center reinitialize

- Scope: single Edge controlled scan/export for the current initialized transport disk only. Did not modify/delete RustFS source objects, did not touch Center/import/archive/object_ledger, unrelated disks, or run broad SQL.
- Pre-write gate: target disk mounted on Edge as ext4, protocol `disk_info.json` present, lifecycle `INITIALIZED`, `.partial=0`, Edge service active with `/healthz=200` and `/readyz=200`.
- Scan result: HTTP 200; `bucket_count=3`, `object_seen=59`, `stable_object_count=59`, `source_changed_count=0`, `total_bytes=1428414961`.
- Export job: created with scan reuse (`run_scan=false`) and started through the existing controlled API. Create response reported `object_count=173`, `total_bytes=3331017885`; start assigned `59` objects / `1428414961` bytes to one disk, `worker_started_count=1`, `worker_failed_count=0`.
- Final result: export job reached `SEALED`; final copied count `59`, copied bytes `1428414961`.
- Disk postcheck: lifecycle `SEALED`; `.partial=0`; protocol payload counts `data_files=59`, `meta_files=59`, `manifest_files=2`; Edge service remained healthy. Recent log scan found no new business panic/error/failed markers, only the pre-existing truncated journal warning.
- Local hygiene: temporary local helper script was removed from the workspace after use.

### Center controlled import attempt stopped on API rejection

- Scope: Center controlled import attempt for the current sealed transport disk only. Did not run cleanup/reinitialize, did not touch Edge, source objects, unrelated disks, or perform manual SQL/disk edits.
- Pre-import gate: target disk visible and mounted on Center as ext4; `disk_info.json` present; lifecycle `SEALED`; `.partial=0`; Center service active with `/healthz=200` and `/readyz=200`.
- Manifest/payload gate: `object_count=59`, `total_bytes=1428414961`, `data_files=59`, `meta_files=59`, `manifest_files=2`; disk_info manifest counter, export_manifest object count, data count, and meta count were consistent.
- API action: called existing `POST /api/center/import-jobs/start` once with the real mount path and restricted Center control token; token was not logged.
- API result: HTTP 400, `error_code=INVALID_REQUEST`, message `import_job_status=`. No destructive retry or workaround was attempted.
- Post-failure readonly check: disk lifecycle remained `SEALED`; `.partial=0`; Center service remained healthy. DB impact count was not confirmed because direct container psql attempts did not reach the Center business tables/role cleanly; no manual SQL writes were attempted.
- Local hygiene: temporary local helper scripts were removed from the workspace after use.

### TASK-S1-CENTER-HOTFIX-006：Center import API 空 import_job_status 错误包装修补

- 范围：仅本地代码、测试、TASKS 与本日志记录；未部署 Center/Edge VM，未调用真实 API，未操作运输盘、数据库、RustFS 源对象、archive、object_ledger 或其他盘，未修改冻结文档。
- 根因：`ProductionCenterImportControlService` 在 `ImportWorker` 失败时总是把 `progress.snapshot().import_job_status` 作为 `anyhow::context` 最外层错误；当失败发生在 `progress.start(...)` 之前，例如导入任务创建/前置校验阶段，默认进度状态为空，HTTP API 的 `ApiError` 只序列化最外层 `import_job_status=`，遮蔽了真实 `ImportErrorCode` 与错误说明。
- 修补内容：
  - `crates/center-backend/src/import_runtime.rs`：新增导入失败消息构造 helper，将尚未开始的进度状态归一为 `PENDING`，并在同一层保留真实 `<IMPORT_ERROR_CODE>: <message>`。
  - 新增 `import_job_status` 语义校验 helper，明确任务状态只允许 `PENDING`、`IMPORTING`、`DONE`、`FAILED`、`CANCELLED`；盘生命周期 `INITIALIZED`、`SEALED`、`IMPORTED` 不属于导入任务状态。
- 安全边界：
  - `POST /api/center/import-jobs/start` 请求 DTO 不新增字段，仍只要求 `mount_path`；既有路由测试覆盖缺省 `import_job_status` 可正常进入导入服务。
  - repo/DB 对非法导入任务状态的拒绝逻辑未放宽；本修补只避免空状态遮蔽真实错误。
- 验证结果：
  - `cargo fmt --all -- --check`：通过。
  - `cargo test -p rustfs-transfer-common`：通过。
  - `cargo test -p rustfs-transfer-center`：通过。
  - 首次红灯：新增 `import_runtime::tests` 在 helper 缺失时编译失败；实现 helper 后同一测试转绿。
- 后续真实环境建议：需要仅部署 Center 后端后再受控重试导入。该修补会消除 `import_job_status=` 空错误；若真实导入仍失败，应能返回底层导入错误码/消息以继续定位。

### Center import observability hotfix deploy and controlled retry

- Scope: deployed only the Center backend import error-observability hotfix. Did not deploy Edge/frontend, did not run cleanup/reinitialize, did not touch source objects, unrelated archive/object_ledger, unrelated disks, or perform manual SQL.
- Build/deploy: built Center release from clean `HEAD` plus only `crates/center-backend/src/import_runtime.rs` override; verified both the import error helper and existing center_signature API markers in the remote temporary source tree before installing. Existing Center binary was backed up, replaced, and `rustfs-transfer-center` was restarted.
- Service validation after restart: Center service active; `/healthz=200`; `/readyz=200`; recent log scan found no new business panic/error/failed markers, only the pre-existing truncated journal warning.
- Pre-import gate: target disk visible and mounted on Center as ext4; lifecycle `SEALED`; `.partial=0`; manifest/payload counts consistent: `object_count=59`, `total_bytes=1428414961`, `data_files=59`, `meta_files=59`, `manifest_files=2`.
- API retry: called existing `POST /api/center/import-jobs/start` once with the real mount path and restricted Center control token. Token was not logged.
- API result: HTTP 400, `error_code=INVALID_REQUEST`, now with the true underlying message `import_job_status=PENDING; MANIFEST_INVALID: failed to parse disk_info.json: missing field name at line 28 column 3`. This confirms the observability hotfix worked and the remaining blocker is disk_info shape/compatibility, not a client-supplied import_job_status field.
- Post-failure readonly check: disk lifecycle remained `SEALED`; `.partial=0`; Center service remained active/healthy. No destructive retry or workaround was attempted.
- Local hygiene: temporary local helper script was removed from the workspace after use.

### Edge 自动流程上线要求文档整理

- 范围：仅更新 `docs/上线要求.md` 和本日志；未修改冻结文档，未改代码，未操作 VM、硬盘、数据库、RustFS 源对象、archive 或 object_ledger。
- 内容：整理 Edge 端插入硬盘后的完整自动流程，明确服务启动扫描、udev rescan、硬盘准入、Center HTTP 校验、自动 RustFS 扫描、自动创建/启动导出、DiskWorker 写盘封盘、前端即时视觉反馈和全流程状态展示要求。
- 联调契约：补充 Edge Dashboard 只读 HTTP、Edge WebSocket 事件、状态字段命名、页面刷新恢复、多盘 `disks[]` 独立进度、异常错误码展示和危险操作入口限制。
- 验证方式：文档自查，确认未引入 Edge 端初始化/清理/格式化/导入职责，未要求 WebSocket 承担 Center/Edge 数据同步，未使用裸 `status` 作为 API/WS 字段命名。
- 待跟进：如后续实现自动触发 scan/export，需要按本文清单做真实 Edge 前后端联调，并验证插盘 1 秒内 Dashboard 可见反馈。

### Edge 实时可视化上线剩余工作并行排期

- 范围：仅更新 `work_sprint/TASKS.md` 和本日志；未修改冻结文档，未改代码，未操作 VM、硬盘、数据库、RustFS 源对象、archive 或 object_ledger。
- 架构判断：Edge 核心受控导出能力已具备，真实 VM 已跑通过受控 scan/export/seal；距离“插盘即自动流转 + 前端实时可视化上线”主要缺口是自动编排、插盘/扫描阶段 WS 事件、前端生产态收口和端到端浏览器验收。
- 新增并行任务：
  - `TASK-S1-EDGE-AUTO-001`：Edge 自动编排，串起 READY -> scan -> create export_job -> start DiskWorker。
  - `TASK-S1-EDGE-WS-BOOT-001`：插盘、校验、扫描、导出、封盘全阶段 WebSocket 事件补齐。
  - `TASK-S1-WEB-EDGE-PROD-001`：Edge Dashboard 去预览兜底、去浏览器写操作入口、真实空态/错误态。
  - `TASK-S1-WEB-EDGE-TIMELINE-001`：插盘即时反馈、每盘时间线、扫描态和可拔盘展示。
  - `TASK-S1-EDGE-AUTO-DEPLOY-001`：自动流程配置开关、部署检查和回滚说明。
  - `TASK-S1-EDGE-REALTIME-QA-001`：真实 Edge 前后端实时可视化端到端验收。
- 并行原则：后端自动编排只调用既有受控能力，不复制扫描/Planner/DiskWorker；前端只读展示，不接触控制 token 或密钥；udev 仍只触发 rescan，不直接执行业务导出。
- 验证方式：文档结构自查，确认新增任务已同步写入任务总览和底部任务卡；未标记完成，待各代理领取后按卡片验收。

### Edge RustFS 一天一次扫描与完整对象统计口径

- 范围：更新 Edge 后端扫描门禁、导出 Planner、Edge SQL 迁移、`docs/上线要求.md`、`work_sprint/TASKS.md` 和本日志；未修改冻结文档，未操作 VM、硬盘、数据库、RustFS 源对象、archive 或 object_ledger。
- 用户口径：RustFS 全量扫描条件设置为一天一次；只记录完整文件，RustFS 正在上传或扫描期间变化的文件不统计，只统计上传完成的对象。
- 实现内容：
  - 新增 `sql/edge/0003_scan_run.sql`，记录 Edge 每次 RustFS 扫描运行结果；只有 `scan_status = DONE` 的扫描作为 24 小时复用依据。
  - `crates/edge-backend/src/control.rs`：`scan_once` / `create_export_job(run_scan=true)` 进入扫描前先查询最近 24 小时成功扫描；存在则复用扫描结果和快照，不再重复全量扫描 RustFS。
  - `crates/edge-backend/src/export_planner.rs`：导出计划只读取最近一次成功扫描窗口内 `stable_status = STABLE` 的快照，避免历史旧快照或不稳定对象进入导出队列。
  - `crates/edge-backend/src/scanner/progress.rs`：复用当天扫描时更新扫描进度快照为 `SCAN_DONE`，message 说明复用 24 小时内扫描结果。
  - 当前稳定性判断仍按 List + 前后两次 HEAD 的 bucket/key/etag/size/last_modified 一致性判断；不一致对象记录为 `SOURCE_CHANGED`，不计入稳定对象统计和导出队列。
- 验证结果：
  - `cargo fmt --all -- --check`：通过。
  - `cargo test -p rustfs-transfer-edge`：普通 target 因 Windows 构建锁权限拒绝；使用临时 `CARGO_TARGET_DIR=target-codex-edge-scan` 重跑通过，52 个库测试、1 个 DiskWorker 集成测试、2 个 scanner 测试全部通过。
- 待跟进：尚未在空 PostgreSQL Edge 库实际执行 `sql/edge/` 迁移；后续联调前需按 SQL edge 验证矩阵跑迁移并确认 `edge_scan_run` 表与索引存在。
### TASK-S1-WEB-EDGE-001：Dashboard 运输设备素材替换

- 范围：仅 Edge 前端 Dashboard 运输设备素材与盘位覆盖层样式；未修改后端、冻结文档、路由范围、控制接口或运输盘流程。
- 内容：将 Dashboard 右侧运输设备图替换为 `web/edge-web/public/assets/fustfs-baseline/transport-bay-empty-slots-alpha.png`，并按新 4x4 机箱素材调整设备定位、尺寸、盘位矩阵 inset、密度和选中态透明度，避免旧素材比例导致盘位层级不贴合。
- 验证：当前预览工作树已确认新素材可通过 Vite 静态路径访问；Edge Web `pnpm typecheck` 通过，`pnpm build` 在非沙箱环境通过。
- 待跟进：仍需 UI 复审确认新素材与目标图的最终视觉贴合度；本次未处理粒子效果。

### TASK-S1-CENTER-HOTFIX-007：Center 初始化与重新初始化 disk_info 生成同源修补

- 范围：仅本地 Center 后端代码、测试、TASKS 与本日志记录；未部署 Center/Edge VM，未调用真实 API，未操作运输盘、数据库、RustFS 源对象、archive、object_ledger 或其他盘，未修改冻结文档。
- 根因：Center 注册初始化路径在 `lib.rs` 内生成完整 `disk_info.json`，包含 `protocol.name`、`disk.last_init_time`、`disk.initialized_by` 和完整空 `edge/manifest` 对象；导入后 `reinitializer.rs` 另有一套窄版 clone/改字段逻辑，重新初始化后会写出缺少冻结协议必填字段的 `disk_info.json`，导致后续 Center import 按完整协议解析时报 `missing field name`。
- 修补内容：
  - 新增 `crates/center-backend/src/disk_info_document.rs`，集中生成 `INITIALIZED` disk_info 的完整协议结构、center_signature 与原子写盘布局。
  - `CenterService::initialize_disk` 与 `PostImportReinitializer` 成功写新 `INITIALIZED` 盘时共用同一构造器，不再各自拼 JSON。
  - reinitialize 读取 DTO 补齐 `protocol.name`、`disk.last_init_time`、`disk.initialized_by`、完整 `edge/manifest` 字段；`seal_id/export_job_id` 保持盘内字符串形态，只在准入时解析 UUID，避免 DTO 重序列化改变签名 canonical 输入。
  - 初始化测试挂载目录从工作区 `target/test-disks` 移到系统临时目录，避免 Windows 目标目录 ACL/句柄干扰本地验证。
- 安全边界：
  - `center_signature` 覆盖范围未放宽，仍由 Common 签名 API 计算；没有跳过验签。
  - Center import DTO 未放宽必填字段；修补归属在错误生成路径，而不是让导入接受坏协议文件。
- 验证结果：
  - `cargo fmt --all -- --check`：通过。
  - `cargo test -p rustfs-transfer-common`：通过。
  - `cargo test -p rustfs-transfer-center`：通过。
  - 首次红灯：重序列化 DTO 将空 UUID 字符串变为 `null`、缺少 `disk.last_init_time/initialized_by` 时验签失败；改为协议字符串字段并补齐签名覆盖字段后转绿。
- 后续真实环境建议：需要仅部署 Center 后端后，对当前已通过门禁的 SEALED/IMPORTED 流程按主窗口授权继续受控导入或重试清理/重新初始化；部署前仍需重新做只读盘身份、生命周期、`.partial=0` 和服务健康门禁。

### Center disk_info 同源修补部署与受控导入重试

- 范围：仅部署 Center 后端本次 `disk_info.json` 同源生成/签名/写盘修补，并对当前已封盘目标盘做一次受控 import 重试。未部署 Edge/frontend，未执行 cleanup/reinitialize，未手工 SQL，未触碰源对象、无关 archive/object_ledger、其他盘或冻结文档。
- 部署：在 Center VM 临时目录构建 release 二进制，备份现有 `/opt/rustfs-transfer/rustfs-transfer-center` 后原子替换并重启 `rustfs-transfer-center`。
- 服务验收：Center 服务 active；`/healthz=200`；`/readyz=200`。
- 只读门禁：当前目标为唯一非系统 ext4 协议运输盘；盘内生命周期 `SEALED`；`.partial=0`；manifest 与 disk_info 计数自洽，协议 data/meta 文件计数与 manifest 对象数一致。
- API 重试：使用受限 Center 控制 token 调用既有 `POST /api/center/import-jobs/start` 一次；token 未记录。
- 结果：HTTP 400，`error_code=INVALID_REQUEST`，底层错误为 `MANIFEST_INVALID: failed to parse disk_info.json: missing field name`。该错误发生在创建导入任务前，未进入对象归档或账本写入阶段。
- 后验：盘内生命周期仍为 `SEALED`；`.partial=0`；Center `/healthz` 与 `/readyz` 仍正常。Center 主机缺少 `psql`，未安装工具，未直接读取 DB 业务表。
- 待跟进：当前盘是旧 reinitialize 产物，盘内仍缺冻结协议必填字段；本次修补只能保证后续重新初始化生成的新盘结构正确，不能让旧坏结构自动变成可导入结构。下一步需主窗口决定是否走受控 cleanup/reinitialize 或专门的旧盘兼容/修复路径。

### Center SEALED 盘放弃数据后重新初始化门禁停止

- 范围：仅 Center 只读门禁与状态机检查。未调用 import/reinitialize/cleanup 控制 API，未手工删除或修改盘内文件，未写 DB，未触碰源对象、无关 archive/object_ledger、其他盘或冻结文档。
- 用户口径：用户授权放弃当前唯一目标盘上的旧 `SEALED` 封盘数据，并选择重新初始化后重新扫描导出。
- 只读门禁：Center 当前可见 1 块非系统 ext4 协议候选盘；盘内生命周期 `SEALED`；`.partial=0`；协议文件可见；manifest/data/meta 计数自洽；Center `/healthz` 与 `/readyz` 正常。
- 状态机检查：现有受控 `POST /api/center/disks/{disk_id}/reinitialize` 入口在请求层硬性要求 `expected_status_code=IMPORTED`，前置检查也要求盘内 `disk_status_code=IMPORTED`；当前 `SEALED` 不属于合法入口。
- 决策：按安全边界停止，未绕过状态机，未执行手工清理、SQL 或重新初始化。若要支持“放弃 SEALED 封盘数据并重置”，需要新增或调整受控 Center 实现，明确审计原因、状态转移、密钥生命周期和失败原子性后再部署重试。

### TASK-S1-CENTER-HOTFIX-008：Center 受控丢弃 SEALED 测试导出并重置

- 开始：领取 P0 修补任务，仅本地代码/测试/TASKS/dev log；不部署 VM，不调用真实 API，不操作运输盘、数据库、RustFS 源对象、archive、object_ledger 或其他盘，不修改冻结文档。
- 目标：新增受控 `SEALED` 测试导出丢弃重置分支，要求显式确认、严格只读门禁和失败原子性；普通 `IMPORTED` cleanup/reinitialize 语义保持不变。
- 完成内容：
  - `crates/center-backend/src/reinitialize_runtime.rs`：`CenterReinitializeRequest` 新增默认 false 的 `confirm_discard_sealed_export`；旧 `IMPORTED` 路径保持原门禁，新 `SEALED` 路径必须同时满足 `expected_status_code=SEALED`、`confirm_reinitialize=true` 和 `confirm_discard_sealed_export=true`。
  - 新增 `SEALED` 分支 preflight：ext4、disk_id/seal_id、Center HMAC 签名、`.partial=0`、生命周期 flags、manifest sha256 sidecar、manifest identity/counter、data/meta 文件存在性均通过后才进入写入。
  - `crates/center-backend/src/reinitializer.rs`：新增 `discard_sealed_export_and_reinitialize_from_document`，复用同一 `INITIALIZED` disk_info 构造与 key rotation；`SEALED` 分支先移动 payload 到临时备份，失败时恢复 payload 和 `SEALED` disk_info 边界，成功后丢弃备份。
  - 为历史坏 `SEALED` 测试盘补只读兼容：若 raw `disk_info.json` 缺少 `protocol.name`，仅在内存解析副本补协议名；HMAC 仍验证原始 raw JSON，成功重置后写回完整新结构。
- 安全边界：未新增或修改冻结协议字段；未触碰源对象、archive、object_ledger 或无关 SQL；普通 `IMPORTED` cleanup/reinitialize 仍不要求 `confirm_discard_sealed_export`，且该字段在 `IMPORTED` 路径为 true 时会被拒绝。
- 验证结果：
  - `cargo fmt --all -- --check`：通过。
  - `cargo test -p rustfs-transfer-common`：默认 target 因 Windows `.cargo-build-lock` 权限拒绝，改用 C: 临时 `CARGO_TARGET_DIR` 后通过。
  - `cargo test -p rustfs-transfer-center`：默认 target 因 Windows `.cargo-build-lock` 权限拒绝，改用 C: 临时 `CARGO_TARGET_DIR` 后通过；覆盖 `SEALED` 缺少显式确认拒绝、manifest 自洽门禁、历史缺 `protocol.name` raw 签名兼容、成功重置、key 激活失败恢复 payload 并保持 `SEALED`。
- 部署状态：本轮未部署 Center/Edge VM，未调用真实 API，等待主会话批准后才能部署并触碰当前物理盘。

### Center 受控 SEALED 测试盘丢弃重置真实执行

- 范围：仅 Center VM 后端部署与当前唯一测试运输盘受控 `SEALED` 丢弃重置。未部署 Edge/frontend，未调用导入、扫描或导出，未手工删除盘文件，未手工 SQL，未触碰源对象、其他盘、无关 archive/object_ledger 或冻结文档。
- 部署：使用干净 `HEAD` 源码包并仅覆盖 Center 已验证修补文件，在 Center `/tmp` 构建 release；备份现有 Center 二进制后替换并重启。重启后 `/healthz` 与 `/readyz` 正常。
- 写入前只读门禁：唯一协议候选盘；ext4；生命周期 `SEALED`；`.partial=0`；协议文件可见；manifest sidecar、manifest identity/counter 与 data/meta 文件存在性自洽。
- API 动作：仅调用一次受控 reinitialize 接口，`expected_status_code=SEALED`，并显式确认丢弃已封盘测试导出；控制 token 未记录。
- API 结果：HTTP 200，返回 `disk_status_code=INITIALIZED`、`runtime_status=DONE`，新旧 data_key 引用均只确认存在，不记录值。
- 后验：盘内生命周期 `INITIALIZED`；`sealed=false`、`imported=false`、`reusable=true`；`.partial=0`；协议 `data/meta/manifests` 文件计数均为 0；manifest 计数为 0；edge seal/export 字段已清空；center_signature 与新 data_key 引用存在；Center `/healthz` 与 `/readyz` 正常。
- 下一步：等待用户将已初始化盘物理切回 Edge，再由 Edge 窗口重做只读门禁后进入下一轮扫描/导出。

### TASK-S1-WEB-EDGE-PROD-001：Dashboard 左侧 Edge 名称彻底去重

- 触发问题：用户截图确认左侧源服务器框仍显示 `Edge 节点：edge-demo`，要求把节点名移动到顶部标题右侧同一行展示，避免多次展示 Edge 名称。
- 完成内容：
  - 顶部 header 接收真实 summary 的 `edge_name`/`edge_code`，作为 Edge 节点名唯一展示位置。
  - 左侧源服务器框删除重复节点名，仅保留 `源服务器（RustFS） · Edge` 与 `同步记录`入口。
  - `同步记录`按钮改为本地路由跳转 `/sync-records`，不触发后端控制接口。
  - 不改后端、不改冻结文档、不改 HTTP/WS 数据接线。
- 验证：
  - `pnpm test:unit`：通过，4 个用例通过。
  - `pnpm typecheck`：通过。
  - `pnpm build`：通过；沙箱内 Vite 写 `.vite-temp` 被拒，按权限规则使用非沙箱同命令验证通过。
  - `rg` 复核：`DashboardView.vue` 中已无 `Edge 节点`、`只读同步通道`、`查看源端详情` 文案。

### TASK-S1-WEB-EDGE-PROD-001：同步记录页布局与统计卡收口

- 触发问题：用户截图指出同步记录页返回按钮位置不对、统计卡点击会影响其他卡片信息、列表未撑满、详情抽屉与列表高度不一致、底部提示条需要移除。
- 完成内容：
  - 返回 Dashboard 入口移动到标题行右侧，和 `同步记录` 标题水平对齐。
  - 统计卡改为只读总览卡，不再作为筛选入口；列表筛选只由下方分段控件控制。
  - 统计卡数字改为独立只读统计查询，避免切换某个状态筛选后其他卡片数字被当前列表结果洗成 0。
  - 列表底部扩展到首屏下边距，移除底部 `records-note` 提示条。
  - 详情抽屉 top/bottom 与左侧列表对齐，避免左右高度错位。
  - 补齐 `export_job_status=SEALING` 前端类型白名单，用于进行中统计。
- 验证：
  - `pnpm test:unit`：通过，4 个用例通过。
  - `pnpm typecheck`：通过。
  - `pnpm build`：通过；沙箱内 Vite 写 `.vite-temp` 被拒，按权限规则使用非沙箱同命令验证通过。
  - `rg` 复核：`SyncRecordsView.vue` 模板中已无 `records-note`；统计卡为 `summary-card`，筛选点击只保留在分段筛选控件。

### TASK-S1-WEB-EDGE-PROD-001：同步记录统计与详情默认态修正

- 触发问题：用户截图显示列表已有 3 条 `SEALED` 记录，但顶部统计卡仍显示 0；右侧详情面板仍有关闭按钮，且未默认展示列表第一条。
- 根因判断：
  - 当前后端 `GET /api/edge/dashboard/export-jobs?page=1&page_size=1&export_job_status=SEALED` 返回 `total_count=3`，统计为 0 是前端统计聚合问题。
  - 统计逻辑此前使用 `Promise.all`，任一状态统计请求失败都会把整组统计清 0；应改为逐项容错。
- 完成内容：
  - 统计查询改为 `Promise.allSettled`，单个状态统计失败不影响其他卡片。
  - 当前列表已经包含全部记录时，用列表本身兜底计算统计；当前实测应显示 `全部 3 / 进行中 0 / 已封盘 3 / 失败 0`。
  - 记录列表加载成功后自动打开第一条详情；无记录时详情面板显示 `暂无导出记录`。
  - 删除详情面板关闭按钮，详情作为页面固定区域存在。
  - 删除未使用的 `previewRecords` 死代码，记录页只使用真实只读接口。
- 验证：
  - `pnpm test:unit`：通过，4 个用例通过。
  - `pnpm typecheck`：通过。
  - `pnpm build`：通过；沙箱内 Vite 写 `.vite-temp` 被拒，按权限规则使用非沙箱同命令验证通过。
  - 只读接口核验：`/api/edge/dashboard/export-jobs?page=1&page_size=8` 返回 `total_count=3`；`export_job_status=SEALED&page_size=1` 返回 `total_count=3`。
  - `rg` 复核：`SyncRecordsView.vue` 中已无 `records-note`、`关闭详情`、`selectedId = ''`、`previewRecords`。

### TASK-S1-WEB-EDGE-PROD-001：同步记录筛选按钮文字对齐

- 触发问题：用户截图指出同步记录页分段筛选按钮中 `进行中`、`已封盘` 中文换行后基线错位。
- 完成内容：
  - `.segmented-filter button` 改为 inline-flex 居中，固定单行显示，设置 `line-height: 1` 与 `white-space: nowrap`。
  - 筛选控件宽度从 330px 放宽到 360px，避免中文标签被挤压换行。
- 验证：
  - `pnpm test:unit`：通过，4 个用例通过。
  - `pnpm typecheck`：通过。
  - `pnpm build`：通过；沙箱内 Vite 写 `.vite-temp` 被拒，按权限规则使用非沙箱同命令验证通过。

### Edge controlled scan/export real execution

- Scope: Edge VM only, current initialized test transport disk only. Used deployed Edge controlled APIs for scan, export job creation, start, and polling. Did not manually write transport-disk files, did not run SQL, did not modify RustFS source objects, Center, archive, object_ledger, unrelated disks, code, or frozen docs.
- Readonly gate before writes: exactly one protocol candidate was visible on Edge; ext4; lifecycle `INITIALIZED`; `.partial=0`; protocol payload counts were empty; `/healthz` and `/readyz` returned 200.
- Scan: `POST /api/edge/scan` returned `DONE`; buckets=3, objects_seen=59, stable_objects=59, source_changed=0, total_bytes=1428414961.
- Export: created one controlled export job and started it through `/api/edge/export-jobs/{id}/start`; worker_started=1, worker_failed=0; assigned/copied objects=59, bytes=1428414961; job reached `SEALED`.
- Postcheck: disk lifecycle `SEALED`; `.partial=0`; data files=59, meta files=59; one JSON manifest plus one hash sidecar; manifest object count=59 and bytes=1428414961; sidecar hash matched; Edge `/healthz` and `/readyz` returned 200; recent Edge journal scan found no related panic/error/dispatch-failure/CredentialsNotLoaded markers.
- Next step: wait for user to physically move the sealed disk back to Center, then run Center readonly import gate and controlled import only after authorization.

### Center controlled import and post-import reinitialize real execution

- Scope: Center VM only, current sealed test transport disk only. Used deployed Center controlled APIs for import and post-import cleanup/reinitialize. Did not manually write disk files, did not run SQL, did not modify RustFS source objects, Edge, unrelated archive/object_ledger, unrelated disks, code, or frozen docs.
- Readonly import gate: exactly one protocol candidate was visible on Center; ext4; lifecycle `SEALED`; `.partial=0`; center signature verification passed; manifest JSON, manifest SHA sidecar, disk_info manifest counters, data paths, and meta paths were self-consistent. Center `/healthz` and `/readyz` returned 200.
- Import: `POST /api/center/import-jobs/start` returned HTTP 200 with `import_job_status=DONE`, outcome `IMPORTED`, objects=59/59, bytes=1428414961/1428414961, no import error code. Post-import disk lifecycle was `IMPORTED`; `.partial=0`; Center health/ready stayed 200.
- Cleanup/reinitialize: pre-reinitialize gate confirmed lifecycle `IMPORTED` and `.partial=0`; controlled `POST /api/center/disks/{id}/reinitialize` returned HTTP 200 with `disk_status_code=INITIALIZED` and `runtime_status=DONE`; old and new key references were present but not logged.
- Final postcheck: disk lifecycle `INITIALIZED`; `.partial=0`; center signature verification passed; new data_key reference present; protocol data/meta/manifests file counts were all 0; Center health/ready returned 200; recent Center journal scan found no related panic/error/signature/manifest/decrypt/checksum/reinit failure markers.
- Next step: disk is initialized and reusable for the next Edge cycle after user performs any required physical move.

### TASK-S1-EDGE-AUTO-001: Edge 自动导出编排领取

- 开始：Codex / Agent D3 领取 P0 任务 `TASK-S1-EDGE-AUTO-001`，同步 `work_sprint/TASKS.md` 顶部总览与任务卡状态为开发中。
- 范围：仅 Edge 后端自动编排模块、配置默认值、最小测试、TASKS 和本日志；不改 WS 契约、前端时间线、部署脚本、真实物理盘、数据库数据、RustFS 源对象或冻结文档。
- 目标：Edge 常驻服务在运输盘刷新后按开关执行 `READY -> scan -> create export_job -> start DiskWorker`，复用既有 scanner/export planner/control/DiskWorker 路径，udev 仍只触发设备变化识别与重扫。

### TASK-S1-EDGE-AUTO-001: Edge 自动导出编排完成

- 完成内容：
  - 新增 `crates/edge-backend/src/auto_export.rs`，实现 `AutoExportOrchestrator` 和 `AutoExportRescanRunner`；运输盘 rescan 成功后由 Edge 常驻服务按开关判断是否自动执行扫描、建导出任务和启动 DiskWorker。
  - `auto_export.enabled` 与 `auto_export.start_on_ready` 默认均为 false；只有两者同时打开才会自动流转，`min_ready_disk_count` 默认 1，`cooldown_seconds` 默认 60。
  - 自动编排只通过既有 `EdgeControlService` 调用 `summary`、`export_jobs`、`scan_once`、`create_export_job`、`start_export_job`；不复制 scanner、Planner、DiskWorker 或写盘路径。
  - 门禁覆盖 `runtime_status=READY`、`disk_status_code=INITIALIZED`、挂载路径存在、既有活动导出任务阻塞、扫描结果无稳定完整对象不建任务；拒绝和失败结果均输出结构化 tracing 日志。
  - `server.rs` 只在 rescan runner 外包一层自动编排回调；udev 仍只触发设备变化识别/重扫，不直接执行业务导出；受控 API 前置刷新使用内部 `control_refresh` trigger，只刷新盘态、不触发自动导出。
- 边界确认：未修改 WS 契约、前端时间线、部署脚本、真实物理盘、数据库数据、RustFS 源对象、冻结文档或 Center 代码路径。
- 验证结果：
  - `cargo fmt --all -- --check`：通过。
  - `cargo test -p rustfs-transfer-edge`：默认 `target` 因 Windows `.cargo-build-lock` 权限拒绝；改用 `CARGO_TARGET_DIR=target-codex-edge-auto` 且 `-j1` 后通过，58 个库测试、1 个 DiskWorker 集成测试、2 个 scanner 测试全部通过。
  - 新增自动编排单测覆盖默认关闭、无 INITIALIZED READY 盘拒绝、活动导出任务阻塞、无稳定完整对象不建任务、READY 后串起 scan/create/start、受控 API 前置刷新不触发自动导出。
- 状态：已同步 `work_sprint/TASKS.md` 顶部总览和任务卡为 `[x] 开发完成`。

### TASK-S1-WEB-EDGE-PROD-001: Dashboard removed-disk stale WS guard

- Trigger: user screenshot showed one COPYING disk on `/dashboard` after the Edge VM transport disk had been unplugged.
- Finding: live `GET /api/edge/dashboard/summary` returned `disks: []` with latest export job already terminal (`SEALED`), so the visible disk was not coming from the HTTP summary.
- Root cause: the runtime frontend still allowed stale non-terminal WebSocket progress snapshots to repopulate the disk list after HTTP refresh; the runtime Dashboard copy also still had older preview/recover code before this pass.
- Change:
  - Synchronized the production Dashboard data path to use an empty real summary instead of preview disks.
  - Removed the browser recover POST path from the runtime Dashboard copy.
  - Added a WS guard: when HTTP summary is terminal and `disks[]` is empty, old non-terminal WS snapshots cannot resurrect removed disks.
  - Kept the intended rule that unregistered/abnormal disks are displayed only when the Edge backend actually returns them in `summary.disks[]` or live WS events.
- Verification:
  - `pnpm test:unit` passed, including the new stale-WS regression test.
  - `pnpm typecheck` passed.
  - `pnpm build` passed after rerunning outside the sandbox for Vite temp-file access.
- Follow-up: backend should continue to return currently detected unregistered/abnormal disks in read-only `summary.disks[]`; if it returns an empty array after unplug, the frontend will now show a true empty state rather than stale disks.

### TASK-S1-WEB-EDGE-PROD-001: Dashboard red-box lower module visual realignment

- Scope: only the `/dashboard` lower red-box area requested by the updated design screenshot. Did not change backend, frozen docs, routes, WS fields, proxy, disk flow, or any browser write/control API.
- Change:
  - Replaced the visible lower Dashboard structure with a selected-disk detail strip, full-width global export progress band, and a two-column lower area.
  - Moved disk hardware/runtime fields into the selected-disk strip: `disk_id`, `mount_path`, `disk_sn`, `filesystem`, `runtime_status`, `disk_status_code`, `object_budget_bytes`, and `free_bytes`.
  - Reworked scan/export overview into a 3x2 metric panel and moved warning totals into the right-side "current object and exception handling" panel.
  - Kept the recovery-check visual button disabled with no event handler or network request; browser remains read-only and carries no control token.
  - Kept real-data behavior: if Edge backend returns `disks: []`, the UI shows empty/idle values and does not recreate fake disks.
- Verification:
  - `pnpm test:unit` passed: 11 tests.
  - `pnpm typecheck` passed.
  - `pnpm build` passed after rerunning outside the sandbox for Vite `.vite-temp` file access.
- Follow-up: waiting for user/independent UI review of the updated lower module against the new red-box design; particle/data-flow area was not part of this change.

### TASK-S1-WEB-EDGE-PROD-001: Local Edge web dev proxy config

- Trigger: user requested generating a local frontend config file and stopping the Codex-started frontend dev server.
- Scope: Edge frontend local dev configuration only. Did not change backend, frozen docs, routes, DTO/WS logic, disk flow, or browser write/control behavior.
- Change:
  - Added ignored `web/edge-web/.env.local` with `VITE_EDGE_BACKEND_ORIGIN` and browser-facing readonly route paths.
  - The file contains no Center URL, authorization key, control token, or secret material.
  - Stopped the local Vite dev server that was listening on `127.0.0.1:5173`.
- Verification:
  - Confirmed `5173` no longer has a `LISTENING` socket after stopping PID `30616`.
  - Confirmed `http://127.0.0.1:5173/dashboard` no longer responds.
  - `.env.local` is covered by repository ignore rules.

### TASK-S1-WEB-EDGE-PROD-001: Local Edge web proxy live connection

- Trigger: user requested starting the local Edge frontend and connecting it to the Edge backend interface.
- Scope: Edge frontend local dev server and Vite proxy config only. Did not change backend, frozen docs, routes, disk flow, or browser write/control behavior.
- Change:
  - Updated `vite.config.ts` to load `.env.local` through Vite `loadEnv`, so `VITE_EDGE_BACKEND_ORIGIN` is honored by the dev proxy.
  - Started the local Vite server from `F:\Project\RustFSDisk\web\edge-web` on `http://127.0.0.1:5173/`.
- Verification:
  - `http://127.0.0.1:5173/dashboard` returned 200.
  - `http://127.0.0.1:5173/readyz` proxied to Edge backend and returned 200.
  - `http://127.0.0.1:5173/api/edge/dashboard/summary` proxied to Edge backend and returned 200 with Edge JSON.
  - `ws://127.0.0.1:5173/ws/edge/progress` opened successfully.

### TASK-S1-EDGE-AUTO-001: 续办审计与重复触发补测

- 范围：仅续办审计 Edge 自动编排当前真实状态，并补一条最小回归测试、同步任务卡验收勾选；未改 WS 契约、前端、部署、冻结文档，未操作真实硬盘、数据库或 RustFS 源对象。
- 审计结论：当前分支已有 `AutoExportOrchestrator` 和 `AutoExportRescanRunner`，默认配置关闭，启用后由 rescan 完成回调进入 `READY + INITIALIZED` 门禁，再复用 `EdgeControlService::scan_once`、`create_export_job(run_scan=false)`、`start_export_job`，没有复制扫描、Planner、DiskWorker 或第二套写盘路径。`ControlRefresh` rescan 不触发自动导出，udev/rescan 仍只负责设备变化识别和重扫。
- 可观察门禁：非 `INITIALIZED` 生命周期、非 `READY` 运行态、缺挂载路径、既有活动导出任务、无稳定对象、scan/create/start 失败均以结构化 `AutoExportOutcome` 和 tracing 日志表达；Center verify/export-key 仍由既有受控导出启动和 DiskWorker 路径执行，不在自动编排里绕过。
- 补测内容：新增 `cooldown_blocks_repeated_udev_events_before_scan_or_export`，确认连续 udev 重复触发时第二次停在 cooldown，不再重复进入 scan/create/start。
- 状态同步：`work_sprint/TASKS.md` 顶部总览与任务卡均保持 `[x]`，并把本任务验收清单同步勾选为已完成。
- 验证结果：
  - `cargo fmt --all -- --check`：通过。
  - `cargo test -p rustfs-transfer-edge`：默认 `target/debug/.cargo-build-lock` 权限拒绝未启动；改用 `CARGO_TARGET_DIR=target-codex-edge-auto` 且 `-j1` 后通过，59 个库测试、1 个 DiskWorker 集成测试、2 个 scanner 测试全部通过。
  - 任务记录一致性检查：`work_sprint/TASKS.md` 顶部总览 `TASK-S1-EDGE-AUTO-001` 为 `[x]`，底部任务卡为 `[x] 开发完成`，本任务验收清单均为 `[x]`。
- 待覆盖项：本轮只做本地代码审计和测试，未部署 Edge VM，未真实插盘验证自动流程；部署开关、udev 静态检查和端到端实时可视化仍由 `TASK-S1-EDGE-AUTO-DEPLOY-001` 与 `TASK-S1-EDGE-REALTIME-QA-001` 覆盖。

### TASK-S1-EDGE-WS-BOOT-001: Edge 全阶段 WebSocket 事件补齐领取

- 开始：Codex / Agent D4 领取 P0 任务 `TASK-S1-EDGE-WS-BOOT-001`，同步 `work_sprint/TASKS.md` 顶部总览与任务卡状态为开发中。
- 范围：仅 Edge 后端本端 WebSocket 事件发布和聚焦测试；不改前端时间线、不改部署、不操作真实盘、不修改冻结文档，不通过 WebSocket 做 Edge-Center 通信。
- 目标：由后端权威运行态补齐 `DISK_DETECTED`、`DISK_CHECKING`、`DISK_READY`、`DISK_REJECTED`、`DISK_REMOVED`、`SCAN_STARTED`、`SCAN_PROGRESS`、`SCAN_DONE`、`COPY_STARTED`、`COPY_PROGRESS`、`COPY_DONE`、`SEAL_DONE`、`ERROR`，事件字段继续使用 `disk_status_code`、`runtime_status`、`export_job_status`、`object_status` 等语义字段，兼容页面刷新后的 HTTP summary。

### TASK-S1-EDGE-WS-BOOT-001: Edge 全阶段 WebSocket 事件补齐完成

- 完成内容：
  - 新增 Edge 本地 `EdgeRealtimeHub`，DiskDetector 在运输盘识别/准入校验/最终运行态变化时发布 `DISK_DETECTED`、`DISK_CHECKING`、`DISK_READY`、`DISK_REJECTED`、`ERROR`；`REMOVED` 运行态映射为 `DISK_REMOVED`，供后续真实 removed record 复用。
  - `/ws/edge/copy-progress` 与 `/ws/edge/progress` 仍是本端 Edge 后端到本端前端的单一事件流；发送优先级为 copy progress、scan progress、disk runtime、idle，未引入 Edge-Center WebSocket。
  - 扫描事件直接复用内存 scanner progress snapshot，覆盖 `SCAN_STARTED`、`SCAN_PROGRESS`、`SCAN_DONE`、`ERROR`，不每秒查数据库聚合。
  - 导出/封盘事件继续复用内存 `ProgressAggregator`，补齐 `COPY_STARTED`、`COPY_DONE`、`SEAL_DONE`；`COPY_PROGRESS` 高频字节仍来自内存聚合器。
  - 事件 payload 继续使用 `disk_status_code`、`runtime_status`、`export_job_status`、`object_status` 等语义字段；拒绝/错误事件 message 带标准错误码；未包含控制 token、`disk_data_key`、`edge_auth_secret`、nonce、tag 或 `data_key_id`。
- 边界确认：未修改冻结文档、前端时间线或部署配置；未部署 VM，未操作真实运输盘、数据库数据或 RustFS 源对象；自动导出开关保持默认关闭。
- 验证结果：
  - `cargo fmt --all -- --check`：通过。
  - `cargo test -p rustfs-transfer-edge`：默认 `target/debug/.cargo-build-lock` 权限拒绝未启动；改用 `CARGO_TARGET_DIR=target-codex-edge-auto` 且 `-j1` 后通过，63 个库测试、1 个 DiskWorker 集成测试、2 个 scanner 测试全部通过。
  - 聚焦测试覆盖 DiskDetector `DETECTED -> CHECKING -> READY` 发布序列、disk runtime payload 无裸 `status` 且拒绝事件带 `FILESYSTEM_UNSUPPORTED`、scan event payload 无裸 `status`、copy done/seal done 事件转换。
- 状态同步：`work_sprint/TASKS.md` 顶部总览与底部任务卡均已同步为 `[x]`，验收清单均已勾选。
- 未覆盖项：本轮按任务要求未真实插盘、未部署、未做浏览器时间线端到端观测；真实 1 秒内插盘可见性、removed 事件源和前端时间线展示留给 `TASK-S1-WEB-EDGE-TIMELINE-001` / `TASK-S1-EDGE-REALTIME-QA-001` 联调验收。

### Center Dashboard Product Design：中控前端页面重设计

- 范围：仅 `web/center-web` 前端页面、静态视觉资产复制、Product Design QA 记录与本日志；未修改冻结文档，未修改后端业务状态机，未接入浏览器真实写操作 token。
- 设计目标：按 Edge 参考图风格重做 Center 中控页面，使页面覆盖插盘识别、未注册盘注册初始化入口、SEALED 待导入、导入进度、导入后清理重初始化、异常盘处理、运输盘台账和仅开发环境可见清理工具。
- 完成内容：
  - `DashboardView.vue` 改为深色工业控制台布局：顶部服务状态、机柜/运输盘位首屏、全局导入进度、待导入队列、选中盘操作面板、当前导入对象、开发工具和运输盘台账。
  - `centerDashboard.ts` 补齐 `center_id` / `center_name` 字段映射，继续使用语义字段 `disk_status_code`、`runtime_status`、`disk_enabled`、`import_job_status`。
  - `centerImportProgress.ts` 修复乱码中文和损坏字符串，保持 `/ws/center/progress` 数据流。
  - `style.css` 重写为与 Edge 参考图一致的深色玻璃、蓝色高亮、盘位矩阵和密集运维面板风格。
  - 复用现有 Edge 视觉资产到 `web/center-web/public/assets/fustfs-baseline/`，供 center 页面本地构建使用。
- 安全边界：正式操作按钮当前为页面级审查入口，只展示目标接口和状态语义，不在浏览器中携带或调用 `X-Center-Control-Token`；开发工具仅 `import.meta.env.DEV` 可见。
- 验证：
  - `npm run typecheck`：通过。
  - `npm run build`：通过。
  - 本地预览：`node vite --host 127.0.0.1 --port 4173` 已启动并打开到 Codex 浏览器面板。
  - `rg` 检查 center-web 源码未发现乱码残留；`status` 仅用于 CSS class 或 HTTP Response 原生字段。
- 未覆盖项：当前环境未暴露自动截图/DOM 检查浏览器工具，`design-qa.md` 记录为 `final result: blocked`，需用户在右侧预览面板人工审查视觉细节后再继续微调。
