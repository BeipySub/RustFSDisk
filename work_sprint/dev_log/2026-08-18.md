# 2026-08-18 开发日志

## Edge Dashboard 系统格式展示修复

- 问题：插盘后首个实时事件缺少文件系统类型时，前端把 `filesystem_uuid/fs_uuid` 当作“系统格式”兜底展示，页面刷新或切换磁盘后 HTTP summary 补齐类型才恢复正常。
- 修改：Edge 前端“系统格式”只展示 `filesystem_type/filesystem`，不再使用 `filesystem_uuid/fs_uuid` 作为兜底值。
- 验证：`pnpm test:unit` 通过 28 个 Edge 前端单测；`pnpm typecheck` 通过；`npm run build` 通过。
