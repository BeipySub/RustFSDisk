# 2026-08-10 开发任务记录

## 串行合入：P0 热插拔扫描通知与 Edge 扫描导出触发入口

- 开始前已读取 `AGENTS.md`、`work_sprint/TASKS.md` 和 `docs/v1.0冻结/` 相关冻结文档。
- 已确认本轮任务涉及边缘端、部署脚本、udev/systemd 通知、RustFS 扫描导出触发入口；不修改冻结文档。
- 待合入顺序：
  1. `C:\Users\Beipy\.codex\worktrees\5874\RustFSDisk` 的 P0 热插拔扫描通知修补。
  2. `C:\Users\Beipy\.codex\worktrees\ee8a\RustFSDisk` 的 P0 Edge 扫描导出触发入口。

## 真实环境：Center 测试盘注册与初始化

- 已按用户明确授权，仅针对 Center `/dev/sdb1`、挂载 `/media/control/FUSTFS-TST-A` 执行测试盘注册与初始化；未触碰 Edge 业务写入，未初始化或清理 `FUSTFS-A-DRILL`。
- 执行前硬门槛只读核对全部通过：
  - 设备：`/dev/sdb1`
  - 文件系统：`ext4`
  - label：`FUSTFS-TST-A`
  - FS UUID：`0878ee5b-3e86-4ae0-8d0f-461c2732ee42`
  - 挂载点：`/media/control/FUSTFS-TST-A`
  - SN：`323533394352343033383235`
  - 容量：`1000202788864`
- Center API 执行结果：
  - `POST /api/disk/register` 成功，生成/确认 `disk_id=9f8308ca-045e-425b-8664-4bb6fdaa0db3`。
  - 首次 `POST /api/disk/initialize` 返回 400，原因是服务用户 `rustfs-transfer` 无法穿越 `/media/control` 且无法写测试盘挂载根。
  - 已只对目标路径设置最小 ACL：`/media/control` 允许 `rustfs-transfer` 穿越，`/media/control/FUSTFS-TST-A` 允许 `rustfs-transfer` 写入。
  - 重试 `POST /api/disk/initialize` 成功，返回 `status_code=INITIALIZED`。
  - `POST /api/disk/verify` 成功，返回 `allowed=true`、`disk_enabled=true`、`expected_status=INITIALIZED`、`action=ALLOW_EXPORT`。
- 只读验收结果：
  - Center `/healthz`、`/readyz` 均为 200。
  - Center DB：`disk_rows=1`，`disk_capacity=1000202788864`。
  - Center DB：该测试盘当前 `data_key_rows=2`，状态为 `ACTIVE,ACTIVE`；其中第一条来自首次初始化失败时在写盘前已插入的残留 key，未泄露 key 明文。
  - 盘内 `/rustfs-transfer/disk_info.json` 存在，非敏感字段为：`status_code=INITIALIZED`、`sealed=false`、`imported=false`、`reusable=true`、`protocol_version=1.0`、`disk_id=9f8308ca-045e-425b-8664-4bb6fdaa0db3`。
  - 盘内协议目录存在：`data/`、`meta/`、`manifests/`、`logs/`、`quarantine/partial/`。
  - Center 服务近 5 分钟日志未发现 `panic`、`fatal`、`permission denied`、`failed`。
- 待跟进：
  - 初始化成功后，可将 `FUSTFS-TST-A` 从 Center 安全移回 Edge，再执行 Edge 只读识别预检。
  - 如主窗口要求清理首次失败留下的未引用 `ACTIVE` data_key，应由编码/DB 操作窗口按冻结密钥生命周期规则评估后处理，不在本次部署窗口擅自修改。

## 真实环境：Edge 接回测试盘只读验收

- 用户将 `FUSTFS-TST-A` 接回 Edge 后，已执行严格只读验收；未写盘、未写 RustFS、未写 DB。
- Edge 当前挂载盘元数据：
  - 受保护盘 `FUSTFS-A-DRILL` 仍在 Edge，设备 `/dev/sdb1`，挂载 `/media/edge/FUSTFS-A-DRILL1`；仅读取块设备元数据，未读取其盘内容。
  - 目标测试盘 `FUSTFS-TST-A` 为唯一三重匹配盘，设备 `/dev/sdc1`，挂载 `/media/edge/FUSTFS-TST-A`，FS UUID `0878ee5b-3e86-4ae0-8d0f-461c2732ee42`，SN `323533394352343033383235`。
- 目标测试盘协议验收：
  - `/rustfs-transfer` 存在。
  - `disk_info.json` 存在，非敏感字段：`disk_id=9f8308ca-045e-425b-8664-4bb6fdaa0db3`、`status_code=INITIALIZED`、`protocol_version=1.0`、`sealed=false`、`imported=false`、`reusable=true`。
  - `data/`、`meta/`、`manifests/`、`logs/`、`quarantine/partial/` 均存在。
  - `.partial` 文件数为 0，总字节为 0。
- Center `/api/disk/verify` 只读校验：
  - HTTP 200。
  - `allowed=true`、`disk_enabled=true`、`expected_status=INITIALIZED`、`action=ALLOW_EXPORT`。
- Edge 服务与 DB 状态：
  - `rustfs-transfer-edge.service` 为 `active`。
  - Edge `/healthz`、`/readyz` 均为 200，`database_ok=true`、`rustfs_ok=true`。
  - 当前已部署 Edge 服务仅暴露 `/healthz`、`/readyz`；`/api/scan`、`/api/export`、`/api/disks/rescan`、`/api/export/start` 均为 404。
  - Edge DB 当前 `disk_runtime_count=0`、`tsta_runtime_count=0`、`export_job_count=0`、`export_object_count=0`、`local_object_snapshot_count=0`。
- 结论：
  - 从盘身份、协议状态和 Center 授权看，`FUSTFS-TST-A` 可安全进入扫描/导出前置阶段。
  - 当前已部署 Edge 服务版本缺少扫描/导出/重扫控制 API，无法通过服务 HTTP 入口启动真实 scan/export；需部署包含控制 API 的新 Edge 版本或提供受控运维触发入口后再执行。

### P0 热插拔扫描通知修补合入

- 合入内容：
  - 新增 Edge 本地 rescan 协调器与 `rustfs-transfer-rescan` 通知二进制。
  - Edge 服务启动后触发一次已有运输盘扫描；新增 `/internal/disk/rescan` 本地通知入口，使用 `X-Rescan-Token` 鉴权。
  - udev systemd 模板改为只调用本地通知二进制，并从 `/etc/rustfs-transfer/edge.env` 读取 token。
  - 配置解析支持 `edge_auth_secret_env`、`transport_mount_root`、`rescan.token_env` 以及相关环境变量覆盖。
  - `scripts/check-deploy.ps1` 增加 rescan service 不直接执行业务/格式化/清理检查。
- 冲突处理：
  - 未合入 `5874` 中与本轮无关的中控端、前端、common 和旧任务记录 diff。
  - 未修改 `docs/v1.0冻结/`。
- 验证：
  - `cargo fmt --all -- --check`：通过。
  - `cargo test -p rustfs-transfer-edge --target-dir C:\Users\Beipy\.codex\visualizations\2026\08\09\019fe57e-f756-7b91-bbdb-92c59b9743e4\cargo-target`：通过。
  - `powershell -ExecutionPolicy Bypass -File scripts\check-deploy.ps1`：通过。
  - `rg "^(<<<<<<<|=======|>>>>>>>)"`：未发现冲突标记。
  - `git diff --name-only -- docs/v1.0冻结`：无输出。

### TASK-S1-CENTER-HOTFIX-001：Center 初始化失败 data_key 一致性修补合入

- 合入内容：
  - Center `/api/disk/initialize` 改为两阶段 key 生命周期：先持久化不可发放 `REVOKED` staging key，盘内协议目录和 `disk_info.json` 写入成功后再激活为 `ACTIVE`。
  - PostgreSQL 激活路径使用同一事务完成新 key 激活、同盘旧未发放 `ACTIVE` key 撤销、`disk_list.last_init_time` 更新。
  - 写盘失败时 staging key 保持 `REVOKED`，不会被 `/api/disk/export-key` 发放。
  - DB 激活失败时新 key 仍不可发放，并尽力将盘内 `disk_info.json.status.code` 写为 `ERROR`。
  - 成功初始化新 key 时，只撤销同盘 `edge_code/export_job_id/seal_id` 均为空的旧 `ACTIVE` key，不触碰 `ISSUED`、`SEALED_READONLY`、`RETIRED` 历史数据。
- 冲突处理：
  - 仅转移 `777c` 中 `crates/center-backend/src/lib.rs` 的 hotfix 代码、`TASK-S1-CENTER-HOTFIX-001` 任务表行/卡片和本段 dev log。
  - 未合入 `777c` 中其它 worktree 旧 diff；保留主目录已有热插拔与 Edge 控制 API 合入成果。
  - 未修改 `docs/v1.0冻结/`。
- 回归测试：
  - `initialize_write_failure_does_not_leave_active_key_and_retry_uses_single_active_key` 覆盖写盘失败后无新增可发放 key，重试成功后盘内引用的 key 是唯一 `ACTIVE`。
  - `initialize_success_revokes_superseded_unissued_active_keys_for_same_disk` 覆盖已有旧未发放 `ACTIVE` 孤儿时，成功初始化会撤销旧 key。
- 验证：
  - `cargo fmt --all -- --check`：通过。
  - `cargo test -p rustfs-transfer-center --target-dir C:\Users\Beipy\.codex\visualizations\2026\08\09\019fe57e-f756-7b91-bbdb-92c59b9743e4\cargo-target`：通过。
  - `cargo test --workspace --target-dir C:\Users\Beipy\.codex\visualizations\2026\08\09\019fe57e-f756-7b91-bbdb-92c59b9743e4\cargo-target`：通过。
  - `powershell -ExecutionPolicy Bypass -File scripts\check-deploy.ps1`：通过。
  - `git diff --name-only -- docs/v1.0冻结`：无输出。
  - `rg "^(<<<<<<<|=======|>>>>>>>)"`：未发现冲突标记。

### TASK-S1-DASHBOARD-REALTIME-001：双端 Dashboard 后端 HTTP summary 与本端 WebSocket 合入

- 合入内容：
  - Center 后端新增 `GET /api/center/summary`，从 `disk_list` 和每盘最新 `import_job` 生成刷新恢复用 Dashboard summary。
  - Center 后端新增 `/ws/center/import-progress`，并保留第四轮前端默认 `/ws/center/progress` 兼容别名，输出 `IMPORT_PROGRESS` 形状事件。
  - Edge 后端扩展 `GET /api/edge/summary`，补齐 `edge_name`、`export_job_id`、`export_job_status`、`disk_status_code`、`global_progress`、`disk_sn`、`device_path` 等前端字段。
  - Edge 后端新增 `/ws/edge/copy-progress`，并保留第四轮前端默认 `/ws/edge/progress` 兼容别名，从服务级 copy `ProgressAggregator` 输出 `COPY_PROGRESS`；无活动快照时发送空闲事件。
  - Edge `start_export_job` 分配 READY 盘对象后注册服务级 copy progress 快照，供 WS publisher 读取。
- 冲突处理：
  - 只合入 `d64a` 中 `crates/center-backend/src/lib.rs`、`crates/edge-backend/src/control.rs`、`crates/edge-backend/src/server.rs` 的 summary/WS 后端增量和本任务记录。
  - 保留第四轮前端真实接线文件不被覆盖；后端通过 WS 兼容别名匹配既有前端默认路径。
  - 保留第五轮 Edge RustFS 静态凭据 fixture，不采用 `d64a` 中删除测试凭据的旧片段。
  - 未修改 `docs/v1.0冻结/`。
- 验证：
  - `cargo fmt --all -- --check`：通过。
  - `cargo test -p rustfs-transfer-center --target-dir C:\Users\Beipy\.codex\visualizations\2026\08\09\019fe57e-f756-7b91-bbdb-92c59b9743e4\cargo-target`：沙箱内 3 个既有初始化写盘测试因 `crates/center-backend/target/test-disks` 权限拒绝失败；非沙箱复跑通过，新增 Dashboard summary/WS shape 测试通过。
  - `cargo test -p rustfs-transfer-edge --target-dir C:\Users\Beipy\.codex\visualizations\2026\08\09\019fe57e-f756-7b91-bbdb-92c59b9743e4\cargo-target`：通过。
  - `web/center-web npm run typecheck`：通过。
  - `web/center-web npm run build`：通过。
  - `web/edge-web npm run typecheck`：通过。
  - `web/edge-web npm run build`：通过。
  - `powershell -ExecutionPolicy Bypass -File scripts\check-deploy.ps1`：通过。
- 部署前置：
  - Center VM 需部署新 `rustfs-transfer-center`，验证 `/healthz`、`/readyz`、`/api/center/summary`、`/ws/center/import-progress` 或 `/ws/center/progress`。
  - Edge VM 需部署新 `rustfs-transfer-edge`，验证带 control token 的 `/api/edge/summary` 和 `/ws/edge/copy-progress` 或 `/ws/edge/progress`。
  - 前端静态代理需转发 `/api/center/summary`、`/ws/center/progress`、`/ws/center/import-progress`、`/api/edge/summary`、`/ws/edge/progress`、`/ws/edge/copy-progress`；Edge summary 继续由服务端代理注入 control token。
- 真实 Center 部署与孤儿 key 处理：
  - 部署前需构建并替换 `rustfs-transfer-center`，确认 Center 配置、PostgreSQL 连接、systemd 服务和 `/api/disk/initialize` 目标挂载权限就绪。
  - 受控撤销历史孤儿 key 前只做只读核验；确认盘内 `disk_info.json.security.data_key_id` 指向成功初始化后的 key，且待撤销旧 key 满足同 `disk_id`、`status='ACTIVE'`、`edge_code IS NULL`、`export_job_id IS NULL`、`seal_id IS NULL`。
  - 满足条件后可在维护窗口内将旧未引用 key 更新为 `REVOKED` 并写入 `remark`；不得删除行，保留审计链路。

### 合入后全量验证

- `cargo fmt --all -- --check`：通过。
- `cargo test --workspace --target-dir C:\Users\Beipy\.codex\visualizations\2026\08\09\019fe57e-f756-7b91-bbdb-92c59b9743e4\cargo-target`：通过。
- `web/center-web npm run typecheck`：通过。
- `web/center-web npm run build`：通过，生成 `web/center-web/dist/`。
- `web/edge-web npm run typecheck`：通过。
- `web/edge-web npm run build`：通过，生成 `web/edge-web/dist/`。
- `powershell -ExecutionPolicy Bypass -File scripts\check-deploy.ps1`：通过。
- 收口检查：
  - `rg "^(<<<<<<<|=======|>>>>>>>)"`：未发现冲突标记。
  - `git diff --name-only -- docs/v1.0冻结`：无输出。
  - 主目录仍保留合入前已有的未提交/未跟踪改动；本轮未执行提交、推送、reset、checkout 或 revert。
- VM 部署前置：
  - 需要部署更新后的 Edge 后端二进制 `rustfs-transfer-edge` 与 `rustfs-transfer-rescan`。
  - 需要部署更新后的 `/etc/rustfs-transfer/edge.toml` 与 `/etc/rustfs-transfer/edge.env`，补齐 `RUSTFS_TRANSFER__RESCAN__TOKEN` 和 `RUSTFS_TRANSFER__SERVER__CONTROL_API_TOKEN`。
  - 需要部署更新后的 `rustfs-transfer-disk-rescan@.service` 并执行 systemd daemon reload；udev 规则仍只触发 rescan service。
  - 真实联调仍要求 Center/Edge PostgreSQL、RustFS S3 endpoint、Center `/api/disk/verify`、运输盘 ext4 挂载根和 token/密钥配置就绪。

### P0 Edge 扫描导出触发入口合入

- 合入内容：
  - 新增 `control` 服务，提供扫描触发、导出任务创建、READY 盘对象分配、导出任务查询和 Edge 摘要。
  - Edge HTTP 服务新增 `/api/edge/summary`、`/api/edge/scan`、`/api/edge/export-jobs`、`/api/edge/export-jobs/{export_job_id}`、`/api/edge/export-jobs/{export_job_id}/start`。
  - 新增 `X-Edge-Control-Token` 鉴权与 `control_api_token_env` 配置，响应字段使用 `scan_status`、`runtime_status`、`export_job_status`，避免裸 `status`。
  - `AdapterBundle` 暴露已创建的 Postgres pool 与 S3 client 给控制服务复用。
  - 保留上一项 `/internal/disk/rescan` 与启动扫描入口，未接受 `ee8a` 旧基线中删除 rescan 的差异。
- 冲突处理：
  - 手工合并 `server.rs`，同时保留 rescan 入口和新增 control API。
  - 手工合并 `config.rs`，同时保留 `rescan.*` 与新增 `server.control_api_token*`。
  - 未合入 `ee8a` 中与本轮无关的中控端、前端、common 和旧任务记录 diff。
  - 未修改 `docs/v1.0冻结/`。
- 验证：
  - `cargo fmt --all -- --check`：通过。
  - `cargo test -p rustfs-transfer-edge --target-dir C:\Users\Beipy\.codex\visualizations\2026\08\09\019fe57e-f756-7b91-bbdb-92c59b9743e4\cargo-target`：通过。
  - `powershell -ExecutionPolicy Bypass -File scripts\check-deploy.ps1`：通过。
  - `rg "^(<<<<<<<|=======|>>>>>>>)"`：未发现冲突标记。
  - `git diff --name-only -- docs/v1.0冻结`：无输出。
### Real Edge deployment update: control API + rescan helper

- Scope: Edge VM only (`edge@192.168.128.130`). No repository code changes, no TASKS.md update, no scan/export trigger.
- Build: created a sanitized source snapshot excluding `.git`, `target`, `node_modules`, `.local.env`, and key-like files; built on Center with `CARGO_TARGET_DIR=/tmp/rustfsdisk-target-20260810-003810`.
- Built artifacts:
  - `rustfs-transfer-edge` sha256 `b11beaaa832445d7f648e5df9d7b0f3abf6e21104d075a45e59e9c2c4f63967b`
  - `rustfs-transfer-rescan` sha256 `4ecfed22f9e2a0723c053fca8cd1a1a2c26961614f662d3330755b4af5f8754f`
- Installed on Edge:
  - `/opt/rustfs-transfer/rustfs-transfer-edge`
  - `/opt/rustfs-transfer/bin/rustfs-transfer-rescan`
  - `/etc/systemd/system/rustfs-transfer-edge.service`
  - `/etc/systemd/system/rustfs-transfer-disk-rescan@.service`
  - `/etc/udev/rules.d/99-rustfs-transfer-disk.rules`
  - `/etc/rustfs-transfer/edge.toml` updated with `control_api_token_env` and `[rescan].token_env`
  - `/etc/rustfs-transfer/edge.env` updated with independent non-empty control/rescan tokens; token values not logged
- Preserved existing `edge_auth_secret`; validated by comparing the field before/after without printing the value.
- Deployment correction: initial TOML patch wrote unquoted env var names and caused service parse failures; fixed in place, then service became `active/running`.
- Verification:
  - `/healthz` HTTP 200.
  - `/readyz` HTTP 200 with `database_ok=true`, `rustfs_ok=true`.
  - `GET /api/edge/summary` without `X-Edge-Control-Token` returned 401.
  - `GET /api/edge/summary` with token returned 200.
  - `POST /internal/disk/rescan` without token returned 401 and logged `rejected unauthorized edge disk rescan request`.
  - `POST /internal/disk/rescan` with token returned 202 and logged `edge disk rescan completed` with `record_count=1`.
  - Rescan helper `--help` works when invoked with root/systemd privileges.
  - Logs after final validation had no panic/error/failed entries.
- Runtime note:
  - Edge service initially could not enumerate `/media/edge` because service user lacked execute permission on `/media/edge`.
  - Added ACL `u:rustfs-transfer:rx` to `/media/edge`; this is host mount-parent access only and does not modify transport disk contents.
  - Rescan now detects `FUSTFS-TST-A` at `/media/edge/FUSTFS-TST-A`, disk_id `9f8308ca-045e-425b-8664-4bb6fdaa0db3`.
  - Current Edge runtime marks it `REJECTED` with `last_error_code=CENTER_REJECTED`; inferred likely cause is Edge `ConfiguredMountProbe` sending the mount directory name as `sn`, while Center verify compares against registered hardware SN.
- Next:
  - Source object upload can begin only after explicit approval to write Edge RustFS.
  - Edge scan API is deployed, but starting scan will write Edge scan snapshot DB; do not run until main window approves.
  - Export must wait until Center verify accepts TST-A and Edge runtime becomes `READY`.

### Real frontend VM deployment: Center + Edge

- Scope: deployed existing `web/center-web/dist` and `web/edge-web/dist` to the two Linux VMs. No repository code changes, no scan/export trigger, no disk/RustFS writes.
- Precheck:
  - `web/center-web/dist` and `web/edge-web/dist` existed with Vite `index.html` plus hashed JS/CSS assets.
  - Current frontends are fixture/mock driven; Center has no real HTTP client, Edge `fetchEdgeDashboardSummary()` returns a local mock object.
  - Center VM had no nginx/caddy, Python 3 present.
  - Edge VM had nginx active on localhost `8080`, Python 3 present; no caddy.
  - VM firewalls inactive.
- Hosting choice:
  - Used minimal systemd + Python stdlib static/proxy service on both VMs, avoiding package installation.
  - Service port: `8088` on `0.0.0.0`.
  - Center deployment path: `/opt/rustfs-transfer/web/center/current`.
  - Edge deployment path: `/opt/rustfs-transfer/web/edge/current`.
  - Shared helper: `/opt/rustfs-transfer/bin/rustfs-transfer-web-static.py`.
  - Units: `rustfs-transfer-center-web.service`, `rustfs-transfer-edge-web.service`.
- Read-only proxy behavior:
  - Both frontends proxy `GET /healthz` and `GET /readyz` to the same-VM backend.
  - Edge frontend host proxies only `GET /api/edge/summary`, injecting the configured control token server-side without logging or exposing it.
  - Scan/export/internal APIs are not proxied.
  - WebSocket upgrade requests return 404 with `WEBSOCKET_NOT_AVAILABLE` because backend WS routes are not deployed in the current service version.
- LAN verification from host:
  - Center `http://192.168.128.131:8088/` returned 200.
  - Center assets `/assets/index-B6flreUY.js` and `/assets/index-DLxiy8k7.css` returned 200.
  - Center `/healthz` and `/readyz` via frontend host returned 200.
  - Center summary path returned 404 `NOT_PROXIED`; no Center summary API exists in current backend.
  - Center WS upgrade returned 404 `WEBSOCKET_NOT_AVAILABLE`.
  - Edge `http://192.168.128.130:8088/` returned 200.
  - Edge assets `/assets/index-B3lEsnUA.js` and `/assets/index-nMpoInkN.css` returned 200.
  - Edge `/healthz` and `/readyz` via frontend host returned 200.
  - Edge `/api/edge/summary` via frontend host returned 200; current disk runtime remains `REJECTED/CENTER_REJECTED` for TST-A.
  - Edge WS upgrade returned 404 `WEBSOCKET_NOT_AVAILABLE`.
- Blockers:
  - Frontend pages themselves still render mock data and do not actively call real backend APIs or WebSocket.
  - Center backend currently has no summary/dashboard API.
  - Backend WebSocket routes are not present, so WS connection cannot succeed until implemented/deployed.

### TASK-S1-EDGE-HOTFIX-DEPLOY-002: Deploy Edge SN hotfix to real Edge VM

- Scope:
  - Deployed latest `rustfs-transfer-edge` and `rustfs-transfer-rescan` to Edge only.
  - Preserved `/etc/rustfs-transfer/edge.env`, existing control/rescan tokens, existing `edge_auth_secret`, and Edge frontend service.
  - Did not call `/api/edge/scan`, did not create export jobs, did not write RustFS source objects, and did not write transport disk content.
- Installed artifacts:
  - `/opt/rustfs-transfer/rustfs-transfer-edge`
    - sha256: `f15d5f3d084b3798857812b57975385d0c658d6cf5ed188d400a678e44623161`
  - `/opt/rustfs-transfer/bin/rustfs-transfer-rescan`
    - sha256: `acebe5d80e0d32c1f0c446581ad7297016001d5869bc9f12963eac482a9b8741`
- Service verification:
  - `rustfs-transfer-edge.service`: active/running after restart.
  - `/healthz`: HTTP 200.
  - `/readyz`: HTTP 200.
  - `rustfs-transfer-edge-web.service`: remained active.
- Rescan verification:
  - Triggered exactly one explicit internal rescan through `rustfs-transfer-disk-rescan@sdc.service`.
  - Oneshot result: success, `ExecMainStatus=0`.
  - Startup rescan and explicit Udev rescan both detected target disk as READY.
- Target disk identity:
  - Device: `/dev/sdc1`.
  - Mount: `/media/edge/FUSTFS-TST-A`.
  - Filesystem: ext4.
  - Label: `FUSTFS-TST-A`.
  - FS UUID: `0878ee5b-3e86-4ae0-8d0f-461c2732ee42`.
  - Hardware SN from udev: `323533394352343033383235`.
  - Disk ID: `9f8308ca-045e-425b-8664-4bb6fdaa0db3`.
- Edge control summary:
  - Current target rows include `runtime_status=READY`, `last_error_code=null`, `error_message=null`.
  - Summary also still exposes an older stale `REJECTED/CENTER_REJECTED` row for the same disk from before the hotfix; no manual DB cleanup was performed.
- Log verification:
  - Recent service logs show persisted runtime snapshot with real SN, matching FS UUID/label, `runtime_status=READY`, and `task_pool_eligible=true`.
  - No panic or service failure observed in the deployment/rescan window.

### TASK-S1-EDGE-CREDENTIAL-HOTFIX-DEPLOY-001: Deploy Edge RustFS credential hotfix and run authorized scan

- Scope:
  - Built latest Linux release artifacts on Center with rustup stable Cargo `1.97.1`, preserving source `Cargo.lock` version 4.
  - Deployed latest `rustfs-transfer-edge` and matching `rustfs-transfer-rescan` to Edge.
  - Preserved `/etc/rustfs-transfer/edge.env`, existing tokens, existing `edge_auth_secret`, and Edge frontend service.
  - Did not create export jobs, did not start export, did not write transport disk content, and did not touch `FUSTFS-A-DRILL`.
- Installed artifacts:
  - `/opt/rustfs-transfer/rustfs-transfer-edge`
    - sha256: `30e0d85109ace254924c8e9b8b9ab180f7f8ece62d4e529aa2bf40af340812b5`
  - `/opt/rustfs-transfer/bin/rustfs-transfer-rescan`
    - sha256: `6d64e576d4fca1037377bad1d6dac1d4dc9c1748401f1de55095472f93c04cbb`
- Service verification:
  - `rustfs-transfer-edge.service`: active/running after restart.
  - `/healthz`: HTTP 200.
  - `/readyz`: HTTP 200.
  - `rustfs-transfer-edge-web.service`: remained active.
- Authorized scan:
  - Called `POST /api/edge/scan` exactly once with `enqueue_stable_objects=false`.
  - Response: `scan_status=DONE`, `bucket_count=2`, `object_seen=57`, `stable_object_count=57`, `source_changed_count=0`, `total_bytes=951301462`.
- DB verification:
  - `local_object_snapshot`: `0 -> 57` rows.
  - Target object row: 1 row, `stable_status=STABLE`, size `24472432`, metadata source sha256 matched.
  - `export_job`: `0 -> 0`.
  - `export_object`: `0 -> 0`.
- Target source object:
  - Bucket: `test`.
  - Key: `codex-real-scan/20260809T173110Z-edge-VMware-Virtual-Platform-eaedeb0088e687bf-rustdesk-1.4.9-x86_64.exe`.
  - Size: `24472432`.
  - Source sha256: `eaedeb0088e687bf46f7c46a9c6ea5493ce51f3134dfd6acbedb47b5b9136274`.
- Log verification:
  - No `dispatch failure`, `CredentialsNotLoaded`, panic, or secret field output observed in the post-deploy scan validation window.

### TASK-S1-ROUND9-VM-DEPLOY-001: Deploy round 9 Center/Edge backend and real Dashboard

- Scope:
  - Read `AGENTS.md`, `work_sprint/TASKS.md`, and the frozen v1.0 docs before deployment.
  - Did not modify frozen docs or `TASKS.md`.
  - Deployed latest Center and Edge backend binaries plus both static Dashboard builds to the real VMs.
  - Updated only deploy/runtime files under `/opt/rustfs-transfer` and `/etc/rustfs-transfer` on the VMs.
  - Did not call Edge scan/export, Center import/initialize/reinitialize/cleanup, or write transport disk content.
  - Did not touch `FUSTFS-A-DRILL`.
- Build:
  - Built backend Linux release artifacts on Center using rustup stable Cargo `1.97.1`; source `Cargo.lock` remained version 4.
  - Built `web/center-web` and `web/edge-web` with `npm run build`.
- Installed backend artifacts:
  - Center `/opt/rustfs-transfer/rustfs-transfer-center`
    - sha256: `2db6577f47f0a3d6d6d224bac15437144d38bf5a9b9ce6e03f327685cf68ca71`
  - Edge `/opt/rustfs-transfer/rustfs-transfer-edge`
    - sha256: `c326595eb7aff9b645f35ff8c4b153983bcbb150da2d1cbcd50c8e3b0484c240`
  - Edge `/opt/rustfs-transfer/bin/rustfs-transfer-rescan`
    - sha256: `f7f2bb5b928f3d86ed4ea77b612d5ec5192786bd84425843bf21c44d1148f487`
- Center env/config:
  - Verified `RUSTFS_TRANSFER__SERVER__CONTROL_API_TOKEN`, `RUSTFS_TRANSFER__SECURITY__LOCAL_MASTER_KEY`, and `RUSTFS_TRANSFER__SECURITY__CENTER_SIGNATURE_KEY` are present and parseable without printing values.
  - Added missing Center control API token and `center.toml` env reference.
  - Normalized `/etc/rustfs-transfer/center.env` to one key per line after detecting a no-trailing-newline append issue.
- Service verification:
  - Center backend: active/running, `/healthz` 200, `/readyz` 200, `/api/center/summary` 200.
  - Edge backend: active/running, `/healthz` 200, `/readyz` 200.
  - Center and Edge web services: active, root HTML 200.
  - Static assets: Center `index-DyOKeVB7.js` / `index-CmKhN_8t.css` 200; Edge `index-CeLX6fGI.js` / `index-odvJyjTY.css` 200.
  - Same-origin Dashboard summary: Center `/api/center/summary` 200; Edge `/api/edge/summary` 200.
  - WebSocket handshake: backend and web-proxy paths both returned `101 Switching Protocols` for Center and Edge.
- Edge readonly rescan:
  - Triggered one internal `rustfs-transfer-disk-rescan@sdc.service`; oneshot result success.
  - No `/api/edge/scan`, export create, or export start was called.
- DB readonly checks:
  - Center: `import_job=0`, `object_ledger=0`, `disk_list=1`.
  - Edge: `local_object_snapshot=57`, `export_job=0`, `export_object=0`, `disk_runtime=7`.
- TST-A protocol note:
  - `/media/edge/FUSTFS-TST-A/rustfs-transfer/disk_info.json` has `disk_id=9f8308ca-045e-425b-8664-4bb6fdaa0db3`, `status_code=INITIALIZED`, protocol `1.0`.
  - Security section only has `data_key_id` and signature metadata; no wrapped/encrypted data key field is present.
  - Treat current TST-A as old mock/safe initialization format requiring Center-side reinitialization before real export.

### TASK-S1-EDGE-HOTFIX-001：Edge 真实挂载盘硬件 SN 探测修补合入

- 合入内容：
  - `ConfiguredMountProbe` 不再将挂载目录名或 label 当作 `sn`。
  - 从 `findmnt` 保留真实 `device_path` 和文件系统类型，并通过 `udevadm` / `blkid` 读取 `ID_SERIAL_SHORT`、`ID_SERIAL`、FS UUID、label 和文件系统类型。
  - 硬件 SN 优先 `ID_SERIAL_SHORT`，其次 `ID_SERIAL`；FS UUID、label、serial 字段进入运行时记录和结构化日志。
  - 新增 `HARDWARE_SN_UNAVAILABLE` 本地拒绝：ext4 运输盘无法读取硬件 SN 时不调用 Center verify、不进入任务池。
  - 非 ext4 拒绝路径仍为 `FILESYSTEM_UNSUPPORTED`，业务主身份仍以盘内 `disk_id` 和生命周期为准。
- 冲突处理：
  - 仅转移 `7182` 中 `crates/edge-backend/src/disk_detection.rs` 的硬件 SN hotfix、`TASK-S1-EDGE-HOTFIX-001` 任务表行/卡片和本段 dev log。
  - 未合入 `7182` 中其它历史 worktree diff；保留主目录已有热插拔、Edge 控制 API、Center 初始化 key 原子性修补和真实部署记录。
  - 未修改 `docs/v1.0冻结/`。
- 回归测试：
  - `configured_probe_does_not_use_mount_label_as_hardware_sn`：验证真实 mount probe 不使用 `FUSTFS-TST-A` 等挂载目录名作为硬件 SN。
  - `ready_after_initialized_disk_passes_center_verify`：验证 Center verify payload 使用硬件 SN。
  - `rejects_missing_hardware_sn_before_center_verify`：验证缺失硬件 SN 时本地拒绝且不调用 Center。
  - `device_metadata_prefers_short_serial_and_preserves_fs_identity`：验证 `ID_SERIAL_SHORT` 优先级和 FS UUID / label 保留。
- 验证：
  - `cargo fmt --all -- --check`：通过。
  - `cargo test -p rustfs-transfer-edge --target-dir C:\Users\Beipy\.codex\visualizations\2026\08\09\019fe57e-f756-7b91-bbdb-92c59b9743e4\cargo-target`：通过。
  - `powershell -ExecutionPolicy Bypass -File scripts\check-deploy.ps1`：通过。
  - `cargo test --workspace --target-dir C:\Users\Beipy\.codex\visualizations\2026\08\09\019fe57e-f756-7b91-bbdb-92c59b9743e4\cargo-target`：通过。
  - `web/center-web npm run typecheck`：通过。
  - `web/center-web npm run build`：通过。
  - `web/edge-web npm run typecheck`：通过。
  - `web/edge-web npm run build`：通过。
  - `git diff --name-only -- docs/v1.0冻结`：无输出。
  - `rg "^(<<<<<<<|=======|>>>>>>>)"`：未发现冲突标记。
- 部署窗口判断：
  - 代码侧已可由部署窗口安全重新部署 Edge 后端与 rescan helper。
  - 部署后可触发只读 rescan；rescan 只做盘识别、Center verify 和 Edge `disk_runtime` 记录，不应启动 scan/export。
  - 触发前需确认 Edge 服务用户可穿越挂载父目录、`udevadm info --query=property --name <device>` 可读到注册硬件 SN，且 `RUSTFS_TRANSFER__RESCAN__TOKEN` 配置就绪。

### TASK-S1-WEB-EDGE-001 / TASK-S1-WEB-CENTER-001：Dashboard 前端真实联调成果合入

- 合入内容：
  - 边缘端和中控端 Vue Dashboard 从 fixture/mock 页面改为同源 HTTP summary + 本端 WebSocket 进度事件接线。
  - Edge 新增真实 summary client 和 `COPY_PROGRESS`/磁盘事件 WebSocket client，兼容当前 Edge control summary DTO 并转换为 Dashboard 展示模型。
  - Center 新增真实 summary client 和 `IMPORT_PROGRESS`/磁盘事件 WebSocket client，HTTP 失败时显示明确错误码和恢复入口。
  - 双端 Dashboard 展示扫描/导入队列、盘列表、当前对象、速度、错误、硬件 SN、ID_SERIAL、FS UUID、PARTUUID、非 ext4/未注册/拔盘/空间不足/恢复要求/`REINIT_FAILED` 等状态。
- 冲突处理：
  - 仅转移 `ca9c` 中双端 `web/*/src/api`、`web/*/src/ws`、`DashboardView.vue`、`style.css` 相关改动，以及 TASKS 前端验收勾选。
  - 未覆盖主目录已有真实部署记录、前三轮合入记录、Edge 控制 API 和硬件 SN hotfix。
  - 未修改 `docs/v1.0冻结/`。
- 后端依赖：
  - Edge 默认读取 `GET /api/edge/summary`，可用 `VITE_EDGE_DASHBOARD_SUMMARY_PATH` 覆盖；默认连接 `/ws/edge/progress`，可用 `VITE_EDGE_DASHBOARD_WS_PATH` 覆盖。
  - Center 默认读取 `GET /api/center/summary`，可用 `VITE_CENTER_DASHBOARD_SUMMARY_PATH` 覆盖；默认连接 `/ws/center/progress`，可用 `VITE_CENTER_DASHBOARD_WS_PATH` 覆盖。
  - 前端不读取、不保存、不发送 `X-Edge-Control-Token`；如 Edge summary 仍要求控制 token，需由后端提供只读 Dashboard summary 或由同源部署代理安全注入。
- 验证：
  - `web/edge-web npm run typecheck`：通过。
  - `web/edge-web npm run build`：通过。
  - `web/center-web npm run typecheck`：通过。
  - `web/center-web npm run build`：通过。

### TASK-S1-EDGE-HOTFIX-002：Edge RustFS S3 凭据显式注入修补合入

- 合入内容：
  - `S3ObjectStore` 使用 `edge.toml` / 环境覆盖中的 `rustfs.access_key_id` 与 `rustfs.secret_access_key` 构造 `aws-sdk-s3` 静态凭据 provider。
  - `EdgeConfig` 新增 `RUSTFS_TRANSFER__RUSTFS__ACCESS_KEY_ID` 和 `RUSTFS_TRANSFER__RUSTFS__SECRET_ACCESS_KEY` 覆盖，并在启动配置校验阶段对缺失凭据返回明确字段名。
  - Edge server 测试 fixture 补齐 RustFS 凭据，避免新配置校验误伤控制 API 测试。
- 冲突处理：
  - 仅转移 `dfaa` 中 `crates/edge-backend/src/adapters.rs`、`crates/edge-backend/src/config.rs`、`crates/edge-backend/src/server.rs` 的静态凭据注入必要改动，以及 `TASK-S1-EDGE-HOTFIX-002` 任务表/卡片和本段 dev log。
  - 未合入 `dfaa` 中前端、部署配置、common、center 或历史任务记录 diff；保留第四轮前端成果和主目录已有部署日志。
  - 未修改 `docs/v1.0冻结/`。
- 安全边界：
  - 不日志输出 `secret_access_key`，不写入数据库或运输盘。
  - `edge_auth_secret` 仍只用于 Edge 到 Center HTTP HMAC 鉴权，不复用于 RustFS/S3。
  - 未改动 RustFS 源对象删除、覆盖或 metadata 写入路径。
- 验证：
  - `cargo fmt --all -- --check`：通过。
  - `cargo test -p rustfs-transfer-edge`：默认 `target` 目录因 `F:\Project\RustFSDisk\target\debug\.cargo-build-lock` 权限拒绝未运行。
  - `cargo test -p rustfs-transfer-edge --target-dir C:\Users\Beipy\.codex\visualizations\2026\08\09\019fe57e-f756-7b91-bbdb-92c59b9743e4\cargo-target`：通过。
  - `cargo test --workspace --target-dir C:\Users\Beipy\.codex\visualizations\2026\08\09\019fe57e-f756-7b91-bbdb-92c59b9743e4\cargo-target`：沙箱内 Center 初始化测试无法写 `crates/center-backend/target/test-disks`，非沙箱复跑通过。
  - `powershell -ExecutionPolicy Bypass -File scripts\check-deploy.ps1`：通过。
  - `git diff --name-only -- docs/v1.0冻结`：无输出。
  - `rg "^(<<<<<<<|=======|>>>>>>>)"`：未发现冲突标记。

### TASK-S1-CENTER-HOTFIX-002：Center verify/export-key 强制 Edge HMAC 鉴权合入

- 合入内容：
  - Center 抽出可复用 `authenticate_edge_request`，统一读取请求 body、校验 HMAC header、timestamp、nonce 防重放、body SHA256、method/path canonical signature、Edge 启用状态和请求体 `edge_code` 一致性。
  - `/api/disk/verify` 与 `/api/disk/export-key` 改为先通过 Edge HMAC 鉴权，再反序列化业务请求并进入原有校验/发 key 逻辑。
  - 新增敏感接口鉴权失败响应适配，保持业务错误与鉴权错误边界清晰。
  - Edge center client 新增与 shared canonical HMAC 一致性测试，防止 verify/export-key 签名形状漂移。
  - Center edge_auth 集成测试覆盖未签名、正确签名、header 小写、timestamp 偏移、nonce 重放、body/method/path 签名错误、禁用 Edge、export-key 鉴权失败不泄露 key。
- 冲突处理：
  - 手工合并 `crates/center-backend/src/lib.rs`，保留第六轮 Dashboard `/api/center/summary`、`/ws/center/import-progress`、`/ws/center/progress` 路由和 HMAC 强制鉴权两方增量。
  - 仅转移 `24d2` 中 `center_auth.rs`、`center lib` 敏感接口 handler、`center tests/edge_auth.rs`、`edge center_client.rs` 的必要增量；未带入无关历史 diff。
  - 未修改 `docs/v1.0冻结/`。
- 验证：
  - `cargo fmt --all -- --check`：通过。
  - `cargo test -p rustfs-transfer-center --target-dir C:\Users\Beipy\.codex\visualizations\2026\08\09\019fe57e-f756-7b91-bbdb-92c59b9743e4\cargo-target`：沙箱内 3 个既有初始化写盘测试因 `crates/center-backend/target/test-disks` 权限拒绝失败；非沙箱复跑通过。
  - `cargo test -p rustfs-transfer-edge --target-dir C:\Users\Beipy\.codex\visualizations\2026\08\09\019fe57e-f756-7b91-bbdb-92c59b9743e4\cargo-target`：通过。
  - `cargo test --workspace --target-dir C:\Users\Beipy\.codex\visualizations\2026\08\09\019fe57e-f756-7b91-bbdb-92c59b9743e4\cargo-target`：通过。
  - `powershell -ExecutionPolicy Bypass -File scripts\check-deploy.ps1`：通过。

### TASK-S1-CENTER-HOTFIX-003：Center 真实 data_key 包裹与 disk_info 签名验签合入

- 合入内容：
  - 新增 `center_security` 模块，从 `RUSTFS_TRANSFER__SECURITY__LOCAL_MASTER_KEY` 与 `RUSTFS_TRANSFER__SECURITY__CENTER_SIGNATURE_KEY` 读取 32-byte base64/hex 密钥；缺失、占位或长度错误时 Center 启动失败。
  - Center 初始化新盘时生成内存态 32-byte `disk_data_key`，使用 AES-256-GCM + AAD(`disk_id + data_key_id`) 包裹为 `local-master-key:v1:<nonce>:<ciphertext>:<tag>`，DB 仅保存包裹密文。
  - `disk_info.json.security.center_signature` 改为 HMAC-SHA256 canonical JSON 签名，覆盖 `protocol`、`disk`、`center`、`security.center_key_id`、`security.signature_alg`、`security.data_key_id`。
  - `/api/disk/export-key` 发放时只解包 `LOCAL-MASTER-KEY` 格式；旧 `mock:v1:*` / `local-wrap:*` 明文等价 key 被拒绝。
  - Center import worker 在导入前验 `disk_info.json` 签名，导入完成写回 `IMPORTED` 时重新签名。
  - Post-import reinitializer 在清理/重新初始化前验旧 `disk_info.json` 签名，新 key 同样走真实包裹，新 `INITIALIZED` disk_info 重新签名。
  - `scripts/check-deploy.ps1` 增加 Center service、center.toml 示例、center.env 示例的安全密钥注入断言。
- 冲突处理：
  - `f86c` 的任务 ID 与第七轮 HMAC 均为 `TASK-S1-CENTER-HOTFIX-002`；主目录保留 HMAC 为 `HOTFIX-002`，本轮 Center Security 记录为 `TASK-S1-CENTER-HOTFIX-003`，避免总览/卡片 ID 冲突。
  - 手工合并 `crates/center-backend/src/lib.rs`，保留第六轮 Dashboard summary/WS 路由和第七轮 verify/export-key HMAC handler，同时接入真实包裹/签名。
  - 未带入 `f86c` 中与本轮无关的前端、Edge、历史任务记录 diff；未修改 `docs/v1.0冻结/`。
- 验证：
  - `cargo fmt --all -- --check`：通过。
  - `cargo test -p rustfs-transfer-common --target-dir C:\Users\Beipy\.codex\visualizations\2026\08\09\019fe57e-f756-7b91-bbdb-92c59b9743e4\cargo-target`：通过。
  - `cargo test -p rustfs-transfer-center --target-dir C:\Users\Beipy\.codex\visualizations\2026\08\09\019fe57e-f756-7b91-bbdb-92c59b9743e4\cargo-target`：沙箱内初始化写盘测试因 `crates/center-backend/target/test-disks` ACL 权限失败；非沙箱复跑通过。
  - `cargo test --workspace --target-dir C:\Users\Beipy\.codex\visualizations\2026\08\09\019fe57e-f756-7b91-bbdb-92c59b9743e4\cargo-target`：通过。
  - `powershell -ExecutionPolicy Bypass -File scripts\check-deploy.ps1`：通过。

### TASK-S1-P0-WORKER-RUNTIME-001：生产 DiskWorker / ImportWorker 受控接线合入

- 合入内容：
  - Edge `POST /api/edge/export-jobs/{export_job_id}/start` 在 control token 通过和 PostgreSQL 分配 READY 盘对象后，启动本次有分配对象的 `DiskWorker`。
  - 新增 Edge `export_runtime`，生产路径读取盘内 `disk_info.json.status.code` 与 `security.data_key_id`，通过 HMAC `CenterHmacClient` 调用 Center `/api/disk/export-key` 获取内存态 `disk_data_key` 后传入 worker；不从配置、请求、DB 或运输盘注入明文 key。
  - 新增 Center `import_runtime` 与 `POST /api/center/import-jobs/start`，使用 `X-Center-Control-Token` 受控触发，显式 `mount_path` 解析到 `/rustfs-transfer` 协议根后启动 `ImportWorker`。
  - Center 生产导入 repository 使用第八轮 `CenterSecurity` 解包 `LOCAL-MASTER-KEY` data_key，并沿用 disk_info HMAC 签名验签。
  - Center 启动入口注入 S3 client、`ProductionCenterImportControlService`、Center control token 与第八轮安全密钥；配置和部署样例新增 `RUSTFS_TRANSFER__SERVER__CONTROL_API_TOKEN`。
- 冲突处理：
  - 手工合并 Center `lib.rs`，保留第六轮 Dashboard `/api/center/summary` 与 `/ws/center/*`，保留第七轮 `/api/disk/verify`、`/api/disk/export-key` HMAC handler，保留第八轮 `CenterSecurity` key wrap / disk_info 签名，再加入 Center import control API。
  - 手工合并 Edge `control.rs` 和 `server.rs`，保留 Dashboard copy progress snapshot、Edge RustFS 静态凭据注入、启动/udev rescan 只读识别，并加入 `DiskWorker` 生产 launcher。
  - 未合入 `75de` 中与本轮 worker/control token/import runtime 无关的历史 diff；未修改 `docs/v1.0冻结/`。
- 验证：
  - `cargo fmt --all -- --check`：通过。
  - `cargo test -p rustfs-transfer-edge --target-dir C:\Users\Beipy\.codex\visualizations\2026\08\09\019fe57e-f756-7b91-bbdb-92c59b9743e4\cargo-target`：通过。
  - `cargo test -p rustfs-transfer-center --target-dir C:\Users\Beipy\.codex\visualizations\2026\08\09\019fe57e-f756-7b91-bbdb-92c59b9743e4\cargo-target`：沙箱内 4 个初始化写盘测试因 `crates/center-backend/target/test-disks` ACL 拒绝访问失败；非沙箱重跑通过。
  - `cargo test --workspace --target-dir C:\Users\Beipy\.codex\visualizations\2026\08\09\019fe57e-f756-7b91-bbdb-92c59b9743e4\cargo-target`：非沙箱通过。
  - `powershell -ExecutionPolicy Bypass -File scripts\check-deploy.ps1`：通过。
  - `git diff --name-only -- docs/v1.0冻结`：无输出。
  - `rg "^(<<<<<<<|=======|>>>>>>>)"`：未发现冲突标记。

### Git integration baseline closeout

- Commit subject: `chore: verified v1.0 integration baseline`.
- Scope: 当前主工作区已验证的 v1.0 串行合入成果、`TASKS.md`、2026-08-09/2026-08-10 dev log、部署样例和脚本；不包含 `.local.env`、真实凭据、`target/`、`dist/`、`node_modules/` 或未合入 worktree 内容。
- Known issue recorded in commit message: Center 受控 cleanup/reinitialize 路由仍缺失；已授权测试盘 cleanup/reinitialize 操作未执行；对应修补仍在独立 worktree 中，不纳入本基线提交。
- Commit gate actual results:
  - `cargo fmt --all -- --check`：通过。
  - `cargo test --workspace --target-dir C:\Users\Beipy\.codex\visualizations\2026\08\09\019fe57e-f756-7b91-bbdb-92c59b9743e4\cargo-target`：非沙箱通过；使用非沙箱是因为沙箱用户对 Center 初始化写盘测试目录 `crates/center-backend/target/test-disks` 存在 Windows ACL 拒绝。
  - `web/center-web npm run typecheck`：通过。
  - `web/center-web npm run build`：通过。
  - `web/edge-web npm run typecheck`：通过。
  - `web/edge-web npm run build`：通过。
  - `powershell -ExecutionPolicy Bypass -File scripts\check-deploy.ps1`：通过。
  - `git diff --check` 与 `git diff --cached --check`：通过。
  - `git diff --name-only -- docs/v1.0冻结`：无输出。
  - `rg "^(<<<<<<<|=======|>>>>>>>)"`：未发现冲突标记。
- Task status note: `TASKS.md` 保持事实一致，未将 Center cleanup/reinitialize 缺失能力标为完成。
- Future gate: 后续每个子任务交付后，必须先检查冻结文档和敏感文件，再串行合入、跑对应验证、同步同一任务 `TASKS.md` 与 dev log，并立即形成独立 commit；任一步未通过不得报告任务完成。

### Real Center disk operation: FUSTFS-TST-A secure reinitialize

- Scope: Center VM only; target `/dev/sdb1` mounted at `/media/control/FUSTFS-TST-A`; no import, cleanup, export, Edge business write, RustFS write, or FUSTFS-A-DRILL access.
- Pre-write identity recheck: exactly one target candidate matched label `FUSTFS-TST-A`, FS UUID `0878ee5b-3e86-4ae0-8d0f-461c2732ee42`, hardware SN `323533394352343033383235`, ext4, mount path, and disk_info disk_id `9f8308ca-045e-425b-8664-4bb6fdaa0db3`; confirmed not protected FUSTFS-A-DRILL.
- Operation: called deployed Center loopback `POST /api/disk/initialize` for the target disk only; HTTP 200; `status_code=INITIALIZED`; new non-secret `data_key_id=1bcf5897-708c-4bb1-ba68-0a9f1386c502`.
- Disk acceptance: `disk_info.json` has protocol `1.0`, `status.code=INITIALIZED`, `sealed=false`, `imported=false`, `reusable=true`, `last_error=null`, `encryption_alg=AES-256-GCM`, `signature_alg=HMAC-SHA256`, center signature present; no plaintext key / encrypted key / wrapped key field on disk.
- DB acceptance: previous mock active data_key rows for this disk were revoked; exactly one active target data_key remains, with `key_wrap_alg=LOCAL-MASTER-KEY` and wrapped key stored in Center DB only.
- Recovery/atomic acceptance: required protocol directories exist, no `disk_info.json.tmp`, no `.partial` files found. Center `/healthz`, `/readyz`, `/api/center/summary` returned HTTP 200; recent Center service logs had 0 `panic`, 0 `error`, 0 `failed` markers.

### Real Edge export attempt: FUSTFS-TST-A blocked by target disk permissions

- Scope: Edge VM only; target `/dev/sdc1` mounted at `/media/edge/FUSTFS-TST-A`; protected `/dev/sdb1` FUSTFS-A-DRILL was identified but not written.
- Readonly identity: target matched disk_id `9f8308ca-045e-425b-8664-4bb6fdaa0db3`, FS UUID `0878ee5b-3e86-4ae0-8d0f-461c2732ee42`, hardware SN `323533394352343033383235`, label `FUSTFS-TST-A`, ext4, protocol `1.0`, status `INITIALIZED`, AES-GCM/HMAC security metadata.
- Rescan/verify: internal Edge rescan accepted and latest target runtime was READY with no last_error; all READY rows referred to the target disk only.
- Export scope: API default would plan all 57 STABLE snapshots, so this execution created one bounded DB export plan for the known test object in bucket `test`: `codex-real-scan/20260809T173110Z-edge-VMware-Virtual-Platform-eaedeb0088e687bf-rustdesk-1.4.9-x86_64.exe`, size `24472432`.
- Export job: `1ebe6cec-6ad9-44ed-832d-39af95fef292`; controlled start returned HTTP 200, assigned one object / 24472432 bytes, worker_started_count=1, worker_failed_count=1.
- Result: job FAILED before object copy; export_object stayed ASSIGNED, copied_count=0, copied_bytes=0, no plaintext/ciphertext sha256, no data/meta paths. Disk stayed INITIALIZED, no manifest, no `.partial`.
- Error evidence: Edge journal logged `export disk worker failed` with `error_code=MANIFEST_INVALID`; Center journal had no panic/error/failed markers.
- Root cause evidence: service runs as `rustfs-transfer:rustfs-transfer`; target protocol dirs/files are owned by `fustfs-agent:fustfs-device` with dirs `755` and disk_info `644`, so service cannot write `disk_info.json` tmp/metadata under the target protocol root. No permission change or cleanup was performed.

### Real Edge export retry after target ACL grant: blocked by issued data_key binding

- Scope: Edge VM only; target `/dev/sdc1` mounted at `/media/edge/FUSTFS-TST-A`; protected `/dev/sdb1` FUSTFS-A-DRILL was identified and not written. No code, service user, global permission, or other disk permission changes.
- Target identity recheck: matched disk_id `9f8308ca-045e-425b-8664-4bb6fdaa0db3`, FS UUID `0878ee5b-3e86-4ae0-8d0f-461c2732ee42`, hardware SN `323533394352343033383235`, label `FUSTFS-TST-A`, ext4, status `INITIALIZED`, AES-GCM/HMAC metadata.
- ACL action: applied non-recursive POSIX ACL `user:rustfs-transfer:rwx` only to `/media/edge/FUSTFS-TST-A/rustfs-transfer` and child dirs `data`, `meta`, `manifests`, `logs`, `quarantine`, `quarantine/partial`; preserved existing owner/group `fustfs-agent:fustfs-device`. Mount root received no rustfs-transfer ACL; protected drill mount/root received no ACL.
- Rescan/verify: internal Edge rescan accepted; target runtime became READY; READY rows referred only to FUSTFS-TST-A.
- Export retry: preserved prior FAILED job `1ebe6cec-6ad9-44ed-832d-39af95fef292`; created new single-object job `ad99c91a-4049-4af2-ac3a-6a270a28cb0d` for bucket `test`, key `codex-real-scan/20260809T173110Z-edge-VMware-Virtual-Platform-eaedeb0088e687bf-rustdesk-1.4.9-x86_64.exe`, size `24472432`.
- Result: controlled start returned HTTP 500 `launch DiskWorker instances for assigned READY disks`; new job remained `COPYING` with object `ASSIGNED`, copied_count=0, copied_bytes=0. Disk remained `INITIALIZED`; no manifest; no `.partial`; hashes were not produced.
- Center key state: active key from Center reinitialize is now `ISSUED` to prior FAILED job `1ebe6cec-6ad9-44ed-832d-39af95fef292`; because the user required not reusing that FAILED job, a new job cannot legally receive the same data key without a state-machine recovery/reinitialize decision.
- Service health after failure: Edge `/healthz` 200 and `/readyz` 200; Center logs had no panic/error/failed markers in the checked window.

### Original FAILED job controlled recovery precheck: no legal service entry

- Scope: Edge VM readonly precheck for original failed job `1ebe6cec-6ad9-44ed-832d-39af95fef292`; no DB mutation, no retry/start call, no third job, no reinitialize, no protected disk write.
- Target identity: `/dev/sdc1` mounted at `/media/edge/FUSTFS-TST-A` matched disk_id `9f8308ca-045e-425b-8664-4bb6fdaa0db3`, FS UUID `0878ee5b-3e86-4ae0-8d0f-461c2732ee42`, hardware SN `323533394352343033383235`, label `FUSTFS-TST-A`, ext4.
- Protected disk: `/dev/sdb1` FUSTFS-A-DRILL identified separately and not touched.
- Disk precheck: status `INITIALIZED`, `sealed=false`, `imported=false`, `reusable=true`, no manifest, no `.partial`, security metadata still references data_key_id `1bcf5897-708c-4bb1-ba68-0a9f1386c502`.
- ACL precheck: `user:rustfs-transfer:rwx` exists only on target `/rustfs-transfer` protocol root and child write dirs; mount root has no rustfs-transfer ACL; protected FUSTFS-A-DRILL has no rustfs-transfer ACL.
- Original job precheck: job status `FAILED`, object_count=1, copied_count=0, copied_bytes=0, total_bytes=24472432, exactly one object in `ASSIGNED`, no plaintext/ciphertext hashes and no paths.
- Service/API gap: current Edge routes expose create/get/start only. `start` only assigns PENDING objects and launches workers for disks assigned in that call; it does not transition FAILED jobs for recovery and does not launch existing ASSIGNED objects. No recover/resume/retry route exists. Stopped without workaround because direct SQL state edits are prohibited.

### TASK-S1-P0-EXPORT-RECOVERY-001：Edge 失败导出任务受控零写入恢复合入

- 合入内容：
  - Edge 新增 `POST /api/edge/export-jobs/{export_job_id}/recover`，沿用 `X-Edge-Control-Token` 控制面鉴权，专门恢复原始 `FAILED` 导出任务，不创建新 job。
  - 恢复前校验 `export_job` 必须为 `FAILED`，`copied_count=0`、`copied_bytes=0`，且失败原因属于写盘前失败类别。
  - 恢复前校验所有 `export_object` 仍为 `ASSIGNED`，保留原 `disk_id` 分配，且无明文/密文哈希、数据路径、metadata 路径或 manifest 路径等写后字段。
  - 恢复前校验原盘最新运行态为 `READY`，盘内 `disk_info.json.status.code=INITIALIZED` 且 `disk_id` 一致，协议根下无 data/meta/manifest 文件和 `.partial` 残留。
  - 通过校验后将原 job 受控切回 `COPYING`，追加 `export_recovery_requested_at` 与 `recovery_reason` 审计记录，并启动既有生产 `DiskWorker` launcher 继续使用原 ASSIGNED 分配。
  - Center 增补同 job export-key 幂等测试，确认已签发 key 允许同一 `export_job_id` 重放，拒绝切换到不同 job。
- 冲突处理：
  - 手工保留第六轮 Dashboard summary/WS 路由、第七轮 HMAC 鉴权、第八轮 CenterSecurity、第九轮 Worker runtime 与 production launcher。
  - 第十轮未修改 `export_runtime.rs` 的 worker 复制逻辑；恢复入口复用第九轮已合入的 `export_job_id + disk_id` ASSIGNED 对象加载路径。
  - 未带入 `75de` 中与 recovery API / Edge control-export runtime / Center 同 job 幂等测试无关的历史 diff；未修改 `docs/v1.0冻结/`。
- 验证：
  - `cargo fmt --all -- --check`：首次发现 `crates/edge-backend/src/server.rs` import 换行格式差异；修正后通过。
  - `cargo test -p rustfs-transfer-edge --target-dir C:\Users\Beipy\.codex\visualizations\2026\08\09\019fe57e-f756-7b91-bbdb-92c59b9743e4\cargo-target`：通过。
  - `cargo test -p rustfs-transfer-center --target-dir C:\Users\Beipy\.codex\visualizations\2026\08\09\019fe57e-f756-7b91-bbdb-92c59b9743e4\cargo-target`：沙箱内 4 个初始化写盘测试因 `crates/center-backend/target/test-disks` ACL 拒绝访问失败；非沙箱重跑通过。
  - `cargo test --workspace --target-dir C:\Users\Beipy\.codex\visualizations\2026\08\09\019fe57e-f756-7b91-bbdb-92c59b9743e4\cargo-target`：非沙箱通过。
  - `powershell -ExecutionPolicy Bypass -File scripts\check-deploy.ps1`：通过。
  - `git diff --name-only -- docs/v1.0冻结`：无输出。
  - `rg "^(<<<<<<<|=======|>>>>>>>)"`：未发现冲突标记。

### Edge recover route deployment and original job recovery attempt

- Deployed latest `rustfs-transfer-edge` only to Edge VM; did not replace rescan helper, Center backend, env/token files, or frontend service.
- Build source was current workspace packaged without `.git`, `target`, `node_modules`, or `.local.env`; Linux release build ran on Center VM `/tmp` with rustup stable.
- Deployed Edge binary hash: `045eefc75d0616401d9e3d17bda1cda59c652dd527b2a4cb439a1e1617340227`; service restarted active as `rustfs-transfer:rustfs-transfer`.
- Smoke: Edge `/healthz` 200, `/readyz` 200. `POST /api/edge/export-jobs/{id}/recover` without control token returned 401 `UNAUTHORIZED`; no write action triggered.
- Pre-recover checks for original job `1ebe6cec-6ad9-44ed-832d-39af95fef292`: target `/dev/sdc1` FUSTFS-TST-A identity matched disk_id/FS UUID/SN/label/ext4; FUSTFS-A-DRILL identified separately and not touched; target ACL remained limited to protocol root/write dirs; disk remained `INITIALIZED`, no manifest, no `.partial`.
- Edge DB precheck: original job `FAILED`, object_count=1, copied_count=0, copied_bytes=0; sole object `ASSIGNED`, no hash/key/path/partial/error fields; latest target runtime READY. Center data_key precheck: key `1bcf5897-708c-4bb1-ba68-0a9f1386c502` was `ISSUED` and bound to the original job.
- Recover call: `POST /api/edge/export-jobs/1ebe6cec-6ad9-44ed-832d-39af95fef292/recover` with control token and recovery reason returned HTTP 409 `EXPORT_FAILURE_NOT_RECOVERABLE`.
- Cause: deployed recovery guard classifies write-before recoverability from `export_job.error_message`; the original job has generic message `one or more DiskWorker instances failed; disk was not sealed for failed workers`, which lacks accepted markers such as `diskworker`, `permission denied`, `access denied`, `os error 13`, `write-before`, or `acl`. Direct SQL mutation is prohibited, so stopped without workaround.

### TASK-S1-P0-EXPORT-RECOVERY-002：Edge 恢复失败根因可机读审计与旧任务确认合入

- 合入内容：
  - Edge `ExportWorkerLauncher` 聚合 `DiskWorker` 失败时，继续保留通用摘要，同时追加机器可读审计行：`export_failure_code`、`export_failure_stage`、`worker_error_code`、`worker_error_message`。
  - 权限类 `MANIFEST_INVALID` / IO 失败会标记为 `export_failure_code=WRITE_BEFORE_PERMISSION_DENIED`、`export_failure_stage=WRITE_BEFORE`，用于区分写前协议目录或 manifest 预写权限失败。
  - `RecoverExportJobRequest` 新增 `admin_confirm_write_before_zero_copy` 和 `write_before_failure_code`，用于已有旧 generic failed job 的受控管理员确认。
  - recover guard 优先读取持久化 marker；未知 generic worker 失败默认拒绝。
  - 旧 generic 失败只有在第十轮零写入证据全部通过，且管理员确认字段为 true 并给出白名单写前代码时才允许恢复。
- 冲突处理：
  - 手工保留第十轮恢复入口的 job/object/disk/protocol-root clean guard、Dashboard summary/WS、HMAC、CenterSecurity 和 Worker runtime。
  - 未带入 `75de` 中与 `export_runtime.rs`、`control.rs`、`server.rs` DTO 测试、TASKS/dev log 无关的历史 diff；未修改 `docs/v1.0冻结/`。
- 新增/更新测试：
  - `failure_audit_marks_manifest_permission_denied_as_write_before`。
  - `failure_audit_keeps_generic_summary_but_includes_machine_markers`。
  - `recovery_failure_classifier_allows_persisted_write_before_marker`。
  - `recovery_failure_classifier_rejects_unknown_generic_worker_failure`。
  - `recovery_failure_classifier_allows_legacy_generic_only_with_admin_whitelist`。
  - `recovery_failure_classifier_rejects_non_recoverable_marker_even_with_admin`。
  - Edge recover route 测试请求体覆盖管理员确认 DTO 字段。
- 验证：
  - `cargo fmt --all -- --check`：通过。
  - `cargo test -p rustfs-transfer-edge --target-dir C:\Users\Beipy\.codex\visualizations\2026\08\09\019fe57e-f756-7b91-bbdb-92c59b9743e4\cargo-target`：通过。
  - `cargo test -p rustfs-transfer-center --target-dir C:\Users\Beipy\.codex\visualizations\2026\08\09\019fe57e-f756-7b91-bbdb-92c59b9743e4\cargo-target`：沙箱内 4 个初始化写盘测试因 `crates/center-backend/target/test-disks` ACL 拒绝访问失败；非沙箱重跑通过。
  - `cargo test --workspace --target-dir C:\Users\Beipy\.codex\visualizations\2026\08\09\019fe57e-f756-7b91-bbdb-92c59b9743e4\cargo-target`：非沙箱通过。
  - `powershell -ExecutionPolicy Bypass -File scripts\check-deploy.ps1`：通过。
  - `git diff --name-only -- docs/v1.0冻结`：无输出。
  - `rg "^(<<<<<<<|=======|>>>>>>>)"`：未发现冲突标记。

### Edge v11 recover deployment and original zero-copy job SEALED

- Scope: deployed only latest `rustfs-transfer-edge` to Edge VM. Did not replace rescan helper, Center backend, env/token files, frontend service, or protected disk FUSTFS-A-DRILL.
- Build/deploy: Linux release build ran on Center VM `/tmp` from current workspace package excluding `.git`, `target`, `node_modules`, and `.local.env`. Installed Edge binary SHA256 `cad07e95fb43e268a003aa08ad3cfd4a0717ba5a8202d3e6c6eff834f12dd5ec`; service restarted active.
- Smoke: Edge `/healthz` 200, `/readyz` 200. Recover route without control token returned 401 `UNAUTHORIZED`.
- Pre-recover checks: target `/dev/sdc1` mounted `/media/edge/FUSTFS-TST-A` matched label, FS UUID, hardware SN, ext4, and disk_id `9f8308ca-045e-425b-8664-4bb6fdaa0db3`; protected `/dev/sdb1` FUSTFS-A-DRILL identified separately and not written. Disk status `INITIALIZED`, protocol version `1.0`, AES-GCM/HMAC security fields present, protocol data/meta/manifests files and `.partial` all zero. ACL remained limited to protocol root and write dirs; mount root had no `rustfs-transfer` ACL.
- Original job precheck: job `1ebe6cec-6ad9-44ed-832d-39af95fef292` was `FAILED`, copied_count=0, copied_bytes=0, object_count=1. Sole object was `ASSIGNED` to the target disk with no hash, key, path, partial, or error fields. Latest runtime was READY. Center data_key `1bcf5897-708c-4bb1-ba68-0a9f1386c502` was `ISSUED` and bound to the same job/disk/edge.
- Recover: called controlled `POST /api/edge/export-jobs/1ebe6cec-6ad9-44ed-832d-39af95fef292/recover` with recovery reason plus admin zero-copy confirmation and `WRITE_BEFORE_PERMISSION_DENIED`. API returned 200 and reused original assignments without creating a new job.
- Result: original job reached `SEALED`, copied_count=1, copied_bytes=24472432. Object status `EXPORTED`, plaintext SHA256 `eaedeb0088e687bf46f7c46a9c6ea5493ce51f3134dfd6acbedb47b5b9136274`, ciphertext SHA256 `0bda51746a7177226a51a1258e11500cb8fea43b70583b8d7dace2a61e27d6a1`, data/meta relative paths present, no `.partial`.
- Disk validation: target disk `disk_info.json` status became `SEALED` with sealed=true, imported=false, reusable=false, seal_id present, manifest object_count=1 and total_bytes=24472432. `export_manifest.json` and raw `export_manifest.sha256` matched SHA256 `10fc17ae7e79857381522ee16cdc8a1f71d133176b01af6332d46d99d477a1a2`.
- Post-checks: Edge health/ready stayed 200; latest runtime for target disk was `DONE` with no last_error and zero partial residue. Edge/Center journal grep in the recover window found no panic/error/fail/credential markers except system journal truncation warnings. One older pre-existing job remains `COPYING`/`ASSIGNED`; it was not created or touched in this recovery step.

### Center final import of FUSTFS-TST-A sealed disk

- Scope: Center VM only. Target `/dev/sdb1` mounted at `/media/control/FUSTFS-TST-A`; did not touch FUSTFS-A-DRILL, did not cleanup, did not reinitialize, did not delete or overwrite Edge/source RustFS objects.
- Precheck: target uniquely matched label `FUSTFS-TST-A`, FS UUID `0878ee5b-3e86-4ae0-8d0f-461c2732ee42`, hardware SN `323533394352343033383235`, ext4, and disk_id `9f8308ca-045e-425b-8664-4bb6fdaa0db3`. Protected disk was not present on Center.
- Disk protocol precheck: `disk_info.json` status was `SEALED` with sealed=true, imported=false, reusable=false. Center HMAC signature verification passed, manifest sha256 matched `10fc17ae7e79857381522ee16cdc8a1f71d133176b01af6332d46d99d477a1a2`, manifest export_job_id `1ebe6cec-6ad9-44ed-832d-39af95fef292`, seal_id `d17820a4-637c-4faf-9fba-856b74aeaddc`, object_count=1, total_bytes=24472432, `.partial` count=0.
- DB before import: no prior import_job for this export/disk; `object_ledger` total was 0. Data key `1bcf5897-708c-4bb1-ba68-0a9f1386c502` was `ISSUED` and bound to the export job.
- Import: called authorized `POST /api/center/import-jobs/start` with mount_path `/media/control/FUSTFS-TST-A`. Response HTTP 200: import_job_id `efc6117f-2bc2-4a8e-9c07-d74a2907a879`, import_job_status `DONE`, outcome `IMPORTED`, object_done=1, done_bytes=24472432.
- DB after import: `import_job` DONE with object_count/imported_count 1/1 and total/imported bytes 24472432/24472432. `object_ledger` total is 1 with source `test/codex-real-scan/20260809T173110Z-edge-VMware-Virtual-Platform-eaedeb0088e687bf-rustdesk-1.4.9-x86_64.exe`, import bucket `archive-edge-demo`, import key `test/codex-real-scan/20260809T173110Z-edge-VMware-Virtual-Platform-eaedeb0088e687bf-rustdesk-1.4.9-x86_64.exe`, plaintext SHA256 `eaedeb0088e687bf46f7c46a9c6ea5493ce51f3134dfd6acbedb47b5b9136274`, ciphertext SHA256 `0bda51746a7177226a51a1258e11500cb8fea43b70583b8d7dace2a61e27d6a1`.
- Archive verification: `mc stat` confirmed archive object exists in `archive-edge-demo` at the ledger import key, size 24472432, etag `3dcd83c485b9fbc53d7d56fb9d4585f0`.
- Disk after import: `disk_info.json` status is `IMPORTED`, sealed=true, imported=true, reusable=true, import_job_id `efc6117f-2bc2-4a8e-9c07-d74a2907a879`; imported disk_info HMAC signature verification passed; manifest hash still valid; `.partial` count=0.
- Service/log checks: Center service active; `/healthz` 200, `/readyz` 200, `/api/center/summary` shows the target disk `IMPORTED`/runtime `DONE`/import_job `DONE`. Journal grep in the import window found no panic/error/fail/credential markers except a system journal truncation warning.

### Center reinitialize request for FUSTFS-TST-A stopped: no deployed controlled API

- Scope: Center VM only. No cleanup, no reinitialize, no initialize, no manual filesystem delete, no manual SQL, no RustFS object/ledger/archive mutation, and no FUSTFS-A-DRILL access.
- User authorization was present, but execution was gated on a deployed controlled cleanup/reinitialize service path.
- Strict readonly precheck passed: target `/dev/sdb1` mounted `/media/control/FUSTFS-TST-A` uniquely matched label `FUSTFS-TST-A`, FS UUID `0878ee5b-3e86-4ae0-8d0f-461c2732ee42`, hardware SN `323533394352343033383235`, ext4, disk_id `9f8308ca-045e-425b-8664-4bb6fdaa0db3`. Protected FUSTFS-A-DRILL was not present on Center.
- Disk protocol precheck passed: `disk_info.json` status `IMPORTED`, sealed=true, imported=true, reusable=true; import_job_id `efc6117f-2bc2-4a8e-9c07-d74a2907a879`; export_job_id `1ebe6cec-6ad9-44ed-832d-39af95fef292`; seal_id `d17820a4-637c-4faf-9fba-856b74aeaddc`. Center HMAC signature valid; manifest sha256 valid `10fc17ae7e79857381522ee16cdc8a1f71d133176b01af6332d46d99d477a1a2`; `.partial` count=0.
- Protocol payload counts before cleanup: data=1, meta=1, manifests=2, logs=0, quarantine=0. These were not modified.
- Center DB readonly precheck: import_job DONE with 1/1 objects and 24472432/24472432 bytes; object_ledger total=1 and import ledger count=1; data_key statuses for the disk were ISSUED=1 and REVOKED=2. No sensitive key material was read or printed.
- Deployed route audit: POST probes without control token returned 404 for likely cleanup/reinitialize paths, including `/api/disk/reinitialize`, `/api/disk/cleanup`, `/api/center/reinitialize`, `/api/center/cleanup`, `/api/center/disks/reinitialize`, `/api/center/disks/cleanup`, and import-job scoped cleanup/reinitialize paths. `strings /opt/rustfs-transfer/rustfs-transfer-center` showed deployed routes for health/summary/register/initialize/verify/export-key/import start, but no cleanup/reinitialize route.
- Stop reason: no legal deployed Center service API exists for post-import cleanup/reinitialize. `/api/disk/initialize` is not safe for this request because it writes a new initialized disk_info/key but does not run the post-import cleanup state machine or clear data/meta/manifests. Stopped without workaround.
- Final readonly state: Center service active; `/healthz` 200, `/readyz` 200, `/api/center/summary` still reports the target disk `IMPORTED`, runtime `DONE`, import_job `DONE`, with message that the disk must be reinitialized before reuse. Journal grep found no new panic/error/fail/credential markers except a system journal truncation warning.
- TASKS.md was not updated because no task status changed and the requested lifecycle transition was not completed.

### TASK-S1-P1-CENTER-REINIT-API-001：Center 受控清理重初始化生产 API 合入

- 合入内容：
  - 新增 `POST /api/center/disks/{disk_id}/reinitialize`，继续使用 `X-Center-Control-Token`，请求必须包含 `mount_path`、`seal_id`、`expected_status_code=IMPORTED`、`operator_reason`、`confirm_reinitialize=true`。
  - 新增 Center `reinitialize_runtime` 生产服务层，写盘前校验 ext4、盘内 `IMPORTED`、disk/seal 身份、Center 签名验签、manifest + sha256 sidecar、`.partial=0`。
  - 生产 DB guard 使用同一查询确认 `import_job.status='DONE'`、`disk_list.status=TRUE`、盘内 old `data_key_id` 属于同盘且处于 `ISSUED`/`SEALED_READONLY`。
  - 复用 `PostImportReinitializer` 执行清理、协议目录重建、原子写 `disk_info.json`、新 key staging/activation、旧 key retire；`CLEANING`/`REINITIALIZING` 只作为运行态日志/响应语义。
  - `PostImportReinitializer` 增加 `.partial` 二次防线，且 completed import 必须匹配盘内 old `data_key_id`。
  - 新 data key 继续使用当前 Center `LOCAL-MASTER-KEY` 包裹，disk_info 继续使用 Center HMAC 签名验签；不记录明文 key。
  - 路由响应使用 `disk_status_code`、`runtime_status`，无裸 `status`。
- 冲突处理：
  - 手工保留当前主线 Dashboard summary/WS、Edge HMAC 强制鉴权、CenterSecurity 真实 key wrap/signature、ImportWorker 受控入口和 Worker runtime。
  - 未带入源 worktree 中旧 mock 安全常量、旧 main 接线、无关 Edge/Web/Deploy 历史 diff；未修改 `docs/v1.0冻结/`。
- 测试覆盖：
  - token 缺失拒绝且不触发 reinitialize service。
  - 合法请求触发受控 service，响应无裸 `status`。
  - 非 `IMPORTED`、签名缺失/验签失败、manifest sha256 不匹配、`.partial` 残留、非 ext4、其他 disk_id 均拒绝且不清理。
  - 成功路径清理 `data/`、`meta/`、`manifests/`，创建空协议目录，盘内状态变 `INITIALIZED`。
  - 成功后新 key 激活、旧 key retire；old key 不匹配 DONE import 时拒绝。
- 验证：
  - `cargo fmt --all -- --check`：通过。
  - `cargo test -p rustfs-transfer-center`：通过。
  - `cargo test --workspace`：通过。
  - `powershell -ExecutionPolicy Bypass -File scripts\check-deploy.ps1`：通过。
  - `git diff --check`：通过。
  - `git diff --name-only -- docs/v1.0冻结`：无输出。
  - `rg "^(<<<<<<<|=======|>>>>>>>)" .`：未发现冲突标记。
- 未覆盖项：
  - 真实 VM 尚未部署本提交，未对 `FUSTFS-TST-A` 执行 cleanup/reinitialize 验收；不得误称该盘已完成重新初始化。
  - 后续部署窗口只能在只读确认 ext4、挂载点、disk_id、seal_id、manifest sha256、`.partial=0`、Center DB DONE import 与 old data_key 绑定后，调用受控 API。

### Center reinitialize API deployment and blocked controlled cleanup for FUSTFS-TST-A

- Scope: Center VM and target disk only. Did not touch Edge, FUSTFS-A-DRILL, RustFS source objects, Center archive objects, or object_ledger. No direct format/rm/manual SQL/initialize bypass was performed.
- Deployed Center backend from commit `76f6b9c764e1d07826433a67a43678b660cf18eb` only. Built release on Center `/tmp` with rustc/cargo 1.97.1 and installed `/opt/rustfs-transfer/rustfs-transfer-center` atomically after backup. New binary SHA256 `63f697d58424e2f08f330fd347071ddc7cb99e20f6ac6a2bcfd8d374fd0df214`; previous binary SHA256 `2db6577f47f0a3d6d6d224bac15437144d38bf5a9b9ce6e03f327685cf68ca71`.
- Config precheck: Center control token env, LOCAL_MASTER_KEY env, and CENTER_SIGNATURE_KEY env keys were present and non-placeholder; values were not printed.
- Service smoke: `rustfs-transfer-center` active; `/healthz` 200, `/readyz` 200, `/api/center/summary` 200. New `POST /api/center/disks/{disk_id}/reinitialize` without token returned an auth rejection (`INVALID_REQUEST`, token missing/invalid); no write was triggered.
- Stage B readonly disk precheck passed: target `/dev/sdb1` mounted `/media/control/FUSTFS-TST-A` matched label `FUSTFS-TST-A`, FS UUID `0878ee5b-3e86-4ae0-8d0f-461c2732ee42`, hardware SN `323533394352343033383235`, ext4, and disk_id `9f8308ca-045e-425b-8664-4bb6fdaa0db3`; protected FUSTFS-A-DRILL was not present on Center.
- Stage B protocol precheck passed: disk status `IMPORTED`, sealed=true, imported=true, reusable=true; old data_key_id `1bcf5897-708c-4bb1-ba68-0a9f1386c502`; center HMAC signature valid; manifest sha256 valid `10fc17ae7e79857381522ee16cdc8a1f71d133176b01af6332d46d99d477a1a2`; export_job `1ebe6cec-6ad9-44ed-832d-39af95fef292`; seal_id `d17820a4-637c-4faf-9fba-856b74aeaddc`; `.partial` count 0; protocol file counts before cleanup were data=1, meta=1, manifests=2, logs=0, quarantine=0.
- Stage B DB precheck passed: import_job `efc6117f-2bc2-4a8e-9c07-d74a2907a879` DONE with 1/1 objects and 24472432 bytes; object_ledger total=1; old data_key status ISSUED, bound to disk/export job; two historical keys remain REVOKED.
- Stage C controlled API attempt: called authorized `POST /api/center/disks/9f8308ca-045e-425b-8664-4bb6fdaa0db3/reinitialize` with mount_path `/media/control/FUSTFS-TST-A`, seal_id `d17820a4-637c-4faf-9fba-856b74aeaddc`, expected_status_code `IMPORTED`, confirm_reinitialize=true, and operator reason. It returned HTTP 400 `filesystem unsupported for cleanup/reinitialize: expected ext4, got ext4\next4`. A second attempt after readonly host/service-user `findmnt` checks returned the same 400.
- Blocker evidence: host and `sudo -u rustfs-transfer` `findmnt -no FSTYPE --target /media/control/FUSTFS-TST-A` each returned single `ext4`, but inside the running service mount namespace (`nsenter -t <MainPID> -m`) the same command returned `ext4;ext4;`. The deployed route compares the entire stdout string to `ext4`, so it rejects before the cleanup/reinitialize worker writes.
- Post-failure readonly checks: disk_info still `IMPORTED`; protocol file counts still data=1, meta=1, manifests=2, `.partial`=0. DB still import_job DONE, ledger total=1, old key ISSUED and not retired; no new active key was created. Archive object still exists in `archive-edge-demo` at the ledger import key, size 24472432.
- Final service/log checks: Center service active; health/ready/summary 200. Journal grep found no new panic/error/fail/credential markers except the recurring system journal truncation warning. TASKS.md was not updated because the lifecycle transition did not complete.

### TASK-S1-P1-CENTER-REINIT-FSTYPE-002：Center reinitialize findmnt 多行 ext4 解析修补合入

- 合入范围：仅 `crates/center-backend/src/reinitialize_runtime.rs`、`work_sprint/TASKS.md`、`work_sprint/dev_log/2026-08-10.md`；未带入源 worktree 其它历史 diff，未修改 `docs/v1.0冻结/`，未触碰 Edge 前端未归属删除。
- 现场依据：真实 Center VM 中服务 mount namespace 下 `findmnt -no FSTYPE --target /media/control/FUSTFS-TST-A` 返回重复 `ext4` 行；此前两次授权 reinitialize API 调用均在写盘前 HTTP 400 安全拒绝，运输盘、DB、archive、object_ledger、data_key 均保持零写入变化。
- 实现内容：新增纯解析函数，将 `findmnt` stdout 按空白拆分；非空且所有 token 都为 `ext4` 时规范化为单个 `ext4`；空输出、混合文件系统、任一非 `ext4`、命令失败仍拒绝。
- 安全边界：ext4 guard 未放宽为忽略异常；解析失败仍发生在 `validate_reinitialize_preflight` 和 `PostImportReinitializer` 写盘前。
- 验证：
  - `cargo fmt --all -- --check`：通过。
  - `cargo test -p rustfs-transfer-center`：通过。
  - `cargo test --workspace`：通过。
  - `powershell -ExecutionPolicy Bypass -File scripts\check-deploy.ps1`：通过。
  - `git diff --check`：通过。
  - `git diff --name-only -- docs/v1.0冻结`：无输出。
  - `rg "^(<<<<<<<|=======|>>>>>>>)" .`：未发现冲突标记。
- 未覆盖项：真实 VM 尚未部署或验收该解析补丁；不得宣称 `FUSTFS-TST-A` 已完成 cleanup/reinitialize。后续部署窗口仍需先做只读身份、ext4、manifest sha、`.partial=0`、Center DB DONE import 与 old data_key 绑定检查，再调用受控 API。

### Center ext4 parser hotfix deployed; FUSTFS-TST-A reinitialize blocked by legacy disk_info schema

- Scope: Center VM and target disk only. Did not touch Edge, FUSTFS-A-DRILL, RustFS source objects, Center archive objects, or object_ledger. No direct format/rm/manual SQL/direct initialize bypass was performed.
- Deployed Center backend from commit `59509ef6b78465a66b7147d73f99afe3a09318e4` only. Built release on Center `/tmp` with rustc/cargo 1.97.1 and installed `/opt/rustfs-transfer/rustfs-transfer-center` atomically after backup. New binary SHA256 `fbb9c08dc133b884ba2dbd15b7b43ba498f587ece923062b32ea4201b2250e19`; previous binary SHA256 `63f697d58424e2f08f330fd347071ddc7cb99e20f6ac6a2bcfd8d374fd0df214`.
- Service smoke: `rustfs-transfer-center` active; `/healthz` 200, `/readyz` 200, `/api/center/summary` 200. New reinitialize route without token returned auth rejection (`INVALID_REQUEST`, token missing/invalid); no write triggered.
- Readonly precheck passed: target `/dev/sdb1` mounted `/media/control/FUSTFS-TST-A` matched label `FUSTFS-TST-A`, FS UUID `0878ee5b-3e86-4ae0-8d0f-461c2732ee42`, hardware SN `323533394352343033383235`, ext4, and disk_id `9f8308ca-045e-425b-8664-4bb6fdaa0db3`; FUSTFS-A-DRILL was not present on Center.
- Protocol precheck passed: disk status `IMPORTED`, sealed=true, imported=true, reusable=true; old data_key_id `1bcf5897-708c-4bb1-ba68-0a9f1386c502`; center HMAC signature valid; manifest sha256 valid `10fc17ae7e79857381522ee16cdc8a1f71d133176b01af6332d46d99d477a1a2`; export_job `1ebe6cec-6ad9-44ed-832d-39af95fef292`; seal_id `d17820a4-637c-4faf-9fba-856b74aeaddc`; `.partial` count 0; pre-cleanup file counts data=1, meta=1, manifests=2, logs=0, quarantine=0.
- DB precheck passed: import_job `efc6117f-2bc2-4a8e-9c07-d74a2907a879` DONE with 1/1 objects and 24472432 bytes; object_ledger total=1; old data_key status ISSUED and bound to disk/export job; two historical keys remain REVOKED.
- Controlled API call: authorized `POST /api/center/disks/9f8308ca-045e-425b-8664-4bb6fdaa0db3/reinitialize` with mount_path `/media/control/FUSTFS-TST-A`, seal_id `d17820a4-637c-4faf-9fba-856b74aeaddc`, expected_status_code `IMPORTED`, confirm_reinitialize=true, and the same audit reason returned HTTP 400: `json error: missing field updated_at at line 44 column 1`.
- Stop reason: hotfix resolved the repeated ext4 parser path, but the current real disk_info was produced by the previous import path without top-level `updated_at`; the new reinitializer deserializer requires `updated_at`, so it rejects before cleanup/reinitialize writes.
- Post-failure readonly checks: disk_info still `IMPORTED`; top-level `updated_at` absent; protocol counts remain data=1, meta=1, manifests=2, `.partial`=0. DB still import_job DONE, ledger total=1, old key ISSUED and not retired; no new active key was created. Center service health/ready/summary remained 200. Journal grep found no new panic/error/fail/credential markers except the recurring system journal truncation warning.
- Temporary Center `/tmp` build/source artifacts and local archive were removed. TASKS.md was not updated because the lifecycle transition did not complete.

### TASK-S1-P1-CENTER-REINIT-UPDATED-AT-003：Center reinitialize 兼容旧盘缺 updated_at 热修补合入

- 合入范围：仅 `crates/center-backend/src/reinitializer.rs`、`crates/center-backend/src/reinitialize_runtime.rs`、`crates/center-backend/src/import_worker.rs`、`crates/center-backend/tests/import_worker.rs`、`work_sprint/TASKS.md`、`work_sprint/dev_log/2026-08-10.md`；严格排除用户并发 Edge 前端改动，未修改 `docs/v1.0冻结/`。
- 现场依据：部署 `59509ef6b78465a66b7147d73f99afe3a09318e4` 后重复 `ext4` 解析已越过，但真实旧 `FUSTFS-TST-A` 盘因顶层 `updated_at` 缺失在 JSON 反序列化层 HTTP 400；该调用已核验在写盘前拒绝，运输盘、DB、archive、object_ledger、data_key 均零写入变化。
- 实现内容：
  - `read_disk_info` 仅在盘内 `status.code=IMPORTED` 且缺少顶层 `updated_at` 时补 Unix epoch sentinel，作为明确可审计的旧盘兼容语义。
  - 空字符串、非法 timestamp、缺少 `security` 等关键字段、非 `IMPORTED` 生命周期缺少 `updated_at` 仍拒绝。
  - 成功 reinitialize 写回 `INITIALIZED` disk_info 仍必须写当前标准 `updated_at`。
  - `ImportWorker` 成功标记 `IMPORTED` 时同步写顶层 `updated_at`，避免继续产生旧格式导入盘。
- 安全边界：兼容只发生在旧 `IMPORTED` 盘读取层；签名、manifest、状态、identity、partial、ext4、DB key/job guard 均未放宽；拒绝路径仍在 `PostImportReinitializer` 写盘前。
- 验证：
  - `cargo fmt --all -- --check`：通过。
  - `cargo test -p rustfs-transfer-center`：通过。
  - `cargo test --workspace`：通过。
  - `powershell -ExecutionPolicy Bypass -File scripts\check-deploy.ps1`：通过。
  - `git diff --check`：通过。
  - `git diff --name-only -- docs/v1.0冻结`：无输出。
  - `rg "^(<<<<<<<|=======|>>>>>>>)"`：未发现冲突标记。
- 未覆盖项：该 `updated_at` 兼容补丁尚未部署或真实 VM 验收；不得宣称 `FUSTFS-TST-A` 已完成 cleanup/reinitialize。后续部署窗口唯一安全路径是部署 Center 后端新二进制后，重新做只读身份、ext4、manifest sha、`.partial=0`、Center DB DONE import 与 old data_key 绑定检查，再调用受控 reinitialize API；不得手工 SQL、手工改盘或绕过 API。

### Center updated_at hotfix deployed; FUSTFS-TST-A reinitialize blocked by disk_info signature guard

- Scope: Center VM and FUSTFS-TST-A only. Did not touch Edge, FUSTFS-A-DRILL, source RustFS objects, Center archive objects, or object_ledger. No direct format/rm/manual SQL/direct initialize bypass was performed.
- Deployed Center backend from commit `56564ee96a54a4e25ba0579e3811e79d41a52862` only. Built release on Center `/tmp` with rustc/cargo 1.97.1 and installed `/opt/rustfs-transfer/rustfs-transfer-center` atomically after backup. New binary SHA256 `da956ece8455d7b5195e304b03520641aacfef498304cc9ec2efa4d9cadc6dc5`; previous binary SHA256 `fbb9c08dc133b884ba2dbd15b7b43ba498f587ece923062b32ea4201b2250e19`.
- Service smoke: `rustfs-transfer-center` active; `/healthz` 200, `/readyz` 200, `/api/center/summary` 200. Reinitialize route without token returned auth rejection (`INVALID_REQUEST`, token missing/invalid); no write triggered.
- Readonly precheck passed: target `/dev/sdb1` mounted `/media/control/FUSTFS-TST-A` uniquely matched label `FUSTFS-TST-A`, FS UUID `0878ee5b-3e86-4ae0-8d0f-461c2732ee42`, hardware SN `323533394352343033383235`, ext4, and disk_id `9f8308ca-045e-425b-8664-4bb6fdaa0db3`. FUSTFS-A-DRILL was not present on Center.
- Protocol/DB precheck passed: disk status `IMPORTED`; manifest sha256 `10fc17ae7e79857381522ee16cdc8a1f71d133176b01af6332d46d99d477a1a2`; `.partial` count 0; protocol file counts data=1, meta=1, manifests=2. Import job `efc6117f-2bc2-4a8e-9c07-d74a2907a879` remained DONE with 1/1 objects and 24472432 bytes; object_ledger total=1; old data_key `1bcf5897-708c-4bb1-ba68-0a9f1386c502` remained ISSUED and bound to the disk/export job.
- Controlled API call: authorized `POST /api/center/disks/9f8308ca-045e-425b-8664-4bb6fdaa0db3/reinitialize` with mount_path `/media/control/FUSTFS-TST-A`, seal_id `d17820a4-637c-4faf-9fba-856b74aeaddc`, expected_status_code `IMPORTED`, confirm_reinitialize=true, and the same audit reason returned HTTP 400: `disk_info center_signature verification failed`.
- Stop reason: the deployed service guard rejected disk_info signature verification before cleanup/reinitialize writes. No workaround was attempted because direct disk edits, direct SQL, direct initialize, and bypassing the controlled state machine are prohibited.
- Post-failure readonly checks confirmed zero write: disk_info still `IMPORTED`; data/meta/manifests counts unchanged; `.partial` still 0; DB still import_job DONE, ledger total=1, old key ISSUED, and no new ACTIVE key. Center service health/ready/summary remained 200; journal grep found no panic/error/fail/credential markers except the recurring system journal truncation warning.
- TASKS.md was not updated because the lifecycle transition did not complete.

### TASK-S1-P1-CENTER-REINIT-SIGNATURE-004：Center reinitialize 兼容旧 IMPORTED 签名 canonical 热修补合入

- 合入范围：仅 `crates/center-backend/src/reinitializer.rs`、`crates/center-backend/src/reinitialize_runtime.rs`、`crates/center-backend/src/lib.rs`、`work_sprint/TASKS.md`、`work_sprint/dev_log/2026-08-10.md`；未触碰用户并发 Edge 前端改动、未跟踪设计目录或 `docs/v1.0冻结/`。
- 现场依据：部署 `56564ee96a54a4e25ba0579e3811e79d41a52862` 后，真实 `FUSTFS-TST-A` 盘的身份、ext4、manifest、partial、import_job、ledger、old key 前置均通过，但受控 reinitialize 在写盘前因 `disk_info center_signature verification failed` 返回 HTTP 400；已核验运输盘、DB、archive、object_ledger、data_key 均零写入变化。
- 根因与实现：
  - 当前 Center HMAC canonical 会覆盖 `protocol`、`disk`、`center`、`security.center_key_id/data_key_id/signature_alg`。
  - 旧导入盘的盘上原始 JSON 保留了历史字段形态；读取成 `reinitializer::DiskInfo` 后会丢弃部分未知字段，直接用反序列化后的结构重建 canonical 会改变签名输入。
  - `read_disk_info_document` 现在同时返回反序列化业务结构、盘上原始 JSON、以及顶层 `updated_at` 是否真实存在。
  - reinitialize preflight 先按当前业务结构做当前 canonical HMAC 验签；若失败，只在原始 JSON 为 `IMPORTED` 且顶层 `updated_at` 缺失时，用盘上原始 JSON 和同一 Center HMAC key 再验一次。
- 安全边界：
  - 没有“验签失败也放行”；当前或历史 canonical 必须至少一个真实 HMAC 验签成功。
  - 无签名、错误签名、错 key、带 `updated_at` 的历史签名、非 `IMPORTED` 历史形态均拒绝。
  - 签名通过后仍继续执行 manifest、identity、partial、ext4、DB key/job guard；拒绝路径仍在 `PostImportReinitializer` 写盘前。
  - 新 initialize 写出的 `disk_info.json` 继续写 `updated_at` 和当前 canonical HMAC 签名，且测试覆盖不写 `mock-signature-adapter`。
- 验证：
  - `cargo fmt --all -- --check`：通过。
  - `cargo test -p rustfs-transfer-center`：通过。
  - `cargo test --workspace`：通过。
  - `powershell -ExecutionPolicy Bypass -File scripts\check-deploy.ps1`：通过。
  - `git diff --check`：通过。
  - `git diff --name-only -- docs/v1.0冻结`：无输出。
  - `rg "^(<<<<<<<|=======|>>>>>>>)"`：未发现冲突标记。
  - staged diff 敏感占位扫描：无命中。
- 未覆盖项：该旧签名兼容补丁尚未部署或真实 VM 验收；不得宣称 `FUSTFS-TST-A` 已完成 cleanup/reinitialize。后续部署窗口唯一安全步骤是只更新 Center 后端二进制，重新做只读身份、ext4、manifest sha、`.partial=0`、Center DB DONE import 与 old data_key 绑定检查，再调用受控 reinitialize API；不得手工 SQL、手工改盘或绕过 API。
### Center legacy signature hotfix deployed; FUSTFS-TST-A reinitialize blocked by inner reinitializer signature guard

- Scope: Center VM and FUSTFS-TST-A only. Did not touch Edge, frontend, FUSTFS-A-DRILL, source RustFS objects, Center archive objects, or object_ledger. No direct format/rm/manual SQL/manual disk edit/direct initialize bypass was performed.
- Deployed Center backend from commit `ab44d201e2150ee861ebb3aabac770f5c190b163` only. Built release on Center `/tmp` with `/home/control/.cargo/bin` rustc/cargo 1.97.1 and installed `/opt/rustfs-transfer/rustfs-transfer-center` atomically after backup. New binary SHA256 `b3ebfeb974824515da9158655ed21ec16b6d3cd4bd359773e9430341103732e4`; previous binary SHA256 `da956ece8455d7b5195e304b03520641aacfef498304cc9ec2efa4d9cadc6dc5`.
- Service smoke: `rustfs-transfer-center` active; `/healthz` 200, `/readyz` 200, `/api/center/summary` 200. Reinitialize route with valid JSON but without token returned auth rejection (`INVALID_REQUEST`, token missing/invalid); no write triggered.
- Readonly gate passed: target `/dev/sdb1` mounted `/media/control/FUSTFS-TST-A` uniquely matched label `FUSTFS-TST-A`, FS UUID `0878ee5b-3e86-4ae0-8d0f-461c2732ee42`, hardware SN `323533394352343033383235`, ext4, and disk_id `9f8308ca-045e-425b-8664-4bb6fdaa0db3`. FUSTFS-A-DRILL was not present on Center.
- Protocol/DB gate passed: disk status `IMPORTED`; legacy raw `center_signature` verified; manifest sha256 `10fc17ae7e79857381522ee16cdc8a1f71d133176b01af6332d46d99d477a1a2`; `.partial` count 0; protocol file counts data=1, meta=1, manifests=2. Import job `efc6117f-2bc2-4a8e-9c07-d74a2907a879` remained DONE with 1/1 objects and 24472432 bytes; object_ledger total=1; old data_key `1bcf5897-708c-4bb1-ba68-0a9f1386c502` remained ISSUED and bound to the disk/export job.
- Controlled API call: authorized `POST /api/center/disks/9f8308ca-045e-425b-8664-4bb6fdaa0db3/reinitialize` with mount_path `/media/control/FUSTFS-TST-A`, seal_id `d17820a4-637c-4faf-9fba-856b74aeaddc`, expected_status_code `IMPORTED`, confirm_reinitialize=true, and the same audit reason returned HTTP 400: `disk_info signature invalid: disk_info center_signature verification failed`.
- Stop reason: the outer reinitialize preflight accepts the legacy raw signature, but `PostImportReinitializer::reinitialize_imported_disk` still calls current-only `verify_disk_info(previous_disk_info)` after injecting the legacy `updated_at` sentinel, so it rejects before cleanup/reinitialize writes. No workaround was attempted.
- Post-failure readonly checks confirmed zero write: disk_info still `IMPORTED`; data/meta/manifests counts unchanged; `.partial` still 0; DB still import_job DONE, ledger total=1, old key ISSUED, and no new ACTIVE key. Center health/ready/summary remained 200; summary still reports disk `IMPORTED`, runtime `DONE`, import_job `DONE`. Journal grep found no panic/error/fail/credential markers except the recurring system journal truncation warning.
- TASKS.md was not updated because the lifecycle transition did not complete.

### TASK-S1-P1-CENTER-REINIT-SIGNATURE-005：Center reinitialize 统一内外层 center_signature 验签热修补合入

- 合入范围：
  - 仅 `crates/center-backend/src/reinitializer.rs`、`crates/center-backend/src/reinitialize_runtime.rs`、`work_sprint/TASKS.md`、`work_sprint/dev_log/2026-08-10.md`。
  - 保留用户并发 Edge 前端改动与未跟踪原型设计目录；未修改 `docs/v1.0冻结/`，未操作 VM、硬盘、部署或 push。
- 现场依据：
  - 真实 VM 第四次部署 `ab44d201e2150ee861ebb3aabac770f5c190b163` 后，外层 reinitialize preflight 已按旧盘 raw canonical 验证 `FUSTFS-TST-A` 的缺 `updated_at` `IMPORTED` 盘。
  - 授权受控 API 仍返回 HTTP 400：`disk_info signature invalid: disk_info center_signature verification failed`。
  - 只读复核确认零写入：盘内仍为 `IMPORTED`，data/meta/manifests 计数不变，`.partial=0`，DB import_job 仍 DONE，ledger total=1，old key 仍 ISSUED，无新 ACTIVE key。
- 根因与实现：
  - 根因是外层 preflight 与 `PostImportReinitializer::reinitialize_imported_disk` 内层使用了不同签名输入语义：外层能验证 raw JSON 历史 canonical，内层重新读取补值后的 `DiskInfo` 后只做 current-only 验签。
  - `validate_reinitialize_preflight` 现在返回同一份 `DiskInfoDocument`，包含业务结构、盘上 raw JSON 和 top-level `updated_at` 是否存在。
  - `PostImportReinitializer` 新增 `reinitialize_imported_disk_from_document`，runtime 将同一 raw-context document 传入核心层。
  - 外层和核心层都调用同一个 `validate_center_signature_for_reinitialize`，不通过裸 `verified=true` 跳过核心验签。
- 安全边界：
  - 当前 canonical 和旧 `IMPORTED`/缺 top-level `updated_at` raw canonical 都必须通过真实 Center HMAC 验签。
  - 无签名、错误签名、错 key、非 `IMPORTED`、带 `updated_at` 的历史签名仍在写入前拒绝。
  - 核心层仍保留 identity、`IMPORTED`、seal、DONE import、old data key、`.partial=0`、key staging/activation/retire 等防线。
- 验证：
  - `cargo fmt --all -- --check`：通过。
  - `cargo test -p rustfs-transfer-center`：通过。
  - `cargo test --workspace`：通过。
  - `powershell -ExecutionPolicy Bypass -File scripts\check-deploy.ps1`：通过。
  - `git diff --check`：通过。
  - `git diff --name-only -- docs/v1.0冻结`：无输出。
  - `rg "^(<<<<<<<|=======|>>>>>>>)"`：未发现冲突标记。
  - staged diff 敏感占位扫描：无命中。
- 未覆盖项：
  - 本轮内外验签一致性补丁尚未部署或真实 VM 验收；不得宣称 `FUSTFS-TST-A` 已完成 cleanup/reinitialize。
  - 后续部署窗口唯一安全步骤是只更新 Center 后端二进制，重新做只读身份、ext4、manifest sha、`.partial=0`、Center DB DONE import 与 old data_key 绑定检查，再调用受控 reinitialize API；不得手工 SQL、手工改盘、直接 initialize 或绕过 API。

### TASK-S1-P1-CENTER-REINIT-ADMISSION-006：Center reinitialize 最小生命周期准入热修补合入

- 合入范围：
  - 仅 `crates/center-backend/src/reinitializer.rs`、`crates/center-backend/src/reinitialize_runtime.rs`、`work_sprint/TASKS.md`、`work_sprint/dev_log/2026-08-10.md`。
  - 保留用户并发 Edge 前端改动与未跟踪原型设计目录；未修改 `docs/v1.0冻结/`，未操作 VM、硬盘、部署、SQL 或 push。
- 用户确认的最小准入边界：
  - cleanup/reinitialize 生命周期准入仅保留 Center 已登记且启用目标盘身份、ext4、`.partial` 恢复检查、盘内 `disk_info.status.code=IMPORTED`、真实 `center_signature` 验签。
  - Center 登记身份以 `disk_id` 为主，SN 仅作辅助核验；登记 SN 与盘内 SN 同时存在且不一致时写入前拒绝。
  - manifest、object_ledger、import_job DONE、old data_key 绑定不再作为重复启动门槛；本轮不修改 archive/source/object_ledger。
- 实现内容：
  - `PostImportRepository` 从 `completed_import` 准入查询改为 `registered_disk` 查询；生产 PG 查询仅确认 `disk_list.disk_id` 存在且 `status=TRUE`，并返回登记 SN。
  - runtime preflight 移除 manifest/sha256 sidecar 校验，只保留 ext4、盘内身份/状态/seal、真实签名和 `.partial=0`。
  - 核心 reinitializer 在清理前再次校验同一 `DiskInfoDocument` 的真实签名、盘内 `IMPORTED`、disk/seal 与 Center 登记盘身份。
  - 新 key 激活拆成必须成功的独立步骤；旧 key 退役失败或缺失只记录 warn，不回滚已成功的清理重初始化。
- 验证：
  - `cargo fmt --all -- --check`：通过。
  - `cargo test -p rustfs-transfer-center`：通过。
  - `cargo test --workspace`：通过。
  - `powershell -ExecutionPolicy Bypass -File scripts\check-deploy.ps1`：通过。
  - `git diff --check`：通过。
  - `git diff --name-only -- docs/v1.0冻结`：无输出。
  - `rg "^(<<<<<<<|=======|>>>>>>>)"`：未发现冲突标记。
  - staged diff 敏感占位扫描：无命中。
- 未覆盖项：
  - 该最小准入补丁尚未部署或真实 VM 验收；不得宣称 `FUSTFS-TST-A` 已完成 cleanup/reinitialize。
  - 后续部署窗口唯一安全步骤是只更新 Center 后端二进制，重新做只读目标盘身份、ext4、`.partial=0`、盘内 `IMPORTED`、真实 `center_signature` 与 Center 登记启用状态检查，再调用受控 reinitialize API；不得手工 SQL、手工改盘、直接 initialize 或绕过 API。
### Center minimal-gate reinitialize attempt for FUSTFS-TST-A stopped by protocol data permission

- Scope: Center VM and FUSTFS-TST-A only. Did not touch Edge, FUSTFS-A-DRILL, source RustFS objects, Center archive objects, or object_ledger intentionally. No direct SQL, format, rm, manual disk edit, or direct initialize bypass was performed.
- Deployed Center backend from commit `bdb1ad3819b0eacfcb20f3b9c7ee3f763aefdf23`. Built release on Center `/tmp` with rustc/cargo 1.97.1. Installed `/opt/rustfs-transfer/rustfs-transfer-center` atomically after backup. New binary SHA256 `e83149d295da79ea8b45940063b5b32bb301da73a11677f8400aa43566d2ad09`; previous binary SHA256 `c1cee5cc9f0d07945ca00a5c1c62d4c8ab3a480fa96e4ea46d425f8d7a58049e`.
- Minimal readonly gate passed before API call: target `/dev/sdb1` mounted `/media/control/FUSTFS-TST-A` uniquely matched label `FUSTFS-TST-A`, FS UUID `0878ee5b-3e86-4ae0-8d0f-461c2732ee42`, hardware SN `323533394352343033383235`, ext4, disk_id `9f8308ca-045e-425b-8664-4bb6fdaa0db3`; Center disk row was registered/enabled; disk_info status `IMPORTED`; legacy raw center_signature verified; `.partial` count 0. FUSTFS-A-DRILL was not present on Center.
- Per updated architecture instruction, manifest/object_ledger/import_job/old key binding were not used as admission blockers. They were only read as audit snapshots.
- Controlled API call: authorized `POST /api/center/disks/9f8308ca-045e-425b-8664-4bb6fdaa0db3/reinitialize` returned HTTP 400 `remove /media/control/FUSTFS-TST-A/rustfs-transfer/data: Permission denied (os error 13)`.
- Actual write scope observed after failure: `disk_info.json` remains `IMPORTED` but now contains top-level `updated_at=1970-01-01T00:00:00Z`; because the old signature was retained, current read-side verification reports signature invalid. Protocol payload counts remained data=1, meta=1, manifests=2; `.partial` remained 0. DB snapshot remained ledger_total=1, old key `ISSUED`, no ACTIVE key. Center service health/ready/summary stayed 200.
- Directory audit: `/media/control/FUSTFS-TST-A/rustfs-transfer` and `data` are owned by `rustfs-transfer:rustfs-transfer`, but API still received OS error 13 while removing the protocol data directory. No permission repair was attempted in this window.
- TASKS.md was not updated because cleanup/reinitialize did not complete.

### TASK-S1-CENTER-HOTFIX-004：Center reinitialize disk_info 失败原子性修补合入

- 合入范围：
  - 仅 `crates/center-backend/src/reinitializer.rs`、`crates/center-backend/src/reinitialize_runtime.rs`、`work_sprint/TASKS.md`、`work_sprint/dev_log/2026-08-10.md`。
  - 保留用户并发 Edge 前端改动 `web/edge-web/src/views/DashboardView.vue` 与未跟踪原型设计目录；未修改 `docs/v1.0冻结/`，未操作 VM、硬盘、部署、SQL 或 push。
- 现场依据：
  - 真实 VM 在最小准入版本 `bdb1ad3819b0eacfcb20f3b9c7ee3f763aefdf23` 上已证明：受控 reinitialize 在清理 `data` 目录权限拒绝前，把 legacy 缺 `updated_at` 的旧盘内存 sentinel 写入了盘内 `disk_info.json`，导致盘仍为 `IMPORTED` 但当前签名失效。
  - 该真实 VM 失败尚未用本补丁重新部署复验；不得宣称 `FUSTFS-TST-A` 已完成 cleanup/reinitialize。
- 实现内容：
  - reinitialize 前置解析、legacy `updated_at` 补值和签名兼容均保持在内存中；失败路径不再把 `previous_disk_info` 写回盘。
  - 清理失败或 key 激活失败时，盘内 `disk_info.json` 原始字节保持不变，旧 key 不退役，新 key 不可发放。
  - 对旧 bug 已写入的精确 `updated_at = 1970-01-01T00:00:00Z` 过渡盘，仅当盘内状态为 `IMPORTED` 且移除该字段后 raw JSON 通过真实 Center HMAC 验签时兼容；错误签名或签名覆盖字段篡改仍拒绝。
  - 成功路径先激活新 key，再最终原子写新的 `INITIALIZED`、标准 `updated_at` 和当前 `center_signature`；若最终写盘失败，尽力把刚激活的新 key 回滚为 `REVOKED`，旧 key 只在写盘成功后退役。
  - 生产 runtime 增加新 key 激活回滚 SQL；`disk_list.last_init_time` 从 key 激活阶段后移到写盘成功后的旧 key 退役路径，减少最终写盘失败后的 DB 误标记。
- 验证：
  - `cargo fmt --all -- --check`：通过。
  - `cargo test -p rustfs-transfer-common`：通过，使用独立 `CARGO_TARGET_DIR` 绕开主 `target/debug/.cargo-build-lock` 权限拒绝。
  - `cargo test -p rustfs-transfer-center`：普通沙箱第一次因既有 `crates/center-backend/target/test-disks` 测试产物目录权限拒绝而 4 个初始化测试误失败；按绝对路径核验后仅重建该测试产物目录，并在提权测试环境下重跑通过。
  - `cargo test --workspace`：通过，使用同一独立 `CARGO_TARGET_DIR` 与提权测试环境，避免 Center 固定测试产物目录权限误失败。
  - `powershell -ExecutionPolicy Bypass -File scripts\check-deploy.ps1`：通过。
  - `git diff --check`：通过，仅有 LF/CRLF warning。
  - `git diff --name-only -- docs/v1.0冻结`：无输出。
  - `rg "^(<<<<<<<|=======|>>>>>>>)" crates/center-backend/src/reinitializer.rs crates/center-backend/src/reinitialize_runtime.rs work_sprint/TASKS.md work_sprint/dev_log/2026-08-10.md`：无命中。
- 后续部署窗口唯一安全步骤：
  - 仅部署新的 Center 后端二进制，不部署或触碰 Edge Web 契约窗口改动。
  - 部署前先只读备份目标盘 `disk_info.json` 并记录 SHA256；确认目标盘身份、SN 辅助匹配、ext4、`.partial=0`、盘内 `IMPORTED`、Center 登记启用状态。
  - 对已被旧 bug 写入 sentinel 的盘，只允许在确认唯一过渡差异为精确 `updated_at=1970-01-01T00:00:00Z` 且移除该字段后真实 HMAC 验签通过时调用受控 reinitialize API。
  - 若仍遇到 `remove .../rustfs-transfer/data: Permission denied`，先只读核验 service user、mount namespace、协议目录 owner/group/mode/ACL；经用户授权后仅对目标测试盘协议树做最小 `chown/chmod/ACL` 修复，再重试受控 API。不得手工 SQL、不得手工改 `disk_info.json`、不得手工删除 data 目录、不得直接 initialize 或绕过 API。
### Center b5a53e7 deployment stopped before ACL/API by transition HMAC gate

- Scope: Center VM and FUSTFS-TST-A only. Did not touch Edge, FUSTFS-A-DRILL, source RustFS objects, Center archive objects, or object_ledger. No ACL repair, reinitialize API call, direct SQL, format, rm, manual disk edit, or direct initialize was performed in this run.
- Deployed Center backend from commit `b5a53e7389b4dbd02bb1de701ec0ec510805e936`. Built release on Center `/tmp` with rustc/cargo 1.97.1 and installed `/opt/rustfs-transfer/rustfs-transfer-center` atomically after backup. New binary SHA256 `a367c6e260a47ea9673b51bd497649a30551f39aebbbc5d0b35d055c400ee048`; previous binary SHA256 `e83149d295da79ea8b45940063b5b32bb301da73a11677f8400aa43566d2ad09`.
- Service smoke after deploy: `rustfs-transfer-center` active; `/healthz` 200, `/readyz` 200, `/api/center/summary` 200.
- Readonly disk_info backup was written to `/tmp/disk_info.FUSTFS-TST-A.pre-b5a53e7.json`; backup hash matched live disk_info hash. Live disk_info SHA256 was `7d3305e0c1600f6ff5293d6639bcb380beedcfb1d0ace53d9f2b7f95619d9998`.
- Readonly gate results: identity matched `/dev/sdb1`, label `FUSTFS-TST-A`, FS UUID `0878ee5b-3e86-4ae0-8d0f-461c2732ee42`, hardware SN `323533394352343033383235`, ext4, and disk_id `9f8308ca-045e-425b-8664-4bb6fdaa0db3`; Center disk row was registered/enabled; disk_info status remained `IMPORTED`; `updated_at` was exact epoch sentinel `1970-01-01T00:00:00Z`; `.partial` count was 0; FUSTFS-A-DRILL was not present on Center.
- Stop reason: independent transition HMAC verification failed for the current epoch-sentinel disk_info state. Per instruction, stopped before any ACL repair or controlled reinitialize retry.
- Post-stop state: disk_info still `IMPORTED`, protocol payload counts remained data=1, meta=1, manifests=2, `.partial` remained 0; DB audit snapshot remained ledger_total=1, old key `ISSUED`, no ACTIVE key. Center service remained healthy. Journal grep found no new panic/error/fail/credential markers except the recurring system journal truncation warning.
- TASKS.md was not updated because cleanup/reinitialize did not complete.
### Center transition HMAC readonly retry still failed; stopped before ACL/API

- Scope: Center VM and FUSTFS-TST-A only. No ACL repair, reinitialize API call, direct SQL, format, rm, manual disk edit, or direct initialize was performed in this retry.
- Updated the one-time validation tool only. The in-memory HMAC check removes `updated_at` only when `disk_info.status.code=IMPORTED` and `updated_at=1970-01-01T00:00:00Z`. The signed coverage was restricted to `protocol`, `disk`, `center`, `security.center_key_id`, `security.data_key_id`, and `security.signature_alg`; it excludes status, edge, manifest, encryption_alg, center_signature, and updated_at. Output included only booleans, covered paths, canonical SHA256, type fingerprints, and per-section hashes.
- Readonly identity/gate results remained OK except signature: target `/dev/sdb1` mounted `/media/control/FUSTFS-TST-A` matched label `FUSTFS-TST-A`, FS UUID `0878ee5b-3e86-4ae0-8d0f-461c2732ee42`, hardware SN `323533394352343033383235`, ext4, disk_id `9f8308ca-045e-425b-8664-4bb6fdaa0db3`; Center disk row was registered/enabled; disk_info status remained `IMPORTED`; epoch sentinel was present; `.partial` count was 0; FUSTFS-A-DRILL was not present.
- HMAC result: `hmac_transition_valid=false`, canonical SHA256 `9810d87f27f7af89fb2d6ae91ff5879d3296fd5121e3df79156725716f798b43`. Covered value hashes: protocol `c2823fb776dfaab48bfa06a33005d02a60492d87762cdb66c9c4155f97fbaa5d`, disk `be608538a5715fc6785997bb95e724fb1db651de641553f08f4fa8c8f677d8a1`, center `9605d1eee79dbd6639c2efdff301d93042a41ef8f48007e6b68df64385153a59`, security_subset `c7ae565f39f25d701242d6e1ad533258602705564b8729baa986d35097e24817`.
- Per instruction, stopped before minimum ACL repair or controlled reinitialize retry. Center service remained healthy: `/healthz` 200, `/readyz` 200, `/api/center/summary` 200. DB audit snapshot remained ledger_total=1, old key `ISSUED`, no ACTIVE key.
- TASKS.md was not updated because cleanup/reinitialize did not complete.
### Center Rust API validation call rejected signature with zero-write evidence

- Scope: Center VM and FUSTFS-TST-A only. Per instruction, did not repair ACL/permissions and did not run chmod/chown/rm/manual SQL/manual disk edit/direct initialize. FUSTFS-A-DRILL was not present and was not touched.
- Executed one authorized controlled API validation call through deployed Center service: `POST /api/center/disks/9f8308ca-045e-425b-8664-4bb6fdaa0db3/reinitialize`.
- API result: HTTP 400 `disk_info signature invalid: disk_info center_signature verification failed`.
- Zero-write evidence: before and after disk_info SHA256 stayed `7d3305e0c1600f6ff5293d6639bcb380beedcfb1d0ace53d9f2b7f95619d9998`; status stayed `IMPORTED`; `updated_at` stayed `1970-01-01T00:00:00Z`; `.partial` stayed 0; protocol counts stayed data=1, meta=1, manifests=2, logs=0, quarantine=0.
- DB/archive audit snapshot before and after stayed unchanged: ledger_total=1; old key `ISSUED`; no ACTIVE key; archive ledger still points to bucket `archive-edge-demo`, key `test/codex-real-scan/20260809T173110Z-edge-VMware-Virtual-Platform-eaedeb0088e687bf-rustdesk-1.4.9-x86_64.exe`, source_size_bytes=24472432.
- Center service stayed healthy: `/healthz` 200, `/readyz` 200, `/api/center/summary` 200.
- TASKS.md was not updated because cleanup/reinitialize did not complete.

### TASK-S1-P1-EDGE-WEB-CONTRACT-001：Edge Web 浏览器公开只读契约合入

- 范围：仅合入 Edge 浏览器只读 Dashboard/导出记录契约；修改 `crates/edge-backend/src/control.rs`、`crates/edge-backend/src/server.rs`、`work_sprint/TASKS.md` 和本 dev log。
- 用户已明确授权最简公开只读方案：`GET /api/edge/dashboard/summary`、`GET /api/edge/dashboard/export-jobs`、`GET /api/edge/dashboard/export-jobs/{export_job_id}` 对所有可访问浏览器公开，不做本机限制、登录、代理认证或浏览器 token。
- 控制接口边界保持不变：`GET /api/edge/summary`、扫描、导出创建、启动、恢复等受控路径继续要求 `X-Edge-Control-Token`。
- 安全输出约束：浏览器只读接口不暴露控制 token、`disk_data_key`、`authorization_key`、nonce、tag 或 `data_key_id`；Dashboard summary 不向浏览器展示 `IMPORTED` 盘生命周期。
- 验证：`cargo fmt --all -- --check`、`cargo test -p rustfs-transfer-edge`、冻结目录未暂存检查、冲突标记检查、敏感字段暂存扫描。
- 未覆盖项：未部署 Edge 后端，未操作 VM/硬盘/SQL；前端三页任务需在本契约提交后单独合入与提交。
### Center zero-test disk reformat/register/initialize

- Scope: Center VM only. Did not touch Edge, source RustFS objects, archive objects, object_ledger, unrelated disks, system disk, or optical drive. No export/import was started.
- Initial irreversible gate used only `/dev/sdb1`, parent SN `323533383650343031383331`, partition size `1000202788864`, and non-system/non-optical block-device checks. Current UUID/label were recorded as audit only per latest instruction.
- DB cleanup: preview found old failed disk row already absent, one orphan `REVOKED` data_key for disk_id `aa236c58-1c7c-4871-937e-26490f46ad3d`, import_job count 0, and object_ledger count 0. Deleted exactly that one `REVOKED` key in a PostgreSQL transaction; post-check old disk/key counts were 0 and same-SN disk registrations were 0.
- Disk operation: formatted only `/dev/sdb1` as ext4 with label `RFS-ZERO-FRESH`. New FS UUID is `2cf552ec-62b8-4e75-84df-eb8da998d2ec`; mount path stayed `/media/control/RFS-ZERO-0810`; capacity stayed `1000202788864`.
- Center API: registered new disk_id `eeca55c4-ee87-4bcb-b797-7ef83899e172`. First initialize attempt failed with HTTP 400 `write disk_info.json under /media/control/RFS-ZERO-0810` because the fresh ext4 root was `root:root`. Applied minimal ACL only on that mount root for service user `rustfs-transfer` (`u:rustfs-transfer:rwx` plus default ACL) and retried the controlled initialize API.
- Initialize result: HTTP 200, disk status `INITIALIZED`, active data_key_id `c1e9d578-1aa9-476b-ad1e-c22a1e168027`. The earlier failed initialize for the new disk left one non-issuable `REVOKED` key `063bbeeb-32fc-4ac8-a790-c83c955e4c01`; it was not manually edited.
- Validation: disk_info fields match disk_id/SN/capacity/protocol 1.0/status `INITIALIZED`; center_key_id matches current config-derived key id; center_signature verification passed; signature bytes length 32; `.partial` count 0; protocol directories were recreated under `/rustfs-transfer`; Center service was active and healthz/readyz were OK. New disk import_job/object_ledger counts remained 0.
- TASKS.md was not updated because this was VM execution/logging, not a new code/task-status change.

### Edge UI/UE 交接文档范围重整

- 范围：新增非冻结文档 `docs/04-原型设计/Edge前端UIUE实现交接方案.md`，用于交接 Edge 前端 UI/UE 与前端实现规格。
- 调整：根据用户最新确认，已删除并剔除 `Edge 首次接入` 页面、`first-access` 路由、中控 URL、授权 Key、注册包、bootstrap 接口等关联内容。
- 当前页面范围：仅保留 `Dashboard` 与 `同步/导出记录` 两页；Dashboard 不再拆运输盘详情页或异常恢复页。
- 约束：文档继续强调复用 `GET /api/edge/summary`、Edge WebSocket、导出任务详情接口，并只标注导出记录列表/详情为最小只读缺口；保持 Edge 禁止初始化、清理、导入、人工取消和展示/写入 `IMPORTED`。
- 验证：仅文档新增，未修改冻结文档，未改前端代码，未运行构建。

### Center sealed transport disk controlled import

- Scope: Center VM and the mounted `RFS-ZERO-FRESH` transport disk only. Did not touch Edge, source RustFS objects, other disks, cleanup, reinitialize, manual SQL updates, or manual disk file edits.
- Readonly gate: target was `/dev/sdc1` mounted at `/media/control/RFS-ZERO-FRESH`, parent SN `323533383650343031383331`, FS UUID `2cf552ec-62b8-4e75-84df-eb8da998d2ec`, label `RFS-ZERO-FRESH`, ext4, partition size `1000202788864`. A stale `/dev/sdb1 -> /media/control/RFS-ZERO-0810` mount entry was observed but was not accessed as the target.
- Protocol precheck: disk_id `eeca55c4-ee87-4bcb-b797-7ef83899e172`, status `SEALED`, seal_id `2fec5fc3-ba73-4797-b668-22efc70c9a05`, export_job_id `502b0555-48bb-4b12-873a-f5e8f750ddf1`, manifest SHA256 `087e65249809560e880ddd44f74a083bbeecb49a099294165d83228e84cfe5ea`, object count 57, total bytes `951301462`, data files 57, meta files 57, `.partial` count 0.
- DB precheck: disk registration was enabled; import_job and object_ledger were 0 before import. Current data_key row for `c1e9d578-1aa9-476b-ad1e-c22a1e168027` was bound to edge `edge-demo` and export job `502b0555-48bb-4b12-873a-f5e8f750ddf1`; one older `REVOKED` key from the failed initialization attempt remained unchanged.
- Controlled API: called `POST /api/center/import-jobs/start` with mount path `/media/control/RFS-ZERO-FRESH`; result HTTP 200, import_job_id `2680f58b-c2d7-49b4-8cc1-550158dc60b7`, status `DONE`, outcome `IMPORTED`, progress 57/57 objects and `951301462/951301462` bytes.
- Post-import validation: disk_info status `IMPORTED`, imported true, reusable true, seal_id and manifest SHA unchanged, import_job_id recorded, center_signature verified with current Center key, signature length 32, `.partial` count 0. Center service active; `/healthz`, `/readyz`, and `/api/center/summary` were healthy.
- DB postcheck: import_job `2680f58b-c2d7-49b4-8cc1-550158dc60b7` was `DONE`, object_count/imported_count 57, total/imported bytes `951301462`. object_ledger contains 57 rows and `951301462` bytes for `edge-demo`, all pointing to `archive-edge-demo`; missing archive reference count 0.
- Risk to track: data_key `c1e9d578-1aa9-476b-ad1e-c22a1e168027` still showed status `ISSUED` and empty `seal_id` after import; no manual DB correction was made.

### TASK-S1-CENTER-HOTFIX-005：Center 导入成功后 data_key 封盘生命周期绑定合入

- 合入范围：仅从 `C:\Users\Beipy\.codex\worktrees\f86c\RustFSDisk` 审核并转移 Center 导入完成后的 data_key 生命周期修补与受控导入路由测试，涉及 `crates/center-backend/src/import_worker.rs`、`crates/center-backend/src/import_runtime.rs`、`crates/center-backend/src/lib.rs`、`crates/center-backend/tests/import_worker.rs`、`work_sprint/TASKS.md` 和本 dev log。
- 任务编号处理：来源 worktree 使用 `TASK-S1-CENTER-HOTFIX-004`，但主线已将该 ID 用于 `Center reinitialize disk_info 失败原子性修补`；本次合入改记为 `TASK-S1-CENTER-HOTFIX-005`，避免总览/卡片语义冲突。
- 实现内容：
  - `ImportRepository::complete_import` 改为接收 `ImportCompletion` 并返回错误，生产 PG 路径在同一事务中锁定 `import_job`、校验 `disk_id + seal_id + export_job_id`、封存同一 `data_key`，再把任务标记为 `DONE`。
  - 新增 `ensure_imported_data_key_sealed`，仅在同一 `disk_id + seal_id + export_job_id` 已 DONE 时幂等补齐旧 `ISSUED` key 的 `SEALED_READONLY + seal_id` 绑定。
  - 导入 manifest 校验要求每个对象的 `data_key_id` 与 `disk_info.security.data_key_id` 一致，避免成功导入后封存错误 key。
  - 受控导入路由补充测试覆盖缺失 `X-Center-Control-Token` 时不调用服务、有 token 时调用服务、服务失败时不返回成功。
  - 内存测试仓库记录 data_key 生命周期和 import job 的 `export_job_id`，覆盖成功封存、重复 DONE 补齐、解密失败不改 key、缺目标 key 不误改其他盘 key。
- 安全边界：
  - 导入失败、解密失败、manifest/key 绑定不一致时不得更新 data_key 生命周期。
  - 本任务只把导入完成后的本次 key 置为 `SEALED_READONLY`；旧 key 退役仍留给重新初始化成功路径，不在导入完成时置为 `RETIRED`。
  - 未修改 `docs/v1.0冻结/`，未部署、未调用真实 API、未操作硬盘或生产 SQL。
- 验证：
  - `cargo fmt --all -- --check`：通过。
  - `cargo test -p rustfs-transfer-center`：普通沙箱因 `target/debug/.cargo-build-lock` 权限拒绝未启动测试；使用同一命令在最小授权测试环境重跑通过，结果为 62 个库测试、14 个路由/HMAC 测试、10 个 import worker 测试全部通过。
  - `git diff --name-only -- docs/v1.0冻结`：无输出。
  - 未部署、未调用真实 API、未操作硬盘或生产 SQL；真实 VM 尚未部署验收该修补。
### TASK-S1-WEB-EDGE-001：Edge Dashboard 与同步记录页候选实现合入

- 来源核验：读取前端任务窗口 `019fe79a-9cbd-72d3-9a53-e354b158aa09` 最终说明与 `C:\Users\Beipy\.codex\worktrees\ca9c\RustFSDisk` worktree；该 worktree 基线较旧且夹带大量后端、Center、部署历史差异，本轮只做文件级白名单合入 Edge 前端候选。
- 合入范围：`web/edge-web` 的 `App.vue`、`DashboardView.vue`、新增 `SyncRecordsView.vue`、`style.css`、`api/edgeDashboard.ts`、`ws/edgeCopyProgress.ts`、单测、静态素材、`design-qa.md`/QA 截图，以及 `work_sprint/TASKS.md` 和本 dev log。
- 范围确认：当前前端仅保留 `/dashboard` 与 `/sync-records`，根路径归一到 `/dashboard`；未恢复 `/first-access`，未提供首次接入页、Center URL、`authorization_key`、注册包或 bootstrap 入口。
- 安全边界：浏览器只调用本地 Edge 公开只读 Dashboard/导出记录 API 与 Edge progress WS，不读取、不展示、不发送 Center 地址、`authorization_key`、控制 token 或 `disk_data_key`；页面不展示 `IMPORTED`，不提供初始化、清理、格式化、导入、取消或任意盘写入入口。
- 视觉 QA 事实：候选实现已完成 Dashboard 工业控制台与同步记录页候选视觉；粒子传输效果按用户最新决定不作为当前阻塞项。剩余可见残留为同步记录统计卡精确图标素材不完全一致，以及最终截图工具未稳定锁定 1440x810，仅保留同等 16:9 截图证据。
- 验证：
  - `web/edge-web npm run typecheck`：通过。
  - `web/edge-web npm run build`：通过。
  - `web/edge-web npm run test:unit`：通过，4 个用例覆盖本地只读路径、导出记录 URL、隐藏 `IMPORTED` 与过滤非 Edge 对象状态。
  - 主线本地 Vite 路由核验：`/dashboard`、`/sync-records`、`/` 均 HTTP 200；浏览器执行后 `/` 归一到 `/dashboard`，`/dashboard` 渲染 Dashboard，`/sync-records` 渲染同步记录，页面文本未出现 `first-access`、首次接入、`authorization_key`、`center_url` 或 `IMPORTED`。
  - `git diff --name-only -- docs/v1.0冻结`：无输出。
  - 未部署、未操作 VM/硬盘/DB，未调用真实业务 API。
