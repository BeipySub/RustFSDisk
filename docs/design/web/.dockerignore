# Local dependency trees and build caches are recreated from pnpm-lock.yaml
# inside the isolated-trial image. Excluding them keeps Docker BuildKit input
# bounded and avoids copying host-specific binaries into the image.
**/node_modules
**/dist
**/.turbo
**/.vite
coverage
.pnpm-store
*.log
