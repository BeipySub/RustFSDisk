# 只触发 systemd rescan 通知，不直接执行导出或导入业务逻辑。
# 部署到中控端或边缘端时，由 rustfs-transfer-disk-rescan@.service 通知本机常驻服务重新扫描磁盘。
ACTION=="add|remove", SUBSYSTEM=="block", ENV{DEVTYPE}=="disk", TAG+="systemd", ENV{SYSTEMD_WANTS}+="rustfs-transfer-disk-rescan@%k.service"
